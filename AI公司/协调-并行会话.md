---
最后更新: 2026-07-12 by campaign-continuous (session 178bffd9)
类型: 协调记录（machine-facing + owner-visible）
---

# 并行会话协调记录 — 两个 campaign 会话相撞的处置

## 发生了什么
2026-07-12 04:00 前后，**两个 Claude 会话同时在 bootstrap 同一个十日战役**：
- **session c6fe6446（campaign-bootstrap）**：先到，正在写 `campaign/lib.py`、`campaign/supervisor.py`。它 seed 的是**旧版**纲领「TEN-DAY CAMPAIGN — FINAL CHARTER」。
- **session 178bffd9（campaign-continuous，本记录作者）**：拿到的是**新版** `CONTINUOUS CAMPAIGN CHARTER`，其首句明令「**supersedes ALL prior campaign and Pi instructions**」。新版是旧版的超集，另含 §0 连续无终点 / §5 Pi 采纳 / §6 vault 分区细则 / §7 信任与反造假。

## 本会话（178bffd9）的处置决定（依据 CLAUDE.md §1.3 不重复造轮子 + parallel-session-coordination 记忆 + 纲领 §1 单写者原则）
1. **纲领单一真相源已更新为新版超集**（`战役纲领.md`）。因为 `campaign/lib.py` 在**运行时从 vault 读取状态/纲领**、不硬编码旧文本，所以本修正会**自动传播**进 c6fe6446 正在建的运行系统——修正真相源，正确行为自然扩散。
2. **不重写、不 clobber c6fe6446 的 `campaign/*.py`**。经审阅，它的续跑层（O_EXCL 锁+stale 检测、v2 RUN_WINDOW/兄弟环 deference、relaunch 天花板+指数退避、crash guard、孤儿回收、local-vs-vault 状态分离）质量高、比从零重写更好。重写它 = 重复造轮子 + 砸掉一个在跑的更优构建。**让 c6fe6446 拥有 `campaign/` 代码构建。**
3. **不启动第二个 launchd 守护**。`com.ai-architect.campaign` 是唯一 label（重复 load 幂等）；加上 O_EXCL 锁保证只有一个 orchestrator 进程活着。本会话**不 load 任何 campaign 守护**，避免双守护/双烧算力。
4. 本会话做**不相撞的增量**：§2.4 实战经验库调研（真实营收可核案例，证据分级，落 `实战经验库/`）。

## 存活的 orchestrator 必须遵守（新版纲领的旧版没有的部分）
- **§0 连续无终点**：无「stop」状态；十日只是下限，改进循环永不终止。
- **§5 Pi 采纳**（owner 已批，勿再评估）：① Haiku 级 permission 预筛（PERMISSION.md）；② context 降噪 + lazy skill/charter 加载；③ Pi 作为执行 substrate，分级切换含爆炸半径控制、Pi 无护栏故其自写扩展须 sandbox 并由本 harness 包裹；④ Phase B 秘书默认 Pi SDK。
- **§7 信任与反造假**：事实型断言用 CODE 裁（零误差裁判）；判断型才用不同来源集成，**绝不同模型重复 N 次**；任何展示的原则/manual 必须逐字源自 frozen 文件带 sha。

## 遗留核对（本会话 session 末 + 任何后续会话开工时做）
- [ ] c6fe6446 是否已 commit `campaign/` 并**证明**看门狗（kill→单份 resume；crash-loop→退避+天花板；与 v2 window 零相撞）？若已证明 → campaign 才算 live。
- [ ] 若 c6fe6446 stalled/死亡且 infra 未证明 → 下一个会话**接管**：补全、跑证明、再上 launchd。
- [ ] 确认全仓只有一个 `com.ai-architect.campaign` plist、一把活锁。
