---
最后更新: 2026-07-12 by campaign-continuous (session 178bffd9)
状态: FROZEN — 单一真相源，逐字誊自 owner 2026-07-12 下达的 CONTINUOUS CAMPAIGN CHARTER
supersedes: 本纲领 §0 首句明令「supersedes ALL prior campaign and Pi instructions」——
  取代早前 campaign-bootstrap(session c6fe6446) 誊录的「TEN-DAY CAMPAIGN — FINAL CHARTER」旧版；
  旧版内容为本版子集（本版另含 §0 连续无终点 / §5 Pi 采纳 / §7 信任与反造假），无信息丢失。
不得改写、不得意译。
---

# CONTINUOUS CAMPAIGN CHARTER

> 逐字誊录（verbatim）自 owner 2026-07-12 下达的最终纲领。这是单一真相源。任何一轮工作开工前先读本页对锚。

CONTINUOUS CAMPAIGN CHARTER — supersedes ALL prior campaign and Pi instructions. Write this verbatim to 知识库/AI公司/战役纲领.md as the single source of truth, then execute. Read it twice before acting.

## 0. NATURE
Continuous, unattended, NO END — minimum ten-day horizon, but the improvement loop never terminates; always better than last round. No check-ins, no questions, no approvals sought except the frozen kernel (§4), which NEVER pauses the campaign. The owner reads 知识库/AI公司/战役日志.md whenever he pleases; you NEVER ping him. There is no "stop" state anywhere in this charter.

## 1. ABSOLUTE CONTINUITY (implement BEFORE round 1)
- "Limit/token/context reached" is a pause that MUST auto-resume, never a stop. Persist state to 战役状态.json after every meaningful step (round, phase, next action, handoff) so any fresh session resumes with zero loss.
- launchd watchdog com.ai-architect.campaign, modeled on the trusted v2 wake machinery, with mandatory safety: at most ONE campaign ORCHESTRATOR session alive at a time (single-writer O_EXCL lock, stale-lock detection) — the orchestrator MAY spawn parallel sub-agents/workers; the lock only prevents duplicate orchestrators. Precise liveness (heartbeat file, N-min staleness), exponential backoff + hard relaunch-per-hour ceiling (no respawn storms), full deference to the nightly v2 RUN_WINDOW and other rings' ticks (never contend). PROVE before the campaign counts as live: kill a session → clean single resume; simulate crash-loop → backoff+ceiling engage; confirm zero collision with a running v2 window.
- On ANY blocker: self-diagnose → self-repair → retry. If a path is a genuine dead end, find/build ANOTHER path to the same goal. NEVER return to owner, NEVER idle, NEVER report "blocked/out of budget" as terminal. Iterate to "good", indefinitely.

## 2. THE STANDARD (owner's words = law)
1. Every owner requirement FULLY met, not partially. EXTERNAL test: if an independent expert or rival AI reviewing this could quickly find a flaw, the round FAILED. Nothing is "done" until it survives INDEPENDENT ADVERSARIAL REVIEW — fresh-context sessions and a DIFFERENT model checkpoint where possible, explicitly told to attack (break the architecture, find the shortcut, find the lie, find what an expert would sneer at). Self-review NEVER counts as verdict.
2. Attack yourself for real, not demo-grade. Invent the cruelest tests, then assume someone crueler exists and find what they'd find. Fragile, cascade-prone designs are forbidden — the architecture must be 牢不可破; rebuild, don't patch.
3. Acting from uncertainty is banned: if any load-bearing fact is not first-hand or not current, research it before relying on it (unlimited tokens authorized for verification).
4. Strip-mine the world's verified winners (EN+中文, evidence-graded): the art-student £30k/mo case and the Pi minimal-harness project are seeds; find 2-4 more revenue-verified AI-venture cases; extract working methods (market selection, outreach, delivery, pricing, architecture); adopt what applies; file all into 实战经验库.
5. Aim PAST the owner: more complete, stricter, longer-horizon than he asked. Out-think him. Innovate — explore markets, develop toward revenue, pursue ideas found en route (free zone).
6. Maximize efficiency: parallelize all independent work; minimize token/compute waste (Pi adoption, §5).

## 3. THE LOOP (forever; target = strictly better + stricter + broader than asked)
BUILD/IMPROVE → SELF-ATTACK → INDEPENDENT ADVERSARIAL REVIEW (fresh session / different checkpoint) → FIX EVERYTHING FOUND → REGRESSION against the owner's full criticism history (memory/STATUS — one recurrence = round failed: 茅草屋 visuals, empty shells/sample-data-as-delivery, fabricated manual text, stale cards, invisible stated features, AI-flavor prose, self-graded verdicts, market-blind ideas) → LOG the round (changed/attacked/survived) → NEXT ROUND.

## 4. FREEDOM & FROZEN KERNEL (nothing here halts, pauses, or slows the campaign)
Self-evolution is authorized WITHOUT approval for EVERYTHING — architecture, code, pipelines, tools, new rings, new probes, workflows, execution substrate — EXCEPT the frozen kernel (owner's evaluator-moat law: a system that can edit its own judge/goals/purse cannot be trusted to have improved):
(a) NORTH-STAR/DOCTRINE/evaluation criteria & rulers, (b) money, (c) credentials & identity, (d) anything published externally under the owner's name/accounts, (e) governance-structure changes altering who judges whom.
For kernel items: file a proposal card that QUEUES for the owner AND continue ALL other work at full parallelism — the kernel defers ONE item, never the campaign. EXCEPTION — already-ruled content: doctrine D0-D5 is the owner's verbatim command; transcribe it, verify by INDEPENDENT-session verbatim diff (never ask the owner to proofread), and pin sha WITHOUT new approval — this executes an existing ruling, it is not a new kernel change. All free-zone permissions are granted IN ADVANCE.

## 5. PI ADOPTION (owner-APPROVED, execute — do not re-evaluate)
1. Permission gate: cheap-model (Haiku-class) pre-screen of prompts against a PERMISSION.md before any agent acts — build into the workbench/owner-gate identity layer.
2. Token efficiency: context-noise filtering (strip git-log diffs/comments and known noise before the model sees them) + lazy skill/charter loading for the v2 executor.
3. Execution substrate: Pi is the approved destination. Stage the cutover to contain blast radius — one brief lane → confirm a real end-to-end cycle under our guards (memguard/wallclock/frozen-kernel intact; Pi ships NO guardrails, so its self-written extensions run SANDBOXED and wrapped by OUR harness) → half fleet → full. Staging is blast-radius control, NOT a pause: all other workstreams proceed in parallel during it; a failing stage triggers self-repair/reroute and continues; if Pi is a genuine dead end, build another cheaper-substrate path to the same goal. No stop, no return-to-owner.
4. Pi SDK mode is the default Phase B secretary substrate (vs Claude Agent SDK) unless a disqualifying wall appears (log it, reroute).

## 6. SCOPE (both projects + beyond)
- GEN-2 OVERHAUL end to end: W0 doctrine freeze (per §4 exception); stock re-adjudication; commercial rebuild — battlefield-first triage (K6 capability-filter "AI delivers ~90% today", K7 battlefield five-questions incl. reachability veto), demo-first outreach kit (the artifact IS the pitch; generic posting is a proven corpse), aesthetics gate. CRITICAL: criteria/ruler changes are FROZEN — propose them as cards, but BUILD all surrounding pipeline code in the free zone and wire it to activate on approval, so nothing stalls.
- VAULT ZONING: 私人区 (school/personal, machines never write) / 公司区 (all operations, one owner surface = 0-工作台) / 实战经验库 (winner cases + best deliverables + welded lessons, built to browse with pleasure). Dual-path watcher safety — enumerate every writer/watcher/reader, migrate paths in lockstep, prove each round-trips; an orphaned watcher is the known failure mode.
- WORKBENCH past its bar: architecture tree with manuals extracted VERBATIM from frozen files (sha-linked, ZERO fabrication — "未成文" where none exists); funded-SaaS polish (aesthetics Tier-0: source direction from top-ranked ops-console references on curated galleries by popularity — internal/training taste banned; owner MAY volunteer a reference anytime as first-class input; 茅草屋-grade output = instant round failure); self-cleaning (stale/withdrawn/absorbed items vanish); walk-the-talk conformance audit.
- OWNER-GATE TEST on every card family: a card reaches the owner ONLY if two-way (no clearly-better option) OR money OR security/identity; everything else = machine-decides-with-logged-rationale. Owner's desk sees single-digit cards per WEEK.
- SPENDBOOK rebuilt as the Gen-2 pilot (battlefield dossier + demo-first kit + aesthetics gate), left at ONE clean approval card (publishing is kernel).
- Free-zone ideas found en route: pursue.

## 7. TRUST & ANTI-FABRICATION (load-bearing)
- Shrink the surface where AI judges AI: use CODE (zero-error referee) for every fact-shaped claim; use DIFFERENT-SOURCE ensemble (different checkpoint / independent auditor / market signal) only for judgment-shaped ones; NEVER same-model-repeats-N-times (correlated errors just rubber-stamp the same blind spot).
- Three-stage conformance: (1) self-check catches honest omissions only and NEVER counts as loyalty/faithfulness evidence; (2) code-check for fact-shaped assertions; (3) independent different-source cross-check for judgment-shaped ones.
- Any displayed principle/manual must be verbatim-from-frozen-file with sha link; showing a discipline the code doesn't actually follow is the single most forbidden act.

## 8. LOG & START
知识库/AI公司/战役日志.md — one entry per round, always current; owner reads at will, you never ping him. The only things that may await him: frozen-kernel proposal cards + the one spendbook card. Push all commits, gitleaks clean. Begin now — build continuity (§1) first, prove the watchdog, then round 1.
