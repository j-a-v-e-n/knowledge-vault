---
最后更新: 2026-07-12 by campaign-R5
类型: 实战经验库 · 最佳交付（战役纲领 §6）
收录标准: 只收「有真实证据、可复现、过了独立评审或代码裁判」的交付；每条给工件的真实路径/commit，路径全部本轮核实过存在。
---

# 最佳交付

> 想看「我们做出来过什么水平的东西」，看这页。每条：交付是什么 → 硬在哪 → 工件在哪。

## 1. 工作台 v2 —— 本地文件业务状态的实时控制台（页面真审批）
- **是什么**：`http://127.0.0.1:8787`，5 tab 全真数据（8 环 + 0-工作台卡片 + pipeline 条目 + 真趋势），页面表单→幂等 intent→applier 代写卡上 verdict 行→现有 watcher 施加后果（环零改动）。
- **硬在哪**：每个数字标来源文件；owner 夜里在文件上办结，面板自动归零（自清洁实证）；架构 tab 的说明书全部逐字摘录 + sha 钉校验，无一句编造；两语言市场调研坐实这层没有现成工具。
- **工件**：`ai-architect/workbench/`（server/applier/intents/architecture）；调研 `workbench/research/`。

## 2. spendbook Gen-2 试点全套 —— 战场档案 + demo-first 外联 kit + 审美闸后的落地页
- **是什么**：商业探针按 Gen-2 教义重造的第一个完整样本：battlefield DOSSIER（五问+可达性）、逐目标 demo-first 外联文案、重建后的落地页。
- **硬在哪**：外联 kit 过 craft voicecheck 5/5 干净；页面重建走 D4 审美闸；全链停在一张干净的 owner 发布审批卡上（发布=冻结内核，机器不越权）。
- **工件**：`ai-architect/innovation/commercial/probes/spendbook.{DOSSIER,PAGE,TARGETS}.json`、`spendbook.KIT/`；0-工作台 对应审批卡。

## 3. 收入已验证 AI 生意案例库 v1 —— 战役 §2.4 的赢家原材料
- **是什么**：4+ 个 revenue-verified solo/小团队 AI 生意案例，证据分级（A–D），5 条跨案例复现 ≥3 次的可迁移打法。
- **硬在哪**：每个收入数字标注证据级、源全部 fetch 过；过 fresh-context 独立反证 pass（抓出并修正 2 处 mis-cite）；诚实声明没有一个案例达第三方审计级。
- **工件**：本目录 `收入已验证的AI生意-案例库-v1.md`。

## 4. 勘探环首跑覆盖地图 —— 「哪些人我们还听不见」的数据自证
- **是什么**：NAICS×信号源覆盖地图 21 段，盲区 top3=62 医疗/31 制造/52 金融；20 venue 合规调研只放行 1 个验活源；反向勘探 8 案。
- **硬在哪**：taxonomy 独立产出 ∉ finding#0 段（独立性=验收标准，代码断言）；科技偏向由数据自证而非自述；合规闸有真死因支撑（GummySearch 之死）。
- **工件**：`ai-architect/innovation/prospector/coverage/map.json`、`findings/`、`research/`。

## 5. ini2json 端到端自主交付 —— 架构师第一次作为「可用整体」跑完全程
- **是什么**：给一个新点子 → 设计 → 生成自证够格的验收 ruler → 真造工具 → 验到过 → 交付，6 段 durable 状态机（fork Burr），中途被 kill 后 resume 续跑成功。
- **硬在哪**：交付件过自己生成的 ruler 48/48；ruler 本身 branch 100%、oracle adequacy 1.0、faithless 版本被判挂；四方防作弊全 code/OS 判（builder 永远看不到 ruler）。
- **工件**：commit `1154d70`（ai-architect 仓库）；`architect/design_loop/`。

## 6. REAL-FAULT head-to-head —— 我们生成的 ruler ≥ 真实开发者修过的 bug
- **是什么**：用 Defects4J 构造法从 CPython configparser 挖真 fault，我们的自动生成 ruler 对 5/5 契约内真 fault 全中（含第一轮输了 2/4、靠可泛化改动补齐的诚实过程）。
- **硬在哪**：对手是真实世界 bug 不是合成 mutant；判决全 code-disposed（flip = V_fix 过 / V_bug 挂）；out-of-contract 案例如实标注不计分。
- **工件**：`ai-architect/architect/design_loop/gate/ruler/headtohead_real/REAL-FAULT-REPORT.md`。

## 7. 自学层无人值守闭环 —— 8 轮实跑：学→分级→沙箱试→校准尺子判→留/拔→存档
- **是什么**：scout 自动发现 → 信任分级 → 沙箱实测 → 已校准验收尺子（kappa 0.902）判 keep/discard → 记忆卫生复验降级，整链无人插手跑了 8 轮。
- **硬在哪**：4 能力全从带 bug baseline 学到 1.00；恶意候选外联被沙箱挡住（network_contained）；漂移工具被复验降级；负例照存。
- **工件**：`ai-architect/EVIDENCE-autonomous-learning.md`、`learning/`。
