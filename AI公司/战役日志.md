# 战役日志

> 每轮一条：做了什么 / 攻击了什么 / 什么活下来。owner 随时看，机器从不 ping。
> 纲领见 [[战役纲领]]；状态见 战役状态.json；实战经验见 [[实战经验库]]。

---

## Round 0 — Bootstrap：续跑引擎建成 + 看门狗证明（2026-07-12）

**做了什么**
- **纲领立权威**：把 owner 的 `CONTINUOUS CAMPAIGN CHARTER`（明令 supersedes ALL prior）逐字写入 `战役纲领.md`，取代早前一个并行会话誊的旧版（旧版是新版子集）。worker 每轮运行时从此文件读「逐字法律」，故纲领改动零代码传播。
- **并行撞车化解**：开工时另一个会话（c6fe6446）正实时建同一战役。判定「整合、不硬刚」——审阅其 `campaign/lib.py`+`supervisor.py` 质量高（O_EXCL 锁+stale 检测、v2/兄弟环 deference、relaunch 天花板+指数退避、崩溃循环退避、孤儿回收、本地锁 vs vault 状态分离），不重写、不砸。后 owner 主动关掉那个窗口，本会话接管补全。
- **补全 finish line**：`worker_run.sh`（无头单轮 round，subscription 计费，读纲领当律法）、两个 launchd plist、`campaign/launchd/memguard.py`（内存看门狗——v2 的 memguard 只管 /v2/ 且本机已停用，47GB 崩机前科；本 guard 精确只扫 supervisor 后代+登记孤儿 pgid，绝不误杀 owner 交互 claude）。
- **证明看门狗（纲领 §1 硬闸，fact-shaped 全 CODE-checked，非自评）**：`campaign/tests/prove_watchdog.sh` 四证全过 → `PROOF-RESULTS.md`：
  1. 单写者 O_EXCL 锁：活 holder 挡住第二个、stale holder 被打破重取；
  2. kill→干净单份恢复：SIGKILL supervisor → 孤儿 worker 被回收、恰好一个 supervisor 存活、sentinel 移除后干净退出；
  3. 崩溃循环→指数退避 + relaunch 天花板：快死触发 backoff_sleep、天花板挡住风暴、launch 数被 bound；
  4. 对 live v2 RUN_WINDOW 完全让路：零 worker 启动、零争抢。
- **memguard 已装载**（保护型，harness 允许）；自测 rc=0、扫描 procs=[] 无误杀。
- **§2.4 实战经验库**：调研落库 4 个真实营收 AI 生意（Base44 [A-]、Chatbase [B]、陈云飞 [B]、HeadshotPro [C+]）+ 反面案例（卖铲子飞轮）+ 5 条可迁移打法，证据分级、红队过、双语井挖干。见 [[收入已验证的AI生意-案例库-v1]]。

**攻击了什么 / 什么活下来**
- 自攻 + code-check：看门狗四证在 env-压缩计时下可复现全过。测试自身两个 bug（kill 打到脚本自身进程组、relaunch_log 污染）被抓出修掉——**证明有效，非摆设**。
- **诚实边界（未活下来 / 未验证）**：只证明了「续跑机器」牢靠；**没有证明「worker 真跑出好一轮」**——一轮真实 round 都还没跑过。这两个是不同的断言，不混。

**下一步为何最接近目标**
- 唯一卡点：启用无人值守守护会 spawn approval-off worker，被 harness 安全闸拦（[Create Unsafe Agents]+[Unauthorized Persistence]）。这**该 owner 拍**（授权 approval-off 自治=安全/授权决定，过 owner-gate）。已出 go-live 卡 + `campaign/go_live.sh`。
- 授权后：守护启动跑 Round 1（真实 BUILD 一轮），其质量由**它自己的独立评审闸**把关；坏轮被 proven 的退避/天花板/wallclock/memguard 兜住，不会风暴或崩机。**授权前 campaign NOT live。**

---

## Round 0.5 — 事故 + 双路独立对抗评审 + 全量修复（2026-07-12 05:1x，Fable-5 checkpoint）

**背景**：owner 04:54 亲自跑通 `go_live.sh`（launchd 守护装载、Round-1 真实 worker 启动）；随后 owner 切换模型至 Fable 5 并令"重做一遍"——执行为纲领 §2.1 的 **different-checkpoint 独立对抗评审 + FIX EVERYTHING FOUND**（推倒重写=重复造轮子，评审比重写更严）。

**事故（如实记，不粉饰）**：重验时 proof 测试与生产**共用状态目录**——删掉了 live 哨兵（= KeepAlive 触发器）并清空生产 events.jsonl（**证据被本机器自己毁掉**）；随后清僵尸的 pkill 误杀了 launchd supervisor。后果：Round-1 worker 无监管裸跑（无 wallclock 罩、memguard 因 children.json 被删而看不见它）。发现后 5 分钟内：worker pgid 重新登记、memguard 重新罩住（实测树 440MB 健康）、根因焊补。

**独立评审判决（两路 fresh-context，Fable-5）**：并发攻击评审 = HAS-ISSUES（1 CRITICAL：memguard 只查单进程不查**树总和**——47GB 事故正是"多个中等进程的子树"；2 HIGH：killpg 罩子代理只是假设从未实测、vault 状态无新旧比较可致回退；+4 MEDIUM/LOW）。合规攻击评审 = HAS-ISSUES（CRITICAL：「kill→干净恢复」在首次真实生产考验中**当场失败**且续跑引擎当初**没过独立评审就自评"已证明"**——正是甲方批评史里的 self-graded verdict 复发；+状态过期、陈云飞案例评级偏宽一档）。

**全部修复（commit a24b4e6，四证 3 连跑全过）**：
- 测试全隔离（专用临时目录）+ **生产在跑即拒测** + trap 杀净自起进程——事故根因焊死；
- **孤儿轮改收养**：supervisor 复活后对在飞的轮"等它跑完（wallclock 罩内），超时才杀"——owner 任何时刻恢复哨兵都不会误杀 Round 1；
- memguard 加**树总和上限**（largest-first cull 到 80% 上限；合成 7GB 树 code-check 通过）+ overlap guard fail-open（修掉自引入的 /bin/pgrep 路径 bug——瞎的看门狗比重叠更危险）；
- 锁 stale-break 补**锁龄**门槛（TOCTOU：刚创建/未首跳窗口不可被打破）；vault/local 状态**取新者**；reap 失败保留登记；worker 日志 50MB 轮转；
- **实测**（评审 HIGH#2）：真实 worker 进程树全体共享 pgid——killpg 罩得住（measured，边界如实标：setsid 后台进程可逃逸，由 memguard 后代分支兜）。
- 纲领誊录 byte-level diff 复核 = 零漂移；陈云飞评级 [B]→[B-]（21财经=编辑背书的转述自述，高于纯自述、低于独立核实——按评审把理由写明）。

**什么活下来**：锁/退避/天花板/让路四证（隔离重跑 3 遍全过）、纲领誊录、案例库主体、卡片格式与 owner-gate 分类。**什么没活下来**：初版"已证明"的自评判决（被推翻——真实考验暴露测试/生产耦合）、memguard 初版 scoping、初版 reap 语义。

**等 owner 的唯一动作**：Round-1 worker 跑完后恢复哨兵——`touch ~/ai-architect/campaign/state/CAMPAIGN_ACTIVE`（或重跑 go_live.sh，幂等）。安全闸把这个开关判给 owner 本人，机器不代按、不绕。

## Round 1 — Gen-2 pilot 第一刀：D4 审美闸 + spendbook 页面重建（2026-07-12）

**做了什么**
- **spendbook 探针页从 Arial 线框重建为 gen2 页并上线**（https://probe-spendbook.vercel.app）：Tailwind v4 + shadcn zinc token + Geist（Tier-0 选源即闸，出处在 `templates/gen2/README.md`）；hero/终端演示/真实痛点引文/三步说明/定价卡/FAQ 完整落地页。acceptance GET 200 + Stripe checkout 303 实测过。**顺手修掉线上真 bug**：旧页按钮价格重复渲染（"Pre-order — $19 — $19.00"）。
- **终端演示 = 真数据真算术**：一条真实 agent transcript 的 claude-sonnet-5 usage 事件，按 pin 死的 LiteLLM 价格表（commit 8447cd3a）计出复式分录；stale-table 硬失败演示用真实 2026-04-29 旧表（真的没有 claude-sonnet-5）。计算脚本+输出存 `runs/2026-07-12/gen2-evidence/preview-computation.txt`。
- **防编造代码闸**（`page_content.py`，fail-closed 且不崩 tick）：页面引文必须逐字（≥20 字符）链回 CK1 验过的冻结 dossier、link 一致；终端演示必须带出处 caption；结构闭世界。假引文实测被拦（测试 + 真 deploy 双证）。
- **D4 审美闸建成、report-only**（`aesthetics.py` = Lighthouse，§1.7 不手搓）：每次 deploy 落证据进 record；enforcement 等 owner 冻结 AESTHETICS.md（提案卡已排队 0-工作台，纲领 §4 内核唯二可等 owner 的卡型；owner-gate 台账已记）。冻结件 criteria.md/checks.py/DOCTRINE/NORTH-STAR/PROBE spec **一字未动**（评审 git diff 验证）。
- gen2 成为**所有未来探针的默认页**（"Nothing Gen-1 ships as-is"）；离线测试 86+10 全绿。

**攻击了什么 / 什么活下来**
- 两个 fresh-context reviewer 子代理独立攻击（页面质量线 + 声明/代码线，判决不由本会话自评）。
- **被击破再修复（blocking，评审的真捕获）**：终端演示把 1 小时 cache write 按 5 分钟档计价、少算 60%——恰是产品自己声称要杀的静默错价。修正为 above_1hr 档（transcript 的 ephemeral_1h 字段亲手复核）、caption 把这个坑写成卖点、分录平账断言焊进测试（D5）。
- **修复的 minor**：移动端账本断词（改横向滚动）；缺 OG/Twitter meta（已补，Reddit/HN 分享卡不再空白）；引文闸缺最短长度下限（cherry-pick 碎片防线，已焊+测试）；arXiv 具体数字本会话复核不到→按"查到 vs 推断"降级标注。
- **活下来的**：引文逐字性（评审用 HN Algolia API 独立验证两条 verbatim）；Stripe TEST 披露（4 处，无空壳冒充）；AI 腔排查零命中（"unusually restrained"）；Lighthouse 终版 a11y 1.0 / best-practices 1.0 / perf 0.98、零失败审计；单写者纪律（FSM 零触碰，mtime 取证验证）。
- **诚实边界**：① 页面重建 ≠ 市场结果——零外发（publish 跳过令维持原判），§1.5 只在 D3 外联真触达后才谈成败；② Reddit 引文外部验证被反爬墙挡（dossier 已知限制，如实标）；③ Tier-2 只有 reviewer pairwise（新页明确胜旧页、无回退），owner 口味台账未建；④ Tier-1 阈值是 DRAFT 尺子，未冻结不 enforce。

**下一步为何最接近目标**：纲领点名的 pilot 交付 =「battlefield dossier + demo-first kit + 审美闸 → 一张干净审批卡」。审美闸已到位，下一轮做 **D3 外联 kit**（战场点名目标 + 逐目标 demo 资产 + K5v2 合规通道）+ **criteria v2（K6/K7）提案卡** + triage v2 管线代码（自由区、批后激活）。

---

## Round 2 — Gen-2 pilot 第二刀：D3 demo-first 外联 kit + criteria v2 提案 + triage v2 代码（2026-07-12）

**做了什么**
- **spendbook 外联 kit 真做出来**（纲领 §6 点名交付）：5 个主发点名目标 + 4 bench，全部两条子代理线一手核验（HN Firebase API / GitHub API / arctic-shift 存档；28 条渠道规则官方原文引文经独立红队逐字复核 28/28）。**两发最强个性化 = 用对方自己公开贴出的数字算账**：lichenyang-spdev（langfuse#14249，他手算的 $0.28 vs $0.20 漏计——我们的复式账逐分复现 $0.276218，cache-write 腿单列可见）；gakugaku（litellm#29011，他审计出的 gpt-oss-120b 13.6× copy-paste 错价——在两版真实 pinned 表上跑出账差）。其余 demo 全部真数据（本机真实会话 usage 事件提取，内容剥离、提取器 committed 可复现）或如实标注 illustrative。
- **K5v2 渠道合规硬表焊成代码**（outreach.py CHANNEL_POLICY，每条带官方规则逐字引文+出处+证据级）：Etsy/小红书/闲鱼/Reddit-DM/GitHub 来源冷邮/自动化群发=结构性禁；冷邮=CAN-SPAM 六件套；GitHub 回帖=仅目标自开 issue、≤1 发/repo；中国收件人=opt-in 制（本轮补查《互联网电子邮件服务管理办法》第十三条，chicogong 因此移 bench）。合规判决直接重排了 kit：3 目标移 bench、1 升主发、2 换渠道——机器按硬表决、台账记 owner-gate.jsonl。
- **criteria v2（K4v2 器械/K5v2 合规/K6 能力滤镜/K7 战场五问含③一票否决）**：DRAFT 落盘 + 冻结件提案卡排队 0-工作台（尺子=内核(a)，只提案不激活）；**triage v2 管线代码自由区建好默认关**（checks_v2.py report-only、config [criteria_v2] enforced=false、sha 篡改闸、批后两行激活），冻结件 criteria.md/checks.py 一字未动（评审 git diff 验证）。
- **两张卡等 owner**：【商业探针｜外联kit审批】发给这N个人-spendbook（发送永远 owner 手；30-min rider 已挂 publish_watch 实测跑通）+【商业探针｜冻结件】批准triage-criteria-v2。
- **顺手抓修的真 bug**：publish_watch 对手贴渠道也写「窗口已从发布时刻起算」（与 FSM T9b 矛盾，spendbook 卡上实证误导）→ 按 auto/manual 分叉修正；过时 go-live 卡、作废手发回执卡、旧发布审批卡三张全部注记/清理。**事实更正**：R1 handoff 说的「跳过令」是对的——旧卡上的「发」属 02:51 未归因事故、03:04 已补救跳过；本 worker 早期误读为 owner 已批发，已在卡上与 KIT.md 更正（kit 现为 spendbook 唯一在推进的外联器械）。

**攻击了什么 / 什么活下来**
- 两路 fresh-context reviewer 独立攻击（kit 内容+合规线 / 代码+纪律线），判决不由本会话自评。**双双 HAS-ISSUES，12 条发现全部修复**：
  - 2 BLOCKING（两路独立命中同一处）：jfrog-boost 文案头仍是灰渠道 github-discussion 而声明是 reddit-thread-reply——**错标渠道能骗过合规闸**。修：文案重写为真 Reddit 帖内回复 + validate_kit 焊上"文案头渠道必须与声明一致"码检 + 测试（D5：错误变结构）。
  - 1 BLOCKING/HIGH：zero-intent 判读没查预注册的「全部 N 条发出」要件，部分发出+沉默会被误杀进 killlist 且 kill 理由字符串撒谎（"kit fully sent"）。修：n_targets 要件 + 部分发出永不判杀 + 测试。
  - 1 HIGH：kit 卡无 done/ 归档路径（违背金库常规）。修：判读收口自动注记+归档 done/（repo runs/ 留档），卡上生命周期如实写明。
  - 2 MEDIUM 代码：write_kit_card 重跑会清 owner 已填数据（加 refusing-to-clobber 闸+测试）；scan 无逐文件隔离+非原子写（加隔离+tmp-rename+测试）。
  - 1 MEDIUM 合规：GitHub 回帖 "allowed" 判定基于自认推断（官方无单条裁定）而 3/5 主发依赖它——修法=把推断和风险归属逐字亮在审批卡上，owner 可回 edit 剔除 GitHub 三条。
  - 2 MEDIUM 诚实性：13.6×（单价）与 11.5×（账面总额）两个基数未分标（已分标）；两封信展示了不存在的 CLI 语法 `$ spendbook post`（改为如实的 prototype-output 框架）。
  - 1 LOW：rstagi 开头"stuck with me"违 facts-only 语域（已改事实句）。
- **评审验证为真的**：全部 7 组账本数字用 committed 工具逐字复算一致；全部 issue/HN/博客引文 gh api/直抓核验 verbatim；email 出处确非 GitHub 抓取；usage 事件确认零对话内容；无 transport 代码（机器只造 kit 成立）；冻结件零改动；v2 默认关对下游零行为变化（dashboard/collectors/kpi 消费路径逐一查过）。
- **回归批评史**：无已知失败复发（空壳=无，卡上声称的机器行为全部真接线并实测；编造=零，评审专项核验；stale 卡=本轮反向清掉三张；self-graded=判决全部出自 fresh 评审）。
- **诚实边界**：① kit 建成 ≠ 市场结果——§1.5 只认真实回复/意向数据，等 owner 批发后的 14 天窗；② GitHub 单条回帖合规性是推断非裁定，风险已亮卡；③ 闲鱼禁令结论是 C 级二手源（官方页不可达，如实标 UNVERIFIED）；④ jfrog-boost 的 Reddit 引文评审端被反爬挡、未能独立复核（调研端 arctic-shift 存档核验过，降半级如实标）。

**下一步为何最接近目标**：两张卡等 owner 期间不空等。Round 3 = W3 GENERATE 出生带战场（candidate schema 加 battlefield 字段 + 勘探环可听段接线——D2 在想法出生处的焊接，比等审批更接近"想法生而有市场"）+ 处理 owner 任何卡上回词（批 criteria v2 → 冻结+激活；发 kit → 窗口起算）。

## Round 3 — Gen-2 第三刀：W3 出生带战场 + 勘探环接线（2026-07-12）

**做了什么**
- **想法出生即带战场（二代改造方案 §5 W3 / 教义 D2「市场选择先于产品」）**：GENERATE 与 owner 投递结构化（intake）两个出生口的 schema 都加了 `battlefield{segment, where_they_live, why_them, reach_path}` 四要件，字段词汇与 `commercial/checks_v2.BATTLEFIELD_FIELDS` 完全同源（出生到 dossier 一套词汇；方案 §5 的第 5 字段 outreach_form 有意折入 dossier 阶段的 D3 instrument.choice，注释如实记录此偏离）。反 Goodhart 写死在规则里：默认种子=引用信号所在的那个社区（"你确实在那里听到了那个痛"）、听不见的段只许诚实标二手证据、宁要薄而诚实不要自信的雾。
- **勘探环可听段接线（W3 第二半）**：`coverage_context()` 只读勘探环 coverage map，把「哪些 NAICS 段我们的信号源真听得见/聋着」渲染进两个出生 prompt；fail-open（map 缺失/坏损降级为诚实一行，绝不弄死每日 run，评审 fuzz 实测）。方案 §5 的「合规听点」数据勘探环侧尚不存在（map/feed_reach 只有可听度），如实记边界、等勘探环产出后接。
- **report-only 硬边界（冻结内核零触碰）**：出生尺子 criteria.md C1-C6 + gate/checks.py 一字未动（评审 git diff 验证）；gate 只在 verdict json 里记 `battlefield_birth{enforced:false, present, errors}`（复用 checks_v2.ck4，try/except 包死绝不影响裁决），日报加一行出生带战场率并注明「只查非空、实质待 K7 批后判」。enforcement 属 criteria v2 K7，卡仍在 0-工作台 排队。
- **battlefield 全链可见**：gate verdict → 日报行 → 商业池（exceeds-envelope 候选把出生战场带进 dossier prompt，明令"当起始假设、用证据改判、绝不静默丢弃"）。
- **顺手修掉真 bug（评审独立验证为真）**：owner 投递的超纲（exceeds-envelope）想法进商业池时 `idea=""`——旧代码只读 candidates.json 从不读 owner-candidates.json，owner 的想法正文整个丢失；已修+回归测试钉死。
- 本轮为**双会话接力**：上一 worker 完成 BUILD 后被杀（未提交、未评审），本 worker 从状态与工作树无损接上，走完评审→修复→收尾。

**攻击了什么 / 什么活下来**
- 两路 fresh-context 子代理独立攻击（代码+纪律线 / 设计+诚实线），判决不由本会话自评。判决：一路 SOUND（3 LOW）、一路 HAS-ISSUES（2 MEDIUM + 3 LOW），无 blocking，全部修掉或如实记边界：
  - [MED×2 编造引文回归风险] 注释写「charter §6 W3」但纲领里 grep 不到 "W3"（W3 只住在方案 §5）；且字段清单引错节（§4b 无字段名、实际在 §5）+ 砍掉 outreach_form 的偏离没记录。→ 全部改为可 grep 核验的出处 + 一句话如实记录偏离。这正是甲方批评史里「编造手册文字」的族——被独立评审在提交前抓住并修掉。
  - [LOW] corpus_hits 为 None 时渲染 "corpus hits: None"（评审 fuzz 抓的）→ 修 + 钉回归测试；[LOW] reach_path 被兼职当证据成色标记（语义混载）→ 二手标记改挂 why_them；[LOW] 日报出生率指标对机器候选近乎恒真（schema 强制四键必填）→ 日报行如实注明只查非空、防止读成实质合格率。
  - **如实记的未修边界**：① 正常过闸→晋升 BRIEF 的路径尚不把 battlefield 写进 BRIEF 文件（COMPILE_PROMPT 未动；方案自己的路线图排在后续 W 刀）；② 合规听点未接线（上游数据不存在）。
- **评审验证为真的**：冻结件五文件零 diff；GATE_PROMPT 未动、LLM 判官看不到 battlefield 规则、不可能借道杀候选；report-only 全链 code-true；owner-candidates.json 读法与真实生产者 schema 逐字一致；引用的 DOCTRINE D2 / 方案 §4b§5 / criteria-v2.DRAFT K7 / 0-工作台卡全部真实在位；无循环 import、checks_v2 零 import 副作用。
- **测试**：172 全绿（tests/ 77 + commercial 52 + prospector 9 + supervisor 10 + craft 10 + workbench 14），含新 W3 套件 12 条 red-first。
- **回归批评史**：无已知失败复发（空壳=无，battlefield 三处真消费；编造=评审抓住两处引文不精确、提交前修净；stale 卡=本轮零新卡；self-graded=判决全出自 fresh 子代理；市场盲=本轮正是焊它的刀）。

**owner 卡零新增**：criteria v2 卡与外联 kit 卡都还在 0-工作台 等回词（本轮开工前查过，decision 行均空）；「凌晨五连批」A/B 也仍待答。

**下一步为何最接近目标**：执行序走到 ⑦ W5 四环轻改——workbench M2 加两卡型（审美闸 pairwise、外联 kit 批准）+ craft 可选外联文案过 VOICE 闸（自由区直接建），supervisor Gen-2 教义镜头属修宪→提案卡排队；勘探环侧推进合规听点产出（正是本轮记下的两个边界之一的解锁钥匙）。想法已生而带市场，下一刀让四环把 Gen-2 教义各自咬合。

## Round 4 — Gen-2 第四刀：W5 四环轻改（2026-07-12）

**做了什么**（执行序 ⑦，方案 §7 W5 表四行逐行落）
- **勘探环（接线，冻结章程零触碰）**：合规听点从「散落在滚动 runs/ 存档」变成稳定段级工件——新 `prospector/listening.py` 把 stage_research 的 vetted venues 投影成 `coverage/listening_posts.json`（hard NO-list venue 排除、probe 结果逐字搬运、纯增量自有工件走写守卫）；已从 07-10 真实 run 回填（62 医疗 7 听点 / 31 制造 7 / 52 金融 6，probe 验活仅 GoActuary 1 个）；innovation 端 `coverage_context()` 新增只读渲染分支喂进 GENERATE/intake 出生 prompt（独立 fail-open，坏档只掉节不掉 run）——**R3 记的边界②就此关闭**：聋区战场的 reach_path 现在有具体可指的合规 venue，渲染里如实写明「NOT ingested feeds、痛证据仍是二手直到 owner 批源」。
- **craft（可选过闸，冻结件零触碰）**：`commercial/voicecheck.py` 只读复用 craft 的 vale_lint（facts-only register 硬规）+ stylecheck（advisory），report-only、验证过未接进 validate_kit（可选=结构性的，测试断言 outreach.py 源码里无 voicecheck）；真 kit 实跑 5/5 干净（证据 runs/2026-07-12/voicecheck-spendbook.json，Vale 不可用时如实报 UNCHECKED 绝不装干净）。
- **workbench（两卡族）**：frozen-criteria（批/edit/驳）+ frozen-aesthetics（冻结/edit/缓）注册进 readmodel/intents/render 三处——此前两张冻结件真卡被通配前缀吞成 attachment、页面上根本无法审批；现已 kickstart 重启后 HTTP 实测两卡按钮齐全、applier 全链可写。
- **supervisor（修宪提案+预建默认关）**：`doctrine_lens.py` 照 premortem 先例预建（LLM 起草+代码把关：违规判决缺 D0-D5 键/环/证据即拒收；DOCTRINE.md 只读、sha 记录在案），config `doctrine_lens=false` 默认关、周报逐字节不变（渲染工件驱动）；修宪提案卡【监事会｜修宪】批准Gen-2教义镜头常设节-2026-07-12 排队 0-工作台，owner-gate 台账记 kernel (a)/(e)。
- **顺手抓修的真 bug（本轮最值钱的捕获）**：`decision[:：]\s*` 跨行正则——空 decision 行会把下一个 Markdown 标题吞成「已填值」，AESTHETICS 真卡实证（readmodel 误判已填→卡失去按钮，applier 误判 conflict→页面审批必败）。workbench 5 处 + 评审追出的六文件 9 处（activate_watch/prospector.watch/supervisor.watch/publish_watch/outreach/craft.approve）全部改 `[ \t]*`，红先回归测试钉死。

**攻击了什么 / 什么活下来**
- 两路 fresh-context reviewer 独立攻击（代码+纪律线 SOUND+1MED / 声称+诚实线 HAS-ISSUES 2MED+1LOW），判决不由本会话自评。全部修掉：
  - [MED 声称线] workbench 页脚对冻结件族谎称「现有 watcher 施加后果/等 watcher 办结」（正是批评史「不实声称」族，提交前抓获）→ 页脚按 family 分叉如实写「本族无 30-min watcher：战役轮工作者开工读 0-工作台 时施加后果」，测试+线上双证。
  - [MED 代码线] 跨行正则残留六文件未修未记 → 全修（见上）。
  - [MED 声称线] W5 偏离未显式记 → 见下方边界①。 [LOW] 修宪卡零 wiki-link → 已补。
- **评审验证为真的**：11 个冻结件路径 git diff 零改动；listening_posts.json 与源 blindspots.json 逐段核对（7/7/6、non_compliant 4/1/3）字段逐字无编造；coverage_context 接线真达 LLM prompt（非装饰）；voicecheck 字节级复现+planted 违规真报警；两卡族 sqlite readmodel 实证分类正确；doctrine lens 默认关代码为真；常设问与方案 §7 逐字节一致（无编造手册文字）；测试非恒真；AI 腔零命中。
- **回归批评史**：无已知失败复发（页脚不实声称在提交前被独立评审抓获修净——闸在工作；空壳=无，四项全部真接线实测；编造=零；stale 卡=零新增；自评=判决全出自 fresh 评审）。测试全绿：pytest 156 + 勘探 35 + 监事 71 + craft 离线。

**如实记的边界**
- ① 方案 W5 workbench 行点名的「审美闸 pairwise 裁决」卡型**未建**：pairwise 卡的生成器不存在——那族卡由 Tier-2 审美闸产生，等 AESTHETICS.md 冻结（卡在 0-工作台 等回词）激活后才有真卡可审；现在注册空族=空壳。「外联 kit 批准」族 R2 已建（caf7ad8）。本轮实际建的两个冻结件族修复的是两张**现存真卡**页面不可审批的真缺口。
- ② supervisor KPI「demo 外联转化列」未预建：kit 未发出、转化数据不存在，先建列只会永久空表；已写进修宪卡（批后随首份真实 outreach 数据一起接）。
- ③ 听点的诚实成色：62/31 段听点全部无可用 feed（api-key/blocked/no-feed），「人在那里但机器听不进」，触达要靠 D3 人工渠道；probe 验活仅 GoActuary（其 R2 批准已被凌晨事故 void，不算已批源）。
- ④ voicecheck 5/5 干净是对 R2 已修过语域的 kit 的复核，增量价值在未来新 kit 的机械闸，不是新发现。

**owner 卡**：+1 修宪卡（教义镜头）；criteria v2 / 外联 kit / AESTHETICS 三卡 decision 仍空，凌晨五连批 A/B 仍未答（开工时查过）。

**下一步为何最接近目标**：执行序走到 ⑧ W6 金库分区（私人区/公司区/实战经验库，先枚举全部 writer/watcher/reader、双路守卫、逐条 round-trip 证明——独立轨可并行开工，切换窗口等卡清零）；owner 若在任何卡回词，下一轮开工先执行。

## Round 5 — W6 金库分区：三区落定 + 私人区写闸（2026-07-12）

**做了什么**：执行序 ⑧（纲领 §6 VAULT ZONING）。① fresh Explore 子代理全库枚举 vault
writer/watcher/reader（含 launchd + 三个仓库外项目），成册 `campaign/VAULT-ZONES.md`；
枚举证明全部机器写点已在公司运营路径、私人区零机器写 → **裁决零物理迁移**（分区=清单声明+
默认拒绝写闸；orphaned watcher 已知失败模式本轮风险为零；「切换等卡清零」因无切换而不再适用）。
② 写权清单 `campaign/vault_zones.json`（公司区六写面允许名单）+ 写闸 `campaign/vaultzone.py`
（stdlib resolve+is_relative_to，带源调研；默认拒绝、symlink 展开、NFC、firmlink 剥离）+
接线 workbench/applier（web 动态卡名唯一入口：非空平名检查+闸）。③ 实战经验库补齐纲领三件套：
赢家案例 v1（已有）+ 最佳交付 7 条（路径全核实）+ 焊住的教训 16 条（每条带 commit/STATUS 源）+
索引；vault 侧 owner 页 `AI公司/金库分区.md`。④ owner 回词巡检：唯一已填的 spendbook「发」
坐实为 02:51 事故残留（03:04 已补救记跳过），真实回词全空、无待执行裁决。

**攻击与判决**：自攻抓出空卡名 IsADirectoryError + pathlib 不折叠 `..` 两坑（红先测试证）。
双路 fresh 子代理独立评审（证据核查 + 红队攻闸）：**HAS-ISSUES，1 CRITICAL + 4 MED + 3 LOW**。
CRITICAL=无人值守 campaign worker 本身跑在 bypassPermissions 下不受 python 闸管、owner 页
「由代码强制」过誓 → 修：新增 PreToolUse hook `.claude/hooks/vault-zone-guard.sh`（复用同一
manifest 判死 Edit/Write/NotebookEdit，Bash 写形命令粗闸；六用例实测），owner 页改为如实分层
表述。MED：枚举表漏 5 个 watcher 兼 writer（补齐）；firmlink `/System/Volumes/Data` 别名管辖
逃逸（闸剥别名+测试）；craft/commercial 动态卡名 f-string 无消毒（`safe_card_name()` 三处接线+
测试）；测试咬不住 startswith 突变（兄弟前缀向量补牙）。LOW×3 记档于 VAULT-ZONES §5。
评审同时核实：教训 11/16 深查无编造、交付 7/7 路径真实、突变检查声称精确复现、无 AI 腔。

**什么活下来**：写闸+hook+消毒+30 项测试（campaign30/workbench19/craft10/commercial22/
prosp+sup21/ops 全绿）；实战经验库三件套；枚举册与分区清单。**诚实边界**：LLM worker 的
Bash 写只有粗正则（内核级 sandbox 属后续轮）；固定路径 python 写点靠 conformance 而非逐点
运行时闸；claudian/douyin（owner 私人自动化）不在辖内。

**回归批评史**：空壳（闸经评审活体验证）/编造（抽查通过）/stale 卡（零新卡）/隐身特性
（文档=修后事实）/AI 腔（评审确认）/自评判决（判决全出自 fresh 子代理）/orphaned watcher
（零迁移）——零复发。

**下一轮**：执行序 ⑨（纲领 §5 Pi 采纳 staging：permission gate 预筛已批准执行）或按状态 json
next_action。

## Round 6 — W7 owner-gate：三问出卡闸焊进每个写卡点（2026-07-12）

**做了什么**：执行序 ⑨（纲领 §6 OWNER-GATE TEST）。① §1.7 前置调研（fresh 子代理，带源）：
OPA/Cerbos=policy daemon、LangGraph/OpenAI-SDK=agent-runtime 特性、HumanLayer(已自弃)/
gotoHuman=SaaS——三类形态全不匹配"本地文件卡+stdlib+无常驻服务"→ 排除法定案：在既有
owner-gate.jsonl 台账上写薄层，借 OPA decision-log 字段概念（decision_id/input）+ AWS
AGENTSEC04-BP02 铁律（分类器必须确定性规则、不许 LLM 过闸；全过人审=rubber-stamp 反模式，
恰是 owner"个位数卡"目标的权威背书）。存档 `ledger/research/2026-07-12-ownergate-survey.md`。
② `ledger/ownergate.py` + 注册表 `ledger/card_families.json`（19 族逐一过三问带理由；表外
族默认拒绝，先例 vault_zones）：gate()=准入+记账+周预算旗（>9/周亮 budget_breach 不硬扣）、
machine_decide()=代决记账、maybe_weekly_digest()=每 ISO 周一封「机器自决周摘要」进 inbox
（7 天否决窗、写前过金库分区闸、空周不发）。③ 焊接**八个**写卡点（commercial 行动卡三卡组/
外联 kit/craft 发布+冻结/craft 蒸馏提案/激活卡 create-refresh 去重/监事会/勘探环/ops 双路
fail-open——事故通道不因记账失败被压制）+ publish_watch 30-min tick 挂周摘要 rider（生产
launchd tick 当场实证跑通）。④ workbench 注册修宪卡两族（campaign-amendment verdicts/
supervisor-amendment decision），server kickstart 重启后页面渲染验证。⑤ 修宪提案卡
【战役｜修宪】准许两环降级-2026-07-12 上桌（内核 e 排队）：裁决-1 监事会可逆时间盒提案自动跑、
裁决-2 勘探环合规验活源自动加、裁决-3 独立重裁 02:51 被 void 的 5 条（GoActuary RSS + 4 个
联邦公开数据检测器）——一词一事，5 条批后逐条进周摘要可单条否决。

**攻击与判决**：自攻抓出两个真 bug——(a) 环 offline 测试真走焊后写卡路径，往**生产审计台账**
灌了 12 行测试垃圾（07-11 test_regen 清生产 panel-seen 同类病）→ 根修 repo 根 conftest.py
autouse 沙箱 + 默认参数运行时解析，垃圾清洗+记档；(b) 09:30 创新环 wrapper 的 sweep 把在途
焊点改动卷进 a5b01b2（sweep 只卷 tracked 留 untracked → "焊点入库、被 import 的模块还在
树外"的断链 commit），侥幸未推出去，补齐提交一次推送修复 → 焊住的教训 #17。fresh 子代理
独立评审判 **FAIL：2 CRITICAL + 4 MED + 2 LOW** —— CRITICAL#1=craft/distill.py 是第八个
写卡点、漏出"七处焊全"清单（活体，craft tick 每轮在跑）；CRITICAL#2=修宪卡声称"网页表单
也能回词"但 applier 的 verdicts 路由硬编码 (supervisor,prospector)，本卡族掉 decision 分支
永远被拒（正中"编造手册文字"回归类）。全修：distill 注册+焊闸+WELDS 补条目；applier 改
FAMILY_GRAMMAR kind 驱动路由（主判+self-heal 两处）+ 两族端到端回归测试；MED×4（台账假证据
指针→append-only 更正行 529322d8f429；裁决-1 挪用勘探环事故→改归属+"这句是推断"逐字标注；
周摘要否决措辞→"人手执行、无自动回滚 daemon"；ownergate 头注补"管辖边界"三条）；LOW#7→
裁决拆三行。**复核判决 PASS**（评审重放自己的 repro 验证修复、自跑 220 tests 全绿）。

**什么活下来**：出卡闸+19 族注册表+八焊点+conformance 测试（22 条新测试，全套 202-220 绿）+
周摘要 rider（周一首封 W28 真摘要自动落 inbox）+ 修宪卡（3 裁决行，页面可回词）+ 台账 15 行
（append-only：12 原 + 卡行 + 清洗行 + 更正行）。**诚实边界**：ops 两族闸=记账不是准入
（fail-open 写死+声明）；手写卡焊不了代码闸=会话契约+自记 gate() 行；记账先于落盘，Drive 挂
时台账可能多记一张未送达的卡；否决窗是"下个会话人手执行"不是 daemon；评审 residual：卡行
169de1de4ee1 没补裁决-3 拆分的伴随注（卡内容自洽为准）、:8787 渲染是我 curl 验的、评审未独立
复核（数据层已由测试证明）。

**回归批评史**：空壳/隐身特性（两 CRITICAL 恰是此类，修+测试+活体验证）/编造文字（三处全修，
含台账自引证据）/stale 卡（零新增；spendbook 四卡 11:00 tick 自动善终）/AI 腔（评审未标）/
自评判决（FAIL→修→复核 PASS，判决全程在 fresh 子代理手里）——已知失败零复发。

**owner 卡**：+1 修宪卡（两环降级+5 条重裁，3 裁决行）；criteria v2 / AESTHETICS / kit /
监事会修宪 / 凌晨五连批 A/B 开工时查过，全部仍空。本周出卡 5 张（预算 ≤9，台账可查）。

**下一轮**：开工照例查 owner 回词（现在 6 张卡在等，任何一张有词先执行）；建议执行序=预建
两环降级执行机制（监事会自动跑可逆实验 + 勘探环合规源自动加，代码自由区预建默认关、修宪批后
一行激活——纲领 §6 明令模式）+ 纲领 §5.1 permission gate（Haiku 预筛 PERMISSION.md，已批准
执行）进 workbench 身份层。

---

## Round 7 — §5.1 permission gate 建成 + 三评审判 FAIL→全修→复核 PASS（2026-07-12）

**做了什么**：执行序 ②（纲领 §5 PI ADOPTION 第 1 条 permission gate，owner 已批、不再评估）。
① §1.7 前置调研（fresh 子代理，带源，`ledger/research/2026-07-12-permgate-survey.md`）：六家现成
方案（LlamaFirewall/PromptGuard2、NeMo、GuardrailsAI、llm-guard、Invariant Gateway、superagent）
逐一对约束——全形态性不匹配（HF 模型下载+torch / 常驻 proxy / 接管应用的框架）且问题不同构
（它们做内容安全/注入检测，本任务做自家权限策略判定）→ **不 fork，薄层自建**，三样借现成智慧：
判定内核=Anthropic 官方 harmlessness-screen 成文模式（Haiku + 强约束 JSON）；fail-semantics=
Progent「动作空间只准收缩」+ OWASP LLM06「授权在下游」+ AWS「LLM 不是 security enforcement」。
② `ledger/PERMISSION.md`（辑录既有冻结裁定，B 段每条带出处，唯一综合项 B.8 明标非逐字）+
`ledger/permgate.py`（Haiku 预筛，只判 **DENY/DEFER、永无 ALLOW 权**；故障/超时/pin 不合/闸被关
一律 DEFER 落到确定性闸，且**留痕** `_degraded()`）+ config sha 钉 PERMISSION.md。
③ 焊进**两条** `claude -p` 路径（本仓全部模型调用面）：`v2/lib/claude_cli.call`（innovation 全家
+craft+ops+v2 executor）+ `learning/subscription.complete/complete_meta`（learning 环，含 ai_itself
的 WebFetch/WebSearch agentic 调用）+ 最高权限 agent `campaign/supervisor.run_one_worker` 开跑前筛
行军令。④ workbench 架构树加 permgate 节点（sha 钉合校验 + enabled 状态可见）。
**真 Haiku 实测四类 prompt 全对**（证据 `ledger/research/2026-07-12-permgate-live-validation.md`）：
恶意 force-push+发布+花钱→DENY、凭证外传+注入→DENY（注入未翻转判定）、行军令→DEFER、讨论禁令→DEFER。

**攻击与判决**：自攻先抓出两真 bug 全修——(a) `ops/tests/test_vault_guard.py` standalone 直跑往
**生产** owner-gate 台账灌 2 行测试垃圾（R6 同型病灶残留缺口，1f2f015 焊了四环套件漏了本套件）→
补焊套件台账沙箱 + 新建 `ownergate.void()` 作废机制；(b) `prospector.rollback_source` 二次回滚无
防护 → 加"最后一次 apply 后已有 rollback 行则拒"。**三个 fresh-context 独立评审并行攻击，全判 FAIL**，
合并去重 ~11 条真发现：**CRITICAL** = B4 permgate docstring 谎称"全仓唯一 choke point"（learning/
subscription.py 独立路径绕过、含 agentic 注入面）；B6 DENY 路径 `ownergate_row_failed` 在 append
之后设置=永不落盘；B7 `void()` 无守卫可让 owner 读的周摘要里任意卡（含 kernel 卡）消失，撞"绝不瞒着
不列"；B1 PERMISSION.md item 8「破坏性操作」无出处=私自立法伪装成逐字。**MED/严重** = B2 item 7
引错 §（§7→§4）；②#3 四条闸失效路径不留痕可静默永久关闭；①#2 supervisor 测试 `(A and B) or True`
重言式恒真、覆盖 autoexec 时间盒回滚这条核心安全承诺；①#3 `engaged()` 对缺 families 键注册表抛
KeyError 崩溃周报（与 fail-closed 宣称不符）；①#4 route 缺批内断点、崩溃重跑误报撞名卡；B8 claude_cli
焊点吞异常无日志。**全修**：B4→焊 subscription 两函数+改正两处 docstring+诚实边界；B6→重排先记账后
落盘（实测强制失败后 `ownergate_row_failed` 真落盘）；B7→改成**被作废行照列标 ⊘不隐藏**（只从预算摘除）
+void 守卫（目标须存在、拒 void-of-void）；B1→头注承认 B.8 唯一综合项非逐字+分散依据；B2→改引 §4；
②#3→`_degraded()` 四路径全留痕+workbench 显示 enabled；①#2→去 `or True` 改真断言（红队拆守卫验证
测试真挂）；①#3→`.get("families",{})`+拓宽 except（三种畸形注册表实测 fail-closed）；①#4→
`already_applied_by_us()` 事件流判幂等跳过；B8→stderr 留痕。**第四个 fresh 评审复核判 ALL-FIXED-CLEAN**
（真读码+跑 216 pytest+红队式行为验证；逐条 FIXED，唯一遗留=subscription 焊点缺回归测试的覆盖缺口→
已补 3 条测试闭环）。

**什么活下来**：permgate（判定内核+两焊点+worker 行军令闸）+ PERMISSION.md（sha 钉）+ void 作废机制
（照列不隐藏）+ 12 条新 permgate 测试 + 4 条 supervisor 焊点测试 + 3 条 void 测试 + route 幂等测试；
全套 **263 pytest 绿** + 5 个 standalone runner 绿；真 Haiku 四类实测 + B6/降级留痕代码级实测。
**诚实边界**：真 Haiku 判定是概率层（咨询性减法），唯一放行权威仍在确定性闸（hook/sandbox/ownergate/
vaultzone）；焊的是本仓已知两条 `claude -p` 路径，将来**新**起的调用若不经这两条会漏筛=conformance
约束非结构强制（docstring 已如实标）；permgate 默认 enabled，是加分层不是承重墙（闸炸/被关都 DEFER
放行，但留痕）；prompt 注入对抗是持续军备竞赛，实测只证当前四类不被翻转。

**回归批评史**：空壳（permgate 真 Haiku 实测非摆设）/隐身特性（arch 树可见+sha 钉）/编造手册文字
（B1/B2 恰是此类，评审抓出+全修+现逐字对照通过）/stale 卡（零新增）/AI 腔（评审未标）/自评判决
（三评审 FAIL + 第四评审复核 PASS，判决全程在 fresh 子代理手里，我只自攻不判）/orphaned watcher
（零迁移）——已知失败零复发。

**owner 卡**：本轮**零新增卡**（permgate 是自由区代码，非内核；未碰任何冻结件）。7 张在等的卡
（修宪-两环降级/监事会修宪/criteria v2/AESTHETICS/外联 kit/凌晨五连批 A/B）开工时逐一查过，全部仍空。

**下一轮**：开工照例查 owner 回词（7 张卡在等，任一有词先执行——修宪批的执行路径=誊条文+独立会话
逐字 diff+钉 sha+激活对应机制）。建议执行序=纲领 §5.2 token 效率（context-noise 过滤 strip
git-log diff/注释 + lazy skill/charter 加载，owner 已批准执行）进 v2 executor，或 §5.3 Pi 执行基座
staging（一条 brief lane 试点，沙箱包本仓 harness）。警惕：09:30/21:00 wrapper sweep 落盘即 git add
（教训 #17）；permgate 已对 learning/v2/innovation/craft/ops 全线生效，新起 claude -p 须自己调 _screen/enforce。

## Round 8 — §5.2 token 效率：lean/slim context profiles（2026-07-12）

**做了什么**：执行序 ①（纲领 §5 PI ADOPTION 第 2 条 token 效率，owner 已批、不再评估）。开工照例查 0-工作台：7 张卡全空（占位符未填），无 owner 回词可执行。
① 坐实噪音在哪（代码核对）：v2/rings 没有任何一处把 git-log/diff 喂给模型（那类只在纯代码 metrics 里）；真噪音大头 = headless `claude -p` 每次自动注入的 charter/CLAUDE.md/rules/skills/MCP schema——仓库内 tool-less 调用实测 **31,670** ctx tokens/次。
② §1.7 前置调研（fresh 子代理带源 + 独立红队复核 CONFIRMED，`ledger/research/2026-07-12-token-efficiency-survey.md`）：结论 = **零新轮子**，全用 CLI 原生旗子/env；`--bare` 官方推荐但锁死 API-key 认证（OAuth 永不读）→ 我们订阅计费，不可用，如实记为与官方推荐的唯一分歧；LLMLingua 类压缩器不采用（压缩对象错位：已知来源的固定噪音该"不加载"而非"有损压缩"）。
③ 建成两档 profile 焊进 `v2/lib/claude_cli.call()`：**lean**（auto：tool-less/纯 WebSearch·WebFetch 调用 → 仓库外 leanroom + `--setting-sources ''` + `--disable-slash-commands` + `--strict-mcp-config` + `--no-session-persistence` + `--tools 白名单` + env 禁 CLAUDE.md/auto-memory）= **3,318 tokens/-89%（穿 weld 活体实测）**；**slim**（显式，stages_v2 ruler/build 两处 agentic 调用 → 不动 setting-sources、全部 hooks 保留，只剥 charter/memory/skills/MCP）= **19,274/-39%**。安全线：能碰本地文件系统的调用永不自动进 lean；`V2_LEAN_CONTEXT=0` 一键全回。自动受益（零改动）：innovation 全家、prospector、commercial dossier、student 蒸馏+RAGAS judge、ops 告警、permgate 自身 Haiku 预筛。calls.jsonl 审计加 context_profile/context_tokens；workbench 架构树加 leancontext 节点（逐字引文+sha）。

**攻击了什么**：自攻抓 1 真缺陷先修——CLI 自动更新砍旗子会让 lean 调用静默拿空结果 → 加"rc≠0 且信封不可解析→剥旗回退 full 重试一次+留痕"。承重语义全部代码实测非文档推定：marker-hook 实验证明 slim 下项目 PreToolUse hook 真开火（vault 写闸零损失）；stream-json 证明 lean 下 WebSearch 真开火（注意 `web_search_requests` 计数器在此模式恒 0，是计量口径怪癖）。**两个 fresh-context 独立评审并行判决，双双 PASS**：反 Goodhart 对账员独立复现 token 数字（差 <2.3%）、重放 hook 实验、逐测试判"真测机制非同义反复"、278 pytest 复跑一致，零 REFUTED；攻击手独立活体复测 4 项承重声称全成立、grep CLI 二进制验证旗子/env 无一编造、逐调用点核对覆盖面声称全真，抓出 1 MED+4 LOW+1 nit。

**修了什么（评审发现全修，279 pytest 绿）**：MED=kill switch 对 `context='lean'` 会 ValueError 崩溃而非降级（违反"一键全回"合同）→ 结构不合格与 kill switch 分离，降级+新测试钉死；LOW×4=survey 披露缺口（lean 还剥 UserPromptSubmit/Stop/output-style，已补全损失面）、"唯一 choke point"措辞复发 B4 病灶（已改口，架构树引文自动带上纠正版）、回退不可审计（meta 现报 `full-fallback`）、`--tools` 变参旗吞参脚枪（重排块首）；nit=注释引错实测数。

**什么活下来**：两档 profile + 安全线 + 回退 + kill switch + 16 条新测试；全套 **279 pytest 绿** + craft/ops standalone 绿；survey 文档（实测表 8 行 + 承重语义 3 实验 + 完整损失面披露）。本轮零新增 owner 卡（§5.2 全自由区）。permgate.jsonl 新增 2 行 = 活体验证的真调用留痕（非测试垃圾）。

**诚实边界**：v1 `loop.sh` 三处 claude 调用（design/review/smoke）未动——v1 树 read-only 约定 + agentic 写面，留单独一轮带回归再做；`learning/subscription.py` 第二条路径未动（自有调用面与测试，不扩 blast radius）；ring 输出在 lean 下的行为漂移风险经评审逐 prompt 核对判"无实质影响"，但**首个真实夜窗/日 tick 是真验收**——下一轮开工先看 innovation 日跑与 v2 夜窗的 calls.jsonl（context_profile 字段已可查）有无异常。

**回归批评史**：空壳（穿 weld 活体+评审独立复现）✓；编造手册文字（评审 grep CLI 二进制逐旗验证；架构树引文逐字+sha）✓；自评判决（双 fresh 评审，均独立复测）✓；隐身特性（架构树节点+calls.jsonl 审计字段）✓；stale 卡（零新增）✓；AI 腔（评审明判"无注水"）✓；已知失败零复发。

## 事故轮 — Mac 拖死 → campaign 永久搬离宿主机 + governor 建成实证（2026-07-12 事故 / 2026-07-13 完成）

> 这不是常规 BUILD 轮：owner 2026-07-12 下午报**一级生产事故**（他的日用 Mac 被拖死、鼠标冻住、被迫硬重启），下令 campaign 永久搬离 Mac + 加调速器，证明齐全前保持 STOOD DOWN。本轮=事故响应，由 owner 的交互会话（Fable-5）执行，非无头 worker。

**事故（如实记，不粉饰）**：2026-07-12 下午 load 冲到 **284**、713 进程、UI 冻死、owner 长按电源硬重启。**取证坐实真凶不是"campaign 单轮疯狂 fan-out"**（`campaign/INCIDENT-2026-07-12-spawnstorm.md`，日志实证/推断分开标）：713 进程≈macOS 基线（重启后单会话就 739），真杀手是 **load 284 = 十几棵互不知情的 claude 进程树同时烧 CPU**（owner 多个交互会话 + headless campaign worker + v2 ctxprobe，各自 fan-out 相乘）。崩溃当刻的 campaign worker 只 fan-out 了 1 个子代理。附带坐实：memguard 当天 05:13 起就崩死在 `/bin/pgrep` 路径 bug 全天致盲；硬重启后哨兵幸存导致 launchd **二次点火**在开机中的机器上再烧。**全链条没有任何一道防线是按"全系统并发/负载"这个维度设的**——这是根因。

**做了什么（照 owner 四条令）**
1. **Mac 永久解除**：两个 campaign launchd job 卸载、plist 退役进 `campaign/launchd/RETIRED-mac/`（重启/登录不复活）；`go_live.sh` 在 Darwin 上变拒绝存根。顺手修 `stand_down.sh` 真 bug（收尸把 children.json 当整数列表读、遇真实 dict 抛 TypeError 被吞→killpg 从没生效=owner 第一次 stand-down 没停住的直接原因）→ 改走唯一被测的 `lib.reap_orphans()`+4 回归测试。
2. **§1.7 前置调研**（`ledger/research/2026-07-12-governor-survey.md`，独立红队复核）：**LLM-agent 领域无可 fork 的现成 governor**（Claude Code 子代理 fan-out 官方无并发闸、feature request Closed-not-planned=正是本次根因）；三层架构=`.wslconfig` VM 兜底 + **cgroup v2/systemd 硬顶（内核零误差裁判）** + **AIMD/几何增油门**（借 Netflix concurrency-limits 算法 + GNU parallel "别用 loadavg 用瞬时 runnable" 教训）。
3. **搬迁到 ROG-STRIX WSL box**（v2 夜间执行机，让路 RUN_WINDOW 永不争抢）：`campaign/box/` 全套 systemd units（service cgroup=整棵 worker 树硬顶 TasksMax/CPUQuota/MemoryMax，governor 死了仍在）+ AIMD governor（全机信号，红灯乘性减/绿灯几何增逼近硬顶）+ cgroup 原生 memguard（兼 governor 独立审计）+ 开机冷却/全机忙碌发车闸。金库桥=repo 镜像 `vault_mirror/`（纲领 sha 7c5236a8）+ Mac 信使 `courier_mac.py`（launchd 15min，flock 防叠跑，心跳→ops heartbeat_file 探针 3h 陈旧告警）。配置分离 `governor.local.toml`（标定产物 gitignored）/ `governor.toml`（仓库模板），pull 永不冲突。
4. **红线真实压测标定（不许猜）**：box 上阶梯压测（`calibrate.py`），32 线程机甜点=8 并发（吞吐 4885 单位/秒后平台化），红线=实测 knee_p95(49)×1.2=**58**，硬顶 TasksMax=**136**、CPUQuota 上限=2400%（24 核，给别家留 8）。USL 拐点法、全量回写、幂等、拒绝在全档中止时标定。

**攻击了什么 / 什么活下来**
- **上机前** fresh 评审攻 box 代码：2 CRITICAL（C1 systemd Environment= 引号切词会让证明空跑；C2 cleanup 链断→假 worker drop-in=permgate 旁路残留生产 unit）+ H1 标定只回写 8 项阈值里 3 项=calibrated 撒谎 + H2/H3 拐点退化/重跑写坏 TOML——全修。
- **对抗性证明跑了 9 次、暴露 6 个真 bug**（全是真机+真持续负载才暴露、无一原地打转）：① .path 单元只 enable 没 --now→哨兵点火没人监听、空跑 405s；② service_state 未定义；③ 加性增 +25%/tick 太慢=16 分钟才爬满 32 核=owner 禁的"保守慢浪费机器"→改**几何增 ×1.5**（~40s 到顶）；④ BUSY_S 漏改名崩在写 verdict（→加 ruff F821 lint 测试防复发）；⑤ A6 撞 supervisor 无冷却重启的内部二次风暴 confound→worker 改长睡不 exit；⑥ **A6 contend 因 Python 3.14 默认 forkserver 需 `__main__` 守卫、从没真跑起来（只 225ms）**=A6 假失败两轮（教训：该第一次失败就读 journal 原始数据、而非猜"负载太轻"——违反了"先看证据再动手"，已改用 subprocess.Popen 绕开）。A2 口径被 A6 逼严（排除故意注入的外部压测窗才是"正常态持续负载"）。
- **最终 7/7 真通过**（`campaign/box/PROOF-GOVERNOR.md`，全部代码对真实日志/cgroup 判定）：A1 spawn-storm 400→内核挡在 134/EAGAIN 255；A2 正常态 p95=9≤58；A3 fork/fsync p95≈1ms；A4 风暴期回压到 50% 地板；A5 安静期爬满 2400%；**A6 外部负载真越红线(峰值 74>58)、governor 8 次全机信号回压("runnable+blocked=73>58")、配额被压到地板让路**；A7 stand_down 零残留。
- **独立评审判 SOUND**（fresh-context，不复用上一个）：手工用 aimd_step 公式逐步复现几何增序列(100→150→…→2400 每步对上)、验证 136−134=2 算术自洽、逐条核对 8 次 backoff reason 全是全机信号非 cgroup_pids、确认 worker 只启动一次。两条非阻塞诚实性改进（samples 落盘可外部重算 / A2 措辞"1 tick"改"逐 tick 乘性回压"）已顺手补。

**诚实边界**
- 标定用合成 CPU+fsync 树，**非真 claude 树**（API-bound）——给的是保守下界；上线后 `governor.jsonl` 实跑遥测是持续真验收。
- `parallel_hint` 是 prompt 级软约束（Claude Code fan-out 官方无闸）；硬保证只来自 cgroup。
- 上一个自评 PASS 曾被独立评审推翻（A2 自评撒谎+A6 因果污染），据此重写多轮——**这道"自评不算判决"的闸真在起作用**。

**§1.5 真实结果验收**：这台 32 线程机器现在从**内核层面**就发生不了 Mac 那种事故（spawn-storm 撞 pids.max 直接 fork 失败）；owner 的 Mac 完全释放。**但 campaign 本体仍 STOOD DOWN——点火权在 owner**（box 上 `campaign/box/go_live.sh`，未标定/未证明/有 drop-in 残留它自己拒绝）。

**回归批评史**：空壳（穿真 cgroup 活体实证）✓；编造（评审逐步复现日志算术）✓；自评判决（自评 PASS 被推翻→重写→fresh 评审 SOUND）✓；stale 卡（本轮出 1 张邮件通道设置卡，注册族+自跑 gate）✓；隐身特性（PROOF/标定报告入库、samples 落盘）✓；orphaned watcher（金库信使 round-trip 测试证明）✓——已知失败零复发。

**owner 侧**：+1 张卡等回词=`【战役｜告警通道】一次性开通邮件门铃-2026-07-13`（EMAIL 告警通道，冻结内核 c 凭证，三选一）。

## Round 10 — Owner 裁决包落地：14 项批复全执行 + 门铃开通 + 两环降级生效（2026-07-19）

**Owner 拍了什么（2026-07-18 顾问窗口逐条口述，本会话按 applier 规则代写，provenance = ledger/owner-gate.jsonl 13 行 owner-verdict-applied）**：13 张待决卡全部落词 + 十日战役点火准备令 + 直令「执行标准=能力边界内的最好，禁最小可行式收缩」（已逐字入 CLAUDE.md §2.9）。

**做了什么（全部真执行，非记账）**
1. **金库桥**：诊断坐实 courier 无故障——根因=Mac 合盖电池睡眠中 launchd 不触发（pmset 日志实证断 24h、开盖自愈）；手动整轮 exit=0、心跳刷新、双向 git 流通、零积压。ops fix_hint 已加「先查合盖睡眠」。根治调研（一劳永逸路线 A/B 栏）另出报告。
2. **watcher 面（30 分钟环手动触发一次全消费）**：创新环 13 briefs 激活（quarantine 实数=卡列 13 逐名一致；上周 12 = costcharter 07-18 才晋升；轮转 21）；监事会 3 提案、勘探环 5 提案记录进各自 decisions.jsonl 并清卡；envgrant「发」→ publish-receipts 落档、2 条手贴等 posted_date；spendbook kit「发」→ 回执表状态。
3. **冻结件生效**：criteria-v2 FROZEN（DRAFT 更名+状态头改写+sha 重钉 c2528324…+tamper-guard 验证）；AESTHETICS 三层闸 FROZEN（[probe].aesthetics_enforced=true + sha 钉，Tier-1 不达标 deploy 不 accepted）。
4. **修宪三件生效**：监事会第 7 条 Gen-2 教义镜头（条文逐字誊入 CHARTER + doctrine_lens=true，周一 12:30 首跑）；两环降级（supervisor/prospector 族 disposition→demoted，预建 autoexec/apply 机制 fail-closed 激活；条文誊入两章程；独立 reviewer 逐字校验另报）。
5. **勘探环批准项应用（重检后）**：GoActuary + KevinMD 真加源（NO-list/robots/probe/撞名重检全过，各带一条回滚命令）；7 条检测器入建设队列 det-2026-07-19-1..7（去重后 5 个独立检测器，建设 agent 在跑，owner 直令完整生产级）。**顺手抓到并焊死一个真洞**：同 URL 异名可绕撞名检查二次入库（GoActuary 实撞）——已回滚重复条 + vet_source 加 URL 判重 + 回归测试。
6. **邮件门铃开通（代办）**：ntfy.sh 注册（验证链接 Gmail 只读抓取）、token 机器持有（gitignored）；首发抓到真 bug（urllib header latin-1 中文必炸）→ 改 ntfy JSON publish；测试邮件+9 条积压日批**均实证到达 INBOX**（Gmail message id 在卡内），队列清空。告警通道卡带证据归档 done/。
7. **二代事故关案**：A=是（02:51 为 owner 本人）→ 非伪造审批，一级事故预案不启动；B=保留。PrimeStoreCard 电话已办结，卡移 done/。
8. **发帖执行卡**：7 条手发（spendbook 5 + envgrant 2）汇总成单卡（直达链接+逐字复制块+回执行指位，条条 5 分钟），gate() 自记账。
9. **测试**：commercial 57 / supervisor 13 / prospector 13 / campaign 53 全绿（含 3 个"前提变化"测试如实改写 + 1 个潜伏时间炸弹测试修复：test_do_publish_receipts_and_anchor 写死 2026-08-01、anchor 用真实时钟，07-19 起必炸——改相对日期）。
10. **点火前自检全绿**：ssh+WSL 可达；calibrated=true；calibration 报告 ×3；PROOF-GOVERNOR VERDICT: PASS；无 drop-in 残留；systemd running；CAMPAIGN_ACTIVE 未点（点火权在 owner）。box repo 与 origin 有分叉（box 在自己 cycle commit 上）——由战役自身 sync 流程收敛，如实标注。

**诚实边界**：检测器/监事会三实验/个人网站流水线由并行 agent 建设中，完成情况见本轮汇总报告；合盖断桥的"一劳永逸"根治里凡碰系统设置/新凭证的部分打包一键卡等 owner，物理边界（合盖+电池睡眠中 Mac 无法运行任何东西）如实呈报。

**owner 侧**：《发帖执行卡》7 条手发等 owner；点火命令已备好（保留 owner 亲手跑）。

**Round 10 收尾补记（同日）**：并行 agent 全部到站——①检测器：3 建成并全国全量真跑（PBJ 132 万行→25 命中 / CMS 缺陷 41.8 万行→25 / HPSA 7.9 万行→25，反造数纪律+40 断言测试），OSHA/NLRB 2 条因外部端点迁移/无稳定 API **诚实排期**（队列 build_note 周报点名，不静默）；②监事会三实验：#1 ruler 预检**诚实负结果**（预注册验证 0/2 未过→criteria.md 一字未动，exp-002 记负例）、#2 prompt 修订与 #3 影子抓取落地首跑成功；互红队捕 1 真洞（span-local 否定规避）已修；③修宪誊录独立校验 SOUND 三项全过；④个人网站 quiet-ink 全站上线（Lighthouse 98/100/100，独立评审 6/6 复跑 + 参照站 pairwise「不逊色，可交付」），报告卡进 0-工作台；⑤金库桥 A1/A2 落地（睡醒即补跑 + 告警睡眠感知），B 栏根治一键卡等 owner；⑥附加战果：courier「办结卡复活」设计洞实撞后根修+回归。全套 151 测试绿，aef818e 收口。


## Round 11 — box 点火后首轮：R8 真实验收 + v1 loop.sh slim/permgate 补焊（2026-07-19，box worker）

**开工时的信息位障（如实先记）**：本 worker 由 box supervisor 02:16 拉起（owner 已在 box 点火：
CAMPAIGN_ACTIVE/supervisor lock/heartbeat 02:15-02:17 落盘——推断的点火证据，go_live 构造性
安全闸决定了 supervisor 能起=闸全过）。但 box 本地 git 自 07-15 停机起未同步 origin（07-15
02:00 断电黑到 07-19 01:22，loop.jsonl 无错误痕迹、纯环境断电），开工读的战役状态/镜像卡片
全是 07-13 旧副本——本轮收尾 rebase 时才看到远端已有「事故轮」（Mac 一级事故、战役搬 box、
governor 证明+独立评审 SOUND）与「Round 10 Owner 裁决包落地」（owner 07-18 裁 13 卡、Mac 会话
07-19 全部执行）。**本轮因此按旧状态选活，所幸选的两件（R8 lean 落地验收 + v1 loop.sh slim）
正是远端 Round 10 next_action 排的执行序，零冲突零重复；唯一撞车=commercial 日期炸弹测试
两边各自独立修出逐字相同的补丁（收尾取远端版）。教训已写进下轮 next_action：box 开工第一步
先 git pull 再读状态。**

**R8 落地验收（§1.5 真实结果）——PASS，独立评审逐项 CONFIRMED**：07-13 日 tick 61 次调用
全 lean（2.6k–18.6k tokens vs 满载 31,670）、craft 5 次全 lean、07-14/15 夜窗 cycle 0082–0086
全 verify_pass=true、全仓零 full-fallback 行、管线产出形态三天对比无漂移。lean/slim 在真实
生产站住了。cycle-0087 被停机打断在 design_converged（非故障），工件随本轮入库。

**做了什么（执行序 ②）**：v1 `architect/design_loop/loop.sh`（夜窗 design 阶段每晚真跑，最多
16 design + review/smoke）三处 `claude` 调用接 **slim** profile——与 claude_cli 同源三旗+两 env、
全部 setting sources 保留（hooks 零损失）、同一 kill switch V2_LEAN_CONTEXT=0（env 继承一键
管到 v1，评审代码核实）、CLI 砍旗回退 full-fallback（run 内粘性）、每调用审计进
<run>/calls.jsonl。**实测两次**：full 26,246/26,323 → slim 18,462/18,539（-30%），原始
envelope 落盘 `ledger/research/2026-07-19-v1-slim-probe/`。**自攻抓出真缺口并补焊**：loop.sh
是本仓**第三条**直呼 claude 的路径——R7"两条=全部"覆盖声称不实（B4 病灶变形复发）；现
permgate 开跑前筛 driver 发送的 prompt 文本（四模板+BRIEF，sha 缓存夜窗零成本），真 Haiku
活体筛五件全 DEFER。附带：worker box 装 vale 补环境缺口（3 个 outreach 测试从此能跑）。

**攻击与判决**：两个 fresh-context 评审并行攻击，**双双 HAS-ISSUES**：攻击手 2 CRIT+3 MED+
2 LOW——CRIT-A"筛全部可发内容"声称不实（模型自己 Read 的 DESIGN/OPEN_QUESTIONS/LEDGER 不经
筛）；CRIT-B bash 把闸 infra 崩溃误当 DENY 堵夜窗（评审活体复现）；MED regate.sh 第四条未焊
休眠路径又漏列；MED 系统性砍旗逐调用白试翻倍成本；MED 头条实测数字无工件。反 Goodhart 审计员
独立复核 R8 验收/测试/permgate 台账（重算 sha 证行非编造）全 CONFIRMED，唯一 ISSUE 同
MED-实测无工件。**全修**：CRIT-A→三处措辞改"只筛 driver 发送的 prompt 参数"+与另两焊点同
边界（评审核实 parity 为真）；CRIT-B→只认 exit 7=DENY、其余非零 DEFER 放行留痕+import 套
broad except+复现测试钉死；MED→regate.sh/changelogen_fragile.py 如实入覆盖图（休眠脚本不
硬焊）；MED→SLIM_BROKEN run 级粘性+测试；MED→重跑探针留原始 envelope+README 如实记"首测
未落盘、复测差 <0.5%"；LOW×2→bypass env 显式留痕、audit 行加 usage_limit_waits。**第三个
fresh 复核者逐条验证（含亲手复现 CRIT-B）判 ALL-FIXED-CLEAN**。

**什么活下来**：loop.sh slim+permgate weld（只认真 DENY、infra 全 DEFER）+ 7 条离线回归
（假 claude shim 证 argv/env/回退/粘性/DENY 拒跑/infra 放行全真机制）+ 探针原始证据目录 +
诚实覆盖图（三焊点+点名未焊休眠路径）。box 套件 202 pytest 绿（1 skip=ruff 未装）。
本轮零新增 owner 卡（全自由区）。

**回归批评史**：空壳（shim 断言真 argv/env+真 Haiku 活体+真探针）✓；编造手册文字（实测数字
无工件被评审抓出→当轮补齐，未带病出厂）✓；自评判决（判决全程在三个 fresh 子代理）✓；
隐身特性（audit 字段+bypass 留痕+覆盖图明示）✓；stale 卡（零新增）✓；AI 腔（评审未标）✓；
orphaned watcher（零迁移）✓——零复发。

**诚实边界**：learning/subscription.py 仍是唯一未 slim 的调用面；regate.sh 未焊（休眠已
点名）；permgate 只筛 prompt 参数、agent 自读文件靠确定性闸（三焊点同边界）；v1 slim 的
**真实夜窗验收**（calls.jsonl 出现 design/review/smoke 行、~18.5k、零 fallback）待下一个真
夜窗；本轮开工时对旧镜像做的"12 卡全空"核查已被远端 Round 10 超越（现桌面=8 张卡，全部
owner 手动动作：手发帖×2/回执/网站 Vercel 授权/金库桥回词/运维告警等，机器无可代执行项）。

**下一轮**：**开工第一步先 `git pull --rebase` 再读状态/镜像（本轮教训，防信息位障）**。
① 查 0-工作台 owner 回词（8 张卡，任一有词先执行）；② v1 slim 真夜窗验收（R9→R10 同款
模式）；③ 主项建议=纲领 §5.3 Pi 执行基座 staging（多会话量级：§1.7 带源调研 badlogic/pi-mono
→ 装+沙箱 → 一条 brief lane 试点包本仓 harness，分段落盘 handoff）；或 §5.2 收尾
learning/subscription.py slim 化（小而完整）。

## ROUND 12（2026-07-19，box 第 2 轮）——learning 环 lean 化 + permgate 生产失效根修；Pi 基座调研撞出待定计费墙

**做了什么**：① 开工 rebase 巡卡：envgrant/spendbook 两张「发」的机器后果已由 Mac watcher
施加（envgrant 有 01:04 回执、spendbook 属 record-only 等 sent 日期），无代办；纠了 R11 状态
文件 updated_iso 误写未来时刻（04:40，实际 R11 完成于 02:49）。② `learning/subscription.py`
委托 `v2/lib/claude_cli.call` choke point（caller 归因透传；legacy fallback 保留）——同轮抓出
**真 bug：learning 环 permgate 焊点在生产从未生效**（launchd 以 `python3 learning/digest_cycle.py`
启动，sys.path[0]=learning/，`from ledger import permgate` 一直 ImportError 被老部署面 fallback
静默吞掉）；根修 = 自插 repo 根，生产同形回归测试钉死。真机 probe：lean profile ctx_tokens
3,323（对照 full 31,670 = **−89.5%**）+ permgate 台账 caller=subscription 行 + 真实订阅计费
$0.004（证据 ledger/research/2026-07-19-subscription-lean-probe/）。③ Pi 基座 §5.3 staging
的 §1.7 前置调研（三路 fresh web agent 带源）：安装/SDK/RPC/srt 沙箱包法技术全通；但计费上
04-04 Anthropic 封第三方 harness（按 token 扣预付池）与 06-15 官方暂停声明互相矛盾 →
**「墙在不在」UNCLEAR，出免费 live-probe 提案卡**（OAuth=内核 c 凭证，等 owner 一个词；
两种探测结局都不花钱）。报告 ledger/research/2026-07-19-pi-substrate/。

**攻击了什么**：自攻（递归/契约/兼容/竞态面）不算数；判决在三只 fresh 子代理：评审 1
（web 验证）抓 1 CRIT+3 MED+2 LOW——CRIT=调研时间线断在 04-04、漏 06-15 官方 pause（承重
结论被推翻，初版卡把「墙在」当既定事实让 owner 二选一）；评审 2（代码）判 HAS-ISSUES
1 MED+4 LOW/MED——MED=连环 usage-limit 等待无共享状态（90 调用夜循环可把 10h 复利成数天）、
children.json 跨进程读改写竞态、最坏时延与 Haiku 预筛开销未披露、旧 survey 边界过时。
**全修**：REPORT 补全时间线改 UNCLEAR、卡整体重写成「批一次免费探测」、悬空 review-log
落成真文件；claude_cli 加进程内 exhaustion streak（穷尽后同进程后续调用 1 次等待封顶，
成功复位）+ fcntl.flock 焊死 registry + docstring 全量披露 + survey 带日期增补。第三只
fresh 复核 9/10 genuine、余 1 条"~20h"数字高估——按复核者亲手模拟的 10h10min 改正收口。

**什么活下来**：subscription→choke point 委托（355 pytest 绿 + 46 条本轮新增/相关测试 +
真机 lean probe + permgate 归因台账行）；streak/flock 两个并发修复（复核者亲测 kill_orphans
端到端）；pi-substrate 调研报告 + review-log + 免费探测卡（1 张新 owner 卡，属内核 c 合法
排队）。**回归批评史零复发**：空壳✓ 编造文字（引文独立重抓逐字核对）✓ 自评判决（三 fresh
判决链）✓ 覆盖声称不实（主动改正 permgate"三条"头注+survey）✓ stale 卡（废卡当轮删）✓
AI 腔（评审核家规）✓ 拿旧数据判断（先 rebase+抓时间戳误写）✓。

**诚实边界**：v1 slim 真夜窗验收仍待今晚 21:00 窗口（calls.jsonl 尚无数据）；learning 环
lean 的完整生产验收在 Mac 下一次真 digest tick（box probe 只是单调用）；Pi 计费墙现状只有
live probe 能定案（卡在 owner 桌上）；「墙在则费率≈API」是推断；regate.sh 休眠未焊照旧。

## R13 — 2026-07-19 04:55（接力轮：接住被停机杀掉的前任、双评审 FAIL 后重建、夜窗当场追回）

**背景**：上一个 R13 worker 干到一半被 box 无预告停机杀掉——126 行已验证的修复躺在工作树里没提交、没日志、没状态。本轮先按脏树纪律 sweep 入库（13459be），再把它当被告送独立评审。

**改了什么**：① 前任的两项根修入库并验部署：learning 环 Mac 侧 **18 晚连挂**（claude 不在 launchd PATH → FileNotFoundError，logs 实锤；另 07-03 一晚 0 字节空日志原因不明，如实分开记）→ run_digest_cycle.sh export PATH（随 repo pull 自部署）+ claude_bin() 绝对路径解析；v2 夜窗 **07-15..07-18 四晚零窗口**（box 关机时 timer 无 Persistent）→ Persistent=true 已装（装置文件与 repo diff 一致、systemctl 读回 yes）。② 评审驱动重建（5a62e77）：白天带跳过收窄到 09-21h、**21:00-08:59 开火一律截断到次日 09:00**（早晨开机补火从"整夜丢弃"变"抢救 0-9 小时"）；corrupt 哨兵与 stale 同等自愈；rebase 前全树 sweep（runner 身份）+ **pull 输出缓冲进 untracked 临时文件**——沙箱端到端抓出 tracked window.log 在 sweep 与 rebase 之间把树重新弄脏的真 bug；sync_push sweep 从 v2/ 放宽到全树；heartbeat 移到 digest+ai_itself **两段都 0** 之后；新增 v2-window-nightly 监控（看 courier clone 里 window-opened.stamp 的 checkout mtime，投递 ~18h vs 主树 pull 的 ~42h）；claude_bin 4 测试补零覆盖。③ **Persistent 语义两步实证**（生产、非推演）：restart 无 stamp → 只登记基线**不补火**（= 07-15..19 五晚结构上就追不回，daemon-reload≠激活）；回拨 stamp 再 restart → **真补火**，start_window.sh 生产真跑，04:44 开窗、end 恰好钳在 09:00（255min）、runner active——**今晚丢掉的窗口当场追回**。④ 机器查证 Windows 不自启 WSL 且无 Startup 项 → 冷启动补火的最后前提只有 owner 能给（管理员/主机安全姿态）→ 出卡【战役｜夜窗自启】两步各 1 分钟。

**攻击了什么**：自攻（heartbeat 被 git checkout 重写 mtime 假新鲜 → gitignore；8 个无 PATH plist 横查 → 全不调 claude，无族故障；"同一 PATH 行"声称逐字核对为真）不算判决。判决 = 两只 fresh 评审**双双 FAIL**：reviewer 3 MAJOR（daemon-reload≠激活・脏树杀死 rebase 回退・ai_itself 盲区）+ 18/19 晚措辞 + corrupt 哨兵永久 no-op + 零测试覆盖；opus（异 checkpoint）9 条含 2 CRIT（夜间带守卫恰好丢弃"早晨开机"这一最常见补火・修复未 push + **Mac 主树 09:30 才 pull 的拓扑滞后**）+ rebase 回退死代码（与 reviewer 交叉印证）+ 夜窗零监控 + 07-13 归因不实。全部修掉或入卡；1 条评审自误如实记（"charter §1"引用真实存在于战役纲领 §1，非编造）。

**什么活下来**：360 pytest 绿；沙箱端到端（真 git 分叉+脏树+陈旧哨兵 → sweep → rebase ok → courier 提交到树 → 开窗 09:00 钳位 → 无残留）；生产补火实证 + 当场追回的活窗口；两条新监控接线。**回归批评史零复发**：空壳✓ 编造文字✓（18/19 晚主动改准）自评判决✓ stale 卡✓ 隐身特性✓（监控盲区是评审抓的、当轮补）。

**诚实边界**：① Mac 侧修复的首个生产 tick：R13 于 04:46 push、早于 Mac 主树每日 09:30 的 pull → **预计 07-20 03:10 即首个修复后 tick**（前提 Mac 09:30 醒着完成 pull；睡过则顺延一天。评审给的 07-21 时间线基于「box 15:00 才 push」假设，本轮提前 push 化解）；② 冷启动补火仍要 WSL 先被拉起 = 等 owner 卡；③ heartbeat/开窗监控都是进程级信号，不证明学到了有价值的东西（工作信号闸留后续轮）；④ slim 夜窗验收：追回的窗口正在跑，calls.jsonl 留 R14 查；⑤ courier 整库重 clone 会给 v2-window 检查一轮假新鲜（已在注释如实标）；⑥ 主项候选（llama.cpp router / workbench conformance audit）本轮让位于故障根治，顺延 R14。

## R14 — 2026-07-19 10:15（言行一致审计上线：双评审 FAIL→全修→双 PASS，首份 stage-3 记录签发）

**巡卡**：12 张卡全部仍真实等 owner、零回词零残留（envgrant/spendbook 的「发」是前轮已执行态；夜窗自启 done: 未填；Pi基座/金库桥/说一次四项未回）。

**验收三件**：a) **R12 遗留的 slim 夜窗验收——过**：今晨追回的窗口（04:44 开、09:07 收）真跑两个完整 cycle，calls.jsonl 6/6 行 context_profile=slim、零 full-fallback、零 usage-limit wait；cycle-0088 交付 DELIVERY.md，cycle-0089 architect 腿 harden 3 次不过 ruler → **诚实拒绝交付**（设计行为）。验收词条两处按实况校准（如实记录，非降 bar）：「review/smoke 行」在 v2 路径结构上不存在（stages_v2.py 恒 DESIGN_ONLY=1，那是 v1 checkpoint 独有段）；「ctx ~18.5k」是单发 probe 口径，生产 agentic 会话 context_tokens 是跨 turn 累计（105k-346k），可比信号=profile+fallback。b) 今晚 21:00 窗验了前提：Persistent=yes、NextElapse=今晚 21:00（实测归 R15）。c) learning 首个修复 tick 预计 07-20 03:10：现在只能验前提（Mac 05:30 有 courier push=醒着；09:30 pull 在本轮开工后才发生），R15 判——**若 FNF 先查 09:30 pull 是否真发生，勿直接判复发**。

**改了什么（主项：纲领 §6 点名的 walk-the-talk conformance audit）**：§1.7 调研先行（fresh 代理带源：业界对"展示片段↔源文件一致"只有 transclusion（Sphinx literalinclude/embedme --verify）与摘要比对（SRI/go.sum/sha256sum -c）两种形态、无现成独立工具；RAGAS/DeepEval 判 paraphrase 蕴含对字节级引文不充分——不采）→ `workbench/conformance.py` stage-2 代码裁判：引文逐字连续子串复现 / git-HEAD 真漂移（冻结文件未提交改动=红）/ render 逆变换保真 / 未成文锚点缺席双扫描 / /doc 可达 / 钉合；stage-3 意见型判决只认独立异源评审记录（钉意见面 hash 含判据码字节 + 60 天保质期，漂移/超期→「已过期·需重评」降级，绝不续挂 PASS）。**法理**：07-11「只设计不建」owner 明令 vs 07-12 纲领 §0 supersedes+§6 点名——证据链逐字留痕 `workbench/audit/SUPERSESSION-2026-07-19.md`（含 owner 回滚开关），两评审独立复核成立。**审计首跑抓修 4 个真缺陷**：quote_head 丢空行拼段（10 处引文 07-11 起非字节连续）、/doc 白名单漏 2 文件（点开原文 404）、coach 引文渗漏、ops 编排未披露。

**攻击了什么**：自攻（server 二次导入、Handler 名、state gitignore 断投递、永真嫌疑）后送双 fresh 独立评审。**reviewer 初判 FAIL(0 blocking, 3 MAJOR)**：sha12 检查在 audit 里同公式自证=永真（只有 pin_* 有真牙）；推翻 owner 明令一行带过没留痕（自授权类风险）；只审 dict 字段不审屏显（render 层盲区）+4 minor（quote_head 首空行欠捕获/CRLF 误杀隐患/调研漏 anchor_absent/384 不可复现）。**opus 异 checkpoint stage-3 初判 FAIL**：A 意见面 1 实锤（ops 编排充成文）+2 minor（coach 渗漏、chips 瞥读）；B 2 MAJOR（「言行一致」徽超实证——talk 侧≠claim-vs-impl；stage-3 单文件记录无保质期必腐化）+ 判据码可自编辑护城河保留。

**什么活下来（FIX EVERYTHING FOUND 后双复核）**：全部 12 项逐条修复——check_git_drift 独立源真漂移、sha12 如实降级不计数、SUPERSESSION 留痕（评审逐字节比对引文无曲解）、check_render_fidelity（偷改一字负例实测抓获）、徽章降级「展示忠实」+claim-vs-impl 未接线固定披露、stage3_record 三态保质期、判据码入 hash 链、ops/coach/chips/空声明各修。**reviewer 复核逐项 FIXED → PASS；opus 复核逐项 FIXED → PASS 并签发首份 stage-3 记录**（conformance-stage3.json，钉 surface_sha12=ca9dc1c09714）。测试：workbench 41 绿（21 个新校准测试全为种植违规先红后绿）；全仓分列 391 passed 1 skipped 0 failed。

**回归批评史零复发**：空壳✓（149 项现算检查+工件）编造手册文字✓（本轮正是给这类上闸+修掉在线 10 处非逐字）自评判决✓（stage-2=代码、stage-3=独立异源、轮判决=双 fresh 评审）stale 卡✓ 隐身特性✓（404 修掉+未接线明示）茅草屋✓（复用 Tabler 既有件）AI 腔✓（评审未报）市场盲✓（N/A）。

**诚实边界**：① claim-vs-impl（成文纪律↔运行代码兑现）整类未接线——面板已固定披露，评审记录在案「下轮必须兑现、兑现前披露不许摘」；② commercial/craft 钉合在 box 无 state 可比（如实 warn，Mac 端才有真比对）；③ conformance.py/architecture.py 此后任何改动会自动作废 stage-3 记录（设计行为，改判据必须重评，勿当 bug）；④ v2 test_usage_limit_streak 线程 flaky（R12 遗留、隔离 3/3 过、全仓并跑偶发 can't start new thread）移交 R15；⑤ test_stage3_hash_covers_judge_code 是镜像实现测试（防实现变弱有效、防实现+测试同改无效——真防线是 hash 作废机制本身，评审定性可接受）。

## R15 — 2026-07-19 10:25（claim-vs-impl 行为兑现接线：R14 记录在案的义务兑现，双评审 FAIL→全修→双 PASS，stage-3 重签）

**巡卡**：12 张卡零新回词（envgrant/spendbook 的「发」均已处理态：envgrant 有 01:04 机器回执、spendbook 等 owner 手发填 sent_*；夜窗自启 done:/Pi基座/金库桥/说一次四项仍空）。

**验收**：②a（07-19 21:00 v2 窗）和 ②b（07-20 03:10 learning tick）在本轮开工时（09:47）均未到时点——移交 R16（连带 R14 的判读指引：learning FNF 先查 Mac 09:30 pull 是否真发生）。次项 v2 test_usage_limit_streak 今日隔离 3×3 + 全套件均过、未复现，根治顺延。

**改了什么（主项：claim-vs-impl 接线，R14 评审记录在案的义务）**：§1.7 调研先行（fresh web 代理带源，`workbench/research/2026-07-19-claim-vs-impl-survey.md`）：业界一致分工=执法引擎产证据、面板只 surfacing（InSpec control=声明+可执行断言、Gatekeeper audit+自写薄 exporter、Auditree "checks as unit tests"+git evidence locker、GitLab/Drata control→自动化测试、SLSA/in-toto 声明↔行为记录机械比对）；surfacing 层无通用现成件、各家自写薄胶水是常态——采 Auditree 概念模型 + OSCAL AR 三层形状（control→observation→evidence），不采整套 OSCAL/商业 SaaS。→ `workbench/enforcement.py`：controls 注册表 + 三种 referee 执行器（pytest exit code / hook 注册状态+可执行位 / JSONL 运行时账本按 caller 分列），零判断逻辑、referee 不可用=unknown 绝不假 PASS、空套件（exit 5）=fail（空壳充绿不许）。接线三节点：**vaultzone**（新架构树节点，引 VAULT-ZONES.md 逐字；hook 布防检查+test_vaultzone.py 11 项）、**permgate**（四焊点测试全引 38 项+permgate.jsonl 510 行真实调用痕迹）、**workbench**（test_intents_applier.py 20 项）。render `_enforcement_html`：接线节点亮真实 referee 结果+诚实边界，其余 14 节点保留「本节点未接线」披露（兑现在哪只亮在哪）。/doc 白名单+VAULT-ZONES.md。19 项新校准测试（种植失败测试被抓/假 settings 被抓/无 x 位被抓/stale 账本降级/hash 覆盖 enforcement.py 字节）。

**攻击了什么**：自攻（settings/permgate.jsonl/VAULT-ZONES 入 git 核实、claim 措辞超实证自查 3 处收窄、hook fail-open 补披露、嵌套 pytest 插件炸弹根修=PYTEST_DISABLE_PLUGIN_AUTOLOAD 且 --collect-only 前后收集数核对一致）后送独立评审。**reviewer fresh 初判 FAIL(2 CONFIRMED MAJOR)**：① permgate claim 覆盖「worker/claude 调用」却只引 1/4 焊点测试、boundaries 空、账本 493/510 是未引用焊点的流量——R14 B1「徽章超实证」同型复发形态；② enforcement.py 整个 CONTROLS 面在 stage-2/stage-3 保护之外=新 self-editable-judge 开口（改 claim 文案/换弱测试不作废旧裁决）。**opus 异 checkpoint stage-3 初判 FAIL(1 必须修)**：vault-zoning boundaries 把「sandbox 属后续轮」误引为 VAULT-ZONES.md §4（实载 §5 诚实边界）——指错章节破坏证据链。

**什么活下来（FIX EVERYTHING FOUND 后双复核）**：MAJOR-1→四焊点测试全引（campaign weld+tests/ledger+v2 subscription 委托+v2 loop_slim，38 passed 且禁插件前后收集数相同）+boundaries 三条（措辞取自 permgate.py 焊点图诚实边界节）+账本按 caller 分列；MAJOR-2→enforcement.py 字节入 opinion_surface_hash 链（三份判据码）+两个校准测试钉死；§4→§5 单行修正。**reviewer 复核：两 MAJOR 逐项独立验证（含亲手篡改 enforcement.py 字节证 hash 必变、验旧记录真 expired_drift）→ PASS**；**opus 复核：单行回退重算 hash 恰还原上轮钉值=改动范围密码学级证实恰为该行 → PASS 并签发新 stage-3 记录**（钉 surface_sha12=39bd21d03677）。测试：全仓分列 **410 passed 1 skipped 0 failed**（=R14 基线 391+新增 19）。

**回归批评史零复发**：自评判决✓（轮判决=fresh reviewer、stage-3=异 checkpoint、evidence=pytest/文件系统/账本三类代码 referee）徽章超实证✓（正是本轮抓修的 MAJOR-1，终态四焊点全引+边界如实）空壳✓（空套件=fail 有校准测试钉着）编造手册文字✓（claim/boundaries 措辞逐句对照源文件、§4 误引被 stage-3 抓修）隐身特性✓（/doc 补白名单）stale 卡✓ 茅草屋✓（复用既有 badge/src-note/details 样式，评审验 HTML 结构）AI 腔✓（评审未报）市场盲✓（N/A）。

**诚实边界**：① enforcement 是证据面不是强度证明——「兑现」徽章语义=「护栏已布防+其测试现跑通过+有真实调用痕迹」，不证护栏运行时实际强度（srt sandbox 接 worker 属后续轮，VAULT-ZONES.md §5）；② referee 测试自身质量（cited pytest 是否语义覆盖 claim）由本轮两评审人工实读认定（intents_applier 20 项、四焊点 38 项均实读过），无自动化语义核对——结构上不可自动化，靠 stage-3 重评纪律兜底；③ 14 个未接线节点如实保留披露——不是每条纪律都有执行式护栏可接，充数即空壳；④ jsonl「pass」语义=窗口内有真实调用，非「闸在拦截」（DENY 样本为 0 属事实：510 行全 DEFER）；⑤ permgate.jsonl 本轮多 5 行=今晨 design_loop 真实调用（运行时账本随轮 sweep 提交，非本轮改动）。

## R16 — 2026-07-19 11:02（worker 内核级写监狱：VAULT-ZONES §5 记录在案边界闭口，三轮独立评审 FAIL→全修→PASS）

**巡卡**：开工 rebase 后 13 张卡（courier 同步新增【创新环｜激活新brief】+ 运维告警 07-19）零新回词——envgrant/spendbook 的「发」均处理态（envgrant 01:04 机器回执、spendbook 等 owner 手发填 sent_*）；夜窗自启 done:/Pi基座/金库桥/说一次四项/激活 brief 仍空。运维告警（learn-digest heartbeat 缺失）是 Mac 侧路径诊断（/Users/javencao/...），box 无从复现、非本轮工作。

**验收（R15 移交两项）**：②a 今晚 21:00 v2 窗、②b 明晨 03:10 learning tick——开工时点 11:02 **均未到时点**，顺延 R17（判读指引连同移交：learning FNF 先查 Mac 09:30 pull 是否真发生，勿直接判复发）。v2 window-opened.stamp 仍是今晨 04:44 追回窗（R14 已验），无新窗待验。

**改了什么（主项：纲领 §6 VAULT ZONING 的 §5 欠账——worker 进程写监狱）**：R5 VAULT-ZONES.md §5 把「campaign worker（bypassPermissions LLM agent）的 Bash 任意写形越白名单可绕、非内核级保证；内核级=srt sandbox 接进 worker 属后续轮」记为诚实边界。本轮兑现。**§1.7 调研先行**（fresh web 代理带源 + 本机实测，`campaign/research/2026-07-19-worker-jail-survey.md`）：先试 srt（先例 gate/sandbox/、官方文档背书包 claude 进程）——**本机装 0.0.66 实测：发布版 CLI settings schema 强制 network.allowedDomains、`*` 被 schema 拒、空数组=全断网**，表达不了「写监狱+任意域网络放开」（worker 调研要任意域）；main 分支「省略 network 键=fs-only」的路径发布版 CLI 走不到。→ 裁决**裸用 bubblewrap**（srt 在 Linux 本质也是拼 bwrap，薄胶水同 sbx.sh 包 srt、非手搓 checker）。**建成 `campaign/worker_jail.sh`**：`--ro-bind / /` 整盘只读地板 + 可写白名单（repo//tmp/~/.claude/~/.cache/~/.npm-global/~/.config/gh）+ .git/hooks 只读罩，网络保持共享（写监狱非网闸）。接进 `worker_run.sh`：preflight-gated fail-open（bwrap 缺失/preflight 挂→响亮警告+campaign/state/jail_fallback.jsonl 落痕+supervisor 抬升，连续性优先，纲领 §1）；Linux-only、CAMPAIGN_JAIL=off 逃生口。**内核判决实证**（非声称）：狱内真跑 claude -p 全链回路（会话完成、自证写 ~/.profile 被内核挡、无逃逸）；狱内 git fetch/push --dry-run rc=0（收尾 push 通道不被掐）。

**攻击了什么**：自攻（symlink 穿透/cp/tee/python 间接写/嵌套 bwrap/unshare-mount/O_TMPFILE）不算判决。判决在 **fresh 子代理三轮**（全文留痕 `campaign/audit/R16-worker-jail-review-log.md`）：**轮 1 攻击**——评审 A（行为）5 findings、评审 B（架构/选型）**PASS**（亲手在 srt 发布版源码逐行验证承重选型理由为真、非偷懒借口）、评审 C（第二行为）**FAIL** 2 MAJOR（5a "MUST surface" 纯 prose 无代码=claim-exceeds-impl 复发形态、5b permgate 不筛 worker_jail.sh）+ MINOR 4b（Darwin 空数组崩）。**全修**：F1 .git/hooks 越狱路径（钩子 untracked 不进 git 历史、宿主 git 操作以未入狱身份执行→ro-bind 只读罩，objects 仍可写保 commit）、F2/5b permgate 三份全筛、5a/F5 supervisor `_surface_jail_fallback()` 真接线（cursor 去重、子进程测试证）、F3 git 认证补 [实测]、F4 /dev 文档、4b 安全空数组展开、测试异味（精确前缀检查取代脆 .replace）。**轮 2 复核**——评审 D 独立重验 7 条全已修（逐条亲手实跑）+ 广谱逃逸零穿透 + 测试有牙（no-op 替换 7/13 正确失败）→ **PASS**。

**什么活下来**：全仓分列 **423 passed 1 skipped**（R15 基线 410 + 13 新 jail 测试）；内核地板经四名 fresh 评审 + 自攻的广谱逃逸零宿主持久写穿透；三轮评审记录落 review-log 工件（复核者点名 R16 缺此工件，已补）。**回归批评史零复发**：自评判决✓（判决全 fresh 子代理、作者自攻不计）编造手册文字✓（本轮两处过度声称=评审 5a/5b 抓的，全改成代码兑现或如实边界；survey [实测]/[查到] 逐字被复核抽验无编造）空壳✓（no-op 替换 7/13 测试失败=有牙）隐身特性✓（14 未接线节点前轮已披露、本轮 fail-open 降级不静默）stale✓（清 reviewer 探针残留）茅草屋✓（N/A）AI 腔✓（评审未报）市场盲✓（N/A）。

**诚实边界（评审记录在案）**：① **exfil 是更大未闭合面且监狱管不了**——网络共享+worker 合法读凭证，写监狱只管写；收 exfil 要限网，而限网正是 srt 也做不到那件事，是真难题非本轮遗漏；② Mac 侧 worker 未入狱（本闸 box/Linux 形态，Mac=Seatbelt/srt 后续轮，且 go_live 现拒 Mac 启动）；③ 蓄意自改非本闸威胁模型（磁盘改 worker_jail.sh 不 commit + supervisor 闸故障 DEFER 仍可能 execs 阉割脚本；permgate 预筛是缓解非保证，最终防线是评审实读发车面）；④ claude CLI 狱内自动更新写 ~/.local 失败（预期降级警告，会话不受阻）；⑤ 内核地板对蓄意越狱者非 jailbreak-proof（读密钥合法、网络开）；⑥ 验收两项时点未到顺延 R17；⑦ 无 review-log 无法向 owner 自证评审真发生（复核者观察）——已补 review-log 工件缓解，但工件由作者写、评审转述，非评审直签（结构限制，与 stage-3 独立签发不同层级）。

## R17 — 2026-07-19 11:xx（worker exfil 面收敛：deny-read 凭证 + 网络 git 移 host-side，双轮独立评审 2 MAJOR→全修→PASS）

**巡卡**：开工 rebase 后 14 张卡零新回词——envgrant/spendbook 的「发」仍处理态（等 owner 手发填 posted_date/sent_*，机器已回执）；夜窗自启 done:/Pi基座/金库桥/说一次四项/激活 brief 仍空。sweep-commit 了脏的 permgate.jsonl（今晨 design_loop 真实调用行）。

**验收（R14/R15/R16 三轮顺延两项）**：②a 今晚 21:00 v2 窗、②b 明晨 03:10 learning 首个修复 tick——开工时点 11:05 **仍均未到**，继续顺延 R18（判读指引不变：learning FNF 先查 Mac 09:30 pull 是否真发生，勿直判复发）。K6/K7 商业 triage 经 Explore 核实 **R2 建成、R10 owner 批后已激活**（config_v2.enforced=true+sha 钉），R17 next_action 该项措辞 stale——真正未闭合面是 exfil，故本轮做它。

**改了什么（主项：纲领 §6 VAULT ZONING §5 边界④——worker exfil 面，R16 明确移交的"更大未闭合面"）**：R16 记「exfil 面结构上关不掉（写监狱管写不管读、网络共享+读凭证）」。R17 收敛之。**§1.7 调研先行**（fresh 子代理带源 + orchestrator 本机只读复核，`campaign/research/2026-07-19-worker-exfil-survey.md`）：egress allowlist **结构上无效**——Claude Code 的 WebFetch 从本机 IP 直连任意研究域（[查到] 多源逆向 + 官方 sandbox 文档），既是纲领 §2.3 要的研究通道也是 exfil 通道，限域=掐研究、放域=留通道；本机也无 socat/nft/iptables。→ **只能从读侧收**。**建成/接线**：① `worker_jail.sh` 加 deny-read——`--tmpfs ~/.config/gh`（罩 gh token 文件）+`--ro-bind /dev/null ~/.bash_history`（罩 owner shell 史），claude 凭证故意留可读（worker 就是 claude 进程）；② **git fetch/push 移进未入狱的 supervisor**（`supervisor.py _host_git_pull/push`，业界"高价值凭证挡在 untrusted 计算 job 外"模式=GH Actions 信任边界分离，仓库内 sync_push.sh 先例）——worker 从此不需 gh token（blast-radius 最重：默认 scope=owner 全仓读写）；③ worker 只 commit、host push best-effort 非致命（失败落 state/host_push_fail.jsonl+下轮自动补推+courier 第二通道，连续性>送达，纲领 §1）；pull--rebase 冲突/timeout 走 rebase --abort + 清 stale index.lock 兜底。

**攻击了什么**：自攻（rebase-in-progress 坏树、假 worker 误触真 git、gh token 别的副本面）不算判决。判决在 **fresh 子代理两轮**（全文 `campaign/audit/R17-worker-exfil-review-log.md`）：**轮1 攻击**——reviewer 真 bwrap+mutation-kill+真冲突复现，判 HAS-ISSUES 找出 **2 MAJOR**（M1：`gh` 从 GH_TOKEN/GITHUB_TOKEN 环境变量读 token 优先级高于文件=只罩文件被绕过、worker env 只剥 ANTHROPIC_API_KEY；M2：真 claude token 在 `~/.claude/.credentials.json` 非 `~/.claude.json`=survey/文档/测试指错文件）+1 MINOR（测试计数）+2 PLAUSIBLE（preflight rc 覆盖、timeout 残 index.lock）。REFUTED 多条（gh 文件狱内读不到、claude 仍可读、假 worker 不碰真 git、真冲突 abort 留干净树、push 失败非致命、测试有牙、survey 引文无编造）。**全修**：M1→三处剥 gh env token（jail --unsetenv 4 变量+preflight rc=5 断言 / worker_run.sh unset 覆盖 unjailed fail-open / supervisor env _STRIP）；M2→文档/注释/测试全改指 .credentials.json、测试验两文件都可读；MINOR→+11 实数列清单；P1→preflight set_rc sticky；P2→except 分支加 rebase --abort+删 index.lock。**轮2 复核**——第二 reviewer live 复验：两 MAJOR **CLOSED**（狱内 GH_TOKEN/GITHUB_TOKEN 真剥空、gh auth git-credential get 也拿不到、.credentials.json 真可读、文档指对文件）、P1 CLOSED、seeded-break 令测试变红=有牙；指出收口前两办（计数仍写错 +8→已改 +11 实数、P2 无测试→已补 timeout/index.lock 测试）+一条非阻断新发现（supervisor.py 现持 gh token 但不在 permgate 预筛三文件内→已 §3.7 点名留档、下轮纳入）。两办已办。

**什么活下来**：全仓 campaign 分列 **76 passed 1 skipped**（+11 新测试全绿）；exfil 重轴（gh 全仓读写凭证）经两 fresh 评审 live 复验从读侧+env+host-side-git 三面关死；两轮评审记录落 review-log 工件。**回归批评史零复发**：自评判决✓（判决全 fresh 子代理、作者自攻不计）编造手册文字✓（M1/M2 正是抓的过度声称/文件标错，全改成代码兑现+文档指对；survey [查到]/[实测]/[推断] 逐条被复核抽验无编造）空壳✓（seeded-break 测试变红=有牙、P2 补了真覆盖 except 分支的测试）隐身特性✓（残余轴+新 supervisor.py 面全披露不静默）stale✓（VAULT-ZONES §5④/⑦ 校准、next_action K6/K7 stale 已在日志纠正）茅草屋✓（N/A）AI 腔✓（两评审均未报）市场盲✓（N/A）。

**诚实边界（评审在案）**：① **claude OAuth token 轴仍开**（`~/.claude/.credentials.json` 必须可读，worker=claude 进程，罩了=不能思考；泄露 blast-radius=订阅额度滥用、无仓库写权，比 gh 轴轻）——不可约；② **WebFetch 本地任意域 exfil 通道**结构上关不掉（关=掐研究）；③ Mac 侧 worker 未入狱（沿 R16）；④ 蓄意自改 campaign 代码（含新抬高分量的 supervisor.py）非本闸威胁模型，最终防线=独立评审+git 历史；⑤ host push best-effort：push 失败=已 commit 未送达（落痕+下轮补推+courier 第二通道），不改轮次记账；⑥ 环境性测试失败 test_run_escape_hatch（评审/CI 会话 $HOME 只读挂载，真 box 上可写，git-stash 对拍证 pre-R17 同样失败=非回归）；⑦ 验收两项时点未到顺延 R18。

## R18 — 2026-07-19 13:0x（抓获活 daemon 陈旧事故：防护只在 git 不在进程——三层根修 + 预筛读面扩到 supervisor.py，双评审 2 MAJOR→全修）

**做了什么**：开工验收发现 next_action 两项时点仍未到（21:00 v2 窗 / 07-20 03:10 learning tick，顺延 R19）、14 卡零新回词后，主项按计划做「supervisor.py 纳入 permgate 发车预筛读面」（R17 评审欠账）——过程中**抓获一桩更承重的真事故（代码裁判坐实，非声称）**：活 supervisor daemon（pid 1108，02:15 启动）是长命 python 进程，之后 aa023af（10:54，R16 修复）、2021ab4（12:07，R17 exfil 闭合）改了 supervisor.py 但**进程从未重启、一直跑启动时代码**。铁证可复算：① permgate 11:04:57 行 prompt_sha12=71f105db8fee 恰等于**两文件**旧拼接（run+round_system）的 sha256[:12]、三文件不合（评审加码：12:08 行同理=发车 R18 时仍陈旧）；② daemon 全天零 host_git_pull/push、零 jail_fallback_surfaced 事件（11:43 的 pull/push 行是 R17 评审 harness 的 pid 10361/10591，非 daemon）。**后果=R16 surfacing、R17 host-side git 交付、乃至本轮任何 supervisor 改动，全部"在 git 里存在、在活进程里不生效"；worker commit 无人推送。** 三层根修（systemd 语义逐条本机 man 核实，非记忆）：① worker_run.sh 踢击（每次发车 fresh 读盘=陈旧 daemon 唯一看得见新代码的地方）：CAMPAIGN_KICK 开关→父进程必须是 campaign/supervisor.py→必须在 ai-architect-campaign.service cgroup（手动启动无人拉起、拒踢防停摆；本 worker /proc/self/cgroup 实证在 unit 内）→陈旧判定（sha 台账内容比对优先，mtime 启发只作 bootstrap）→1h 限流 stamp→**SIGKILL**（TERM 属 systemd clean 四信号不触发 on-failure 重启；.path 单元双保险）；② supervisor.py 自重载：loop top 无活 worker 时 content-sha（supervisor.py+lib.py）变→exit 75 由 systemd 重拉（不用 execv：exec 保 pid，O_EXCL pidfile 自见存活死锁；--once proof 不查），启动自报 loaded sha 到 state 给踢击当基准；③ 预筛读面 3→4 件（+supervisor.py，44,609 chars<48k 截断上限动态钉；lib.py 有意不纳=62,747 超限即中段静默不筛假覆盖）。附带根修：test_run_escape_hatch 嵌套监狱失败改真写探针门控 skip（首个入狱跑套件的 worker 才暴露的环境假设缺陷）。

**攻击了什么**：自攻（bash -x 逐守卫 trace 抓出踢击测试时序竞态与重定向顺序 bug、缓存命中不落账语义、48k 截断假覆盖风险）不算判决。判决=两个 fresh 子代理（全文留痕 `campaign/audit/R18-stale-daemon-review-log.md`）：**评审①（代码面）FAIL→修**：MAJOR=锚点测试 4 文件只真保 2（评审亲手变异实测：删两件出读面测试照绿）→换排他锚点+`test_surface_anchors_are_exclusive` 把排他性钉成断言；MINOR=bash 踢击 mtime 语义与 python content-sha 不一致→sha 台账优先+双向新测试；MINOR=文档数字过时→收尾实测刷新。评审①并独立复核：sha 证据严丝合缝、systemd 声称零编造、四条踢击测试真跑（真 SIGKILL returncode==-9）。**评审②（声称/证据链）FAIL→修**：MAJOR=测试套件污染生产 state（fixture 没隔离 STATE_DIR，进程内 run_one_worker 把真 jail_fallback cursor 写成已消费=10:35:54 裸跑记录将永不被 surface）→fixture 补 STATE_DIR→tmp（套件复跑 cursor mtime 不动=隔离实证）+cursor 重置 0 让重启后 daemon 真发该事件；MINOR=stale docstring/"10+ hours"误导措辞/嵌套监狱下 skip 理由失实→全修。

**什么活下来**：campaign 91 passed 4 skipped（R17 基线 76+15 新：踢击 fire/守卫/限流/开关/sha 双向/自重载单元+接线/锚点排他/截断校准/隔离修复）；permgate **预缓存两种发车拼接均真 Haiku 判 DEFER 入缓存**——R19 无论踢前（陈旧 daemon 两文件）踢后（新 daemon 四文件）发车均确定性放行，预筛误杀停摆风险归零。**回归批评史零复发**：自评判决✓ 编造手册文字✓（systemd/man 逐字对拍）空壳✓（评审变异实测抓出的假保护锚点已换真）隐身特性✓（踢击 JSONL+stderr、自重载 event、fallback cursor 重置留痕）stale 卡✓ 茅草屋/AI 腔/市场盲 N/A 过程指标当结果✓（下条边界如实拒绝提前宣功）。

**诚实边界**：① **三层修复=已布防、本轮未 fire**——活 daemon 截至收尾仍陈旧（结构上踢击要等下一次发车触发，轮内不可观测），「真结果=活进程跑上现行防护」落 R19 开工验收：state/supervisor_kick.jsonl 一行（mode=mtime）、events 新 supervisor_start pid、host_git_pull、jail_fallback_surfaced×1（10:35:54）、发车预筛四件 sha；② **delivery gap 如实**：R18 commit 只在本地 .git（本 worker 无 gh token 推不动、陈旧 daemon 无 host push 代码），送达依赖踢击→重启→R19 轮后 host_git_push 补推，courier 镜像第二通道；③ 删闸式自改（把踢击/自重载/预筛调用本身删掉）三层都防不了——筛由被筛文件发起、结构不可自证，归确定性闸+独立评审+git 历史（VAULT-ZONES §3.7 留档）；④ 修完评审 findings 后未送第三轮复核（机械修有测试钉，风险自评低，如实记）；⑤ jail_fallback.jsonl 收尾时仅 1 行（此前观测 11 行，中途被谁截短未查明——cursor=0 只保 surfacing 现存这行，行数变化如实存疑待 R19 顺带看 courier/评审痕迹）。

## R19 — 2026-07-19 13:2x（R18 生效验收判 FAIL：踢击锚点被自家测试毒化——根因活体复现 + pid 绑定根修；按异 checkpoint 评审要求，本轮不宣布 staleness 闭合）

**做了什么**：开工按 R18 手令验收「三层修复真生效」，**判决 FAIL（全代码裁判）**：heartbeat 仍 pid 1108（02:15 旧代码）、state/supervisor_kick.jsonl 不存在=踢击从未 fire、events 最后一条 supervisor_start=02:15:35。根因取证闭环：`state/supervisor_code_sha`（删除前实录：mtime 12:46:03.402、内容 982c42a3…=当时磁盘 supervisor.py+lib.py 拼接 sha、裸格式无 pid）**不是任何 supervisor 写的**（12:46 前后零 supervisor_start/exit_lock_busy 事件）——踢击 sha 比对遂判「已装载最新」、压制重启。**真凶活体抓获**：本轮跑测试套件时 referee（前后 stat 快照）当场看见生产台账被重写成 `061620a7… 3637`（pid 3637=pytest）——`test_once_mode_never_self_reloads`/`test_main_loop_exits_reload_code_on_change` 调 `supervisor.main()` 时 mock 了 lock/heartbeat/log_event **却没隔离 lib.STATE_DIR**，`_persist_code_sha()` 直写生产；R18 的 MAJOR-2「fixture 隔离」修复漏了这两个 main() 调用者（这就是 12:46 毒化的成因，机制被评审① 从 git 历史独立复现）。**修**：① 两测试补 `STATE_DIR→tmp_path`；② **pid 绑定锚点**——台账改 `"<sha> <pid>"`，踢击只认 `pid==$PPID`（写入者必须就是被验的父 daemon），否则一律退回 mtime 启发：外来写入（测试残留/--once/临时 snippet/legacy 裸 sha）结构性失效；③ 删毒化台账→下次发车 mtime 模式必踢（算术双评审独立复核：code_mt 13:02 > start 02:15+5，无限流 stamp、CAMPAIGN_KICK 未设=on、Restart=on-failure 在案）；④ +2 回归钉（毒化外来 pid→mode=mtime 真踢 returncode==-9；legacy 裸 sha→同）。**附带取证**：events.jsonl 11:43/11:44 两行 host_git_pull/push 系 R17 评审测试污染（remote=/tmp/review_r17_remote+fixture 提交名铁证）——追加 `ledger_annotation` 事件如实标注（不删账本行），防后续验收把假行当真 host git；差点骗过本轮验收②的原文判据。**R18 两遗留收案**：jail_fallback「11 行→1 行」按 mtime referee 收——生产文件 mtime 10:35:54=其唯一行 ts、两个写入者均 append-only、此后零写入痕迹=10:35 后从未被截短，「11 行」观测最可能是 fixture 树误读（R18 自家 review-log 也把 10:35:54 行称"唯一实证"，自洽）；R18 handoff 自称 13:15 收尾实为 12:53 commit（未来时间戳错误，如实记）。**巡卡**：envgrant/spendbook 两张 `decision: 发` 经评审② 实读回执核实=已处理旧词（机器回执 01:04 在案、待 owner 手发填日期），零新词。验收③过：本轮 12:54 发车零新 permgate 行=四件缓存命中（12:46 两条真 Haiku DEFER 系 R18 预热）。

**攻击了什么**：自攻（awk 可移植/set -u 空值/pid 复用/命名空间边界/裸格式读者扫仓/48k 语义）不算判决。判决=两 fresh 子代理：**评审①（reviewer）PASS**——亲手从 git 历史还原 pre-fix 代码复现毒化写入、复现修后不再写、独立复跑 93 passed 4 skipped 全对、逐 case 穷举 malformed 台账（空/垃圾/三字段/裸 sha/外来 pid）证无「误踢 fresh daemon」路径、核实 worker_run.sh 踢击全程在 bwrap unshare-pid 之前=pid 比对无命名空间错位；2 MINOR 留案（见边界）。**评审②（opus 异 checkpoint）FAIL→修**——MAJOR：本轮若以「staleness 已闭合」收尾=与 R18 同型纸面闭合（活 daemon 收尾仍 1108，踢击结构上只能下次发车 fire，轮内不可观测）；已按其要求执行：本条日志与 R20 手令一律措辞「**修复已布防、生效未观测**」，R20 开工设**机械三信号硬闸**（见 handoff）。其余：独立复核 mtime 必踢算术、annotation 做法（append-only 下标注优于删行、假行皆 rc=1 无重计险）、jail_fallback 收案逻辑、两卡旧词判断全 CONFIRMED；2 MINOR（12:46:03 归因部分不可复查、测试数字自报）→前者删除前实录+双重机制复现已写入本条留档，后者评审① 独立复跑覆盖。

**什么活下来**：campaign 93 passed 4 skipped（R18 基线 91+2 新钉；评审① 独立复跑一致）；污染 referee 修后复跑=生产 state/logs 零测试写入（仅活 governor/courier 心跳自然翻动）；pid 绑定经两评审穷举无误踢路径。**回归批评史零复发**：自评判决✓（判决=fresh reviewer+opus 异 checkpoint，作者自攻不计）过程指标当结果✓（评审② MAJOR 正是钉这个——已改口径，不拿「测试全绿+算术对」宣布真结果）编造✓（12:46:03 按评审要求降级为「删除前实录+机制双复现」非硬事实；两评审抽验注释/文档无过度声称）空壳✓（新钉真 SIGKILL -9 断言，非 vacuous）stale✓（R18 未来时间戳、假 host_git 行、两卡旧词全如实校正）隐身特性✓（annotation 留痕、referee 快照留 /tmp 实录）茅草屋/AI 腔/市场盲 N/A。

**诚实边界（评审在案）**：① **活 daemon 截至收尾仍陈旧（pid 1108，第三轮未观测到 fire）**——真结果=观测到重启，落 R20 开工三信号硬闸，三缺一则 staleness 仍未闭合、继续最高优先；② pid 绑定防意外污染、非密码学绑定——蓄意写「正确 sha+真 daemon pid」仍可欺（威胁模型=意外污染，注释如实框定；蓄意自改归独立评审+git 历史，沿 R18 边界③）；③ `_persist_code_sha` 在 acquire_lock 前执行（既有顺序未改）——并发手动起 supervisor.py 会短暂覆盖台账，但 pid 错配→安全降级 mtime，非危险信任；④ 12:46:03 归因不可事后复查（文件已删；删除前内容/mtime 实录本条+机制两次独立复现，何进程所写无法回放）；⑤ 「11 行」原始观测无法重建（R18 transcript 未留），fixture 误读为最可能解释、非定论；⑥ 时点验收继续顺延：今晚 21:00 v2 窗、明晨 03:10 learning tick→R20（判读指引不变）；⑦ worker_run.sh+supervisor.py 已改→下次发车真 Haiku 重筛（延迟数分钟属正常）——**有意不预热**：R18 的预热正是毒化事故载体（caller 顶 supervisor 名+测试进程写生产），宁付一次延迟不复制事故形态。

## R20 — 2026-07-19 14:3x（staleness 三信号硬闸全过=活体生效实证，R17-R19「布防≠生效」之争收口；host git 交付链首跑通；sweep 承接+评审 2 MAJOR 全修）

**做了什么**：① **三信号硬闸验收（R19 手令最高优先）——全过，全代码裁判**：(a) heartbeat pid=429072≠1108；(b) events 新 supervisor_start 13:24:07(427120)+13:31:40(429072)>13:10；(c) supervisor_kick.jsonl 存在且 mode:mtime（13:23:37 fire、ppid 1108）。完整链：踢击杀旧 daemon 1108 → watchdog 拉起 427120（lock_break_stale 收陈旧锁）→ 跑完一个 worker 后自重载（exit 75，at_import 061620a7≠on_disk e944acf——评审① 独立从 git 历史重算 061620a7=R19 提交后 supervisor.py+lib.py 拼接 sha，证载入非伪造）→ 429072 以最新代码常驻，pid 绑定台账「e944acf… 429072」=heartbeat pid。**这是真结果不是过程指标：daemon 真被杀真重启真换代码**。② **host git 交付链首次端到端真跑通**：427120 于 13:31:09 真 push 成功（2021ab4..1f34186 → github.com），R18(273ea58)/R19(1f34186) 均已核为 origin/main 祖先=已送达 owner；jail_fallback_surfaced ×1（10:35:54 那条）✓；11:43/44 两行假 host_git 行按 R19 annotation 正确排除。③ **时间线取证**（修正我自己的初报）：夭折 worker 427205（13:24:38–13:31:09 rc=1）死因**已查明**=session 限额 429（worker.out.log 末行「resets 2:10pm」，评审① 抓的，我初判「死因未明」低估了在手证据）；13:31:40→14:12:18 的 40 分钟事件空窗=新 daemon 预筛撞同一限额、claude_cli 按 429 等待纪律等到重置（permgate 台账 elapsed_s=2438.7 精确对上，与 90s per-attempt timeout 是设计上的两回事[查到:claude_cli.py 头注]）。④ **承接 427205 遗下的未提交 sweep 代码**（修 13:24:38 pull rc=128：permgate prescreen 弄脏 tracked 台账→pull 结构性被挡；活 daemon 14:12:18 已真跑过一次 sweep 产 commit 1e24795+对非白名单脏文件正确响亮跳过），验收中抓出并修掉两个真缺陷：(i) 测试诱饵 AKIAIOSFODNN7EXAMPLE 是 AWS 文档示例键、gitleaks 8.30.1 默认 allowlist 故意放行（rc=0 实测）=假红→换真实样式键（源码拆开拼接防自触提交闸）；(ii) 预筛读面 49,642>48k 帽——13:31:40 发车已真触发一次中段静默截断（台账 truncated:true，评审① 独立发现）→**有据提帽 48k→96k 并把 lib.py 收编读面**（R18 因帽不够承认的覆盖缺口闭合；帽非冻结面[查到:config _note+LEDGER D-052 只钉 PERMISSION.md sha，评审②独立复核程序合法]；锚点 def log_event 排他钉死）。⑤ 巡 0-工作台 14 卡：全部真实在等 owner（宪法四裁决未回、envgrant/spendbook 仍旧词待填手发日期），零可代执行、零 stale 卡。

**攻击了什么**：自攻（gitleaks 全文件扫描永久 fail-closed 面/porcelain 解析/异常包络/生产 state 污染复查）不算判决。判决=两 fresh 子代理：**评审①（reviewer）PASS**——5 声称逐条 CONFIRMED（重算 sha、重测 gitleaks 诱饵、独立复跑 139+53 套件、验 origin 祖先、验两次全套跑后生产 state/logs 零测试写入=R17/R19 毒化形态结构性未复发）；2 MINOR：VAULT-ZONES「四件」残留（本轮已改）+ 427205 死因表述（本条已更正）。**评审②（对抗架构审）HAS-ISSUES→全修→复核 PASS 7/7 VERIFIED-FIXED**（每条自行复现/变异重验，非采信）：**MAJOR-1（sweep diff 引入的回归，评审活体复现）**——sweep 的 dirty-skip 提前 return 把 pull 路径唯一的 rebase --abort 自愈饿死：一次跨机文件冲突→仓库永久 detached HEAD mid-rebase、每轮 worker 被发到冲突树上（违反 pull docstring SAFETY 契约）→修：sweep 开头先探 .git/rebase-merge|apply、有残留先 abort（响亮记 host_git_stale_rebase_abort、abort 失败则本轮不碰任何东西）+push retry-pull 失败镜像补 abort；新测试真造冲突 rebase 残留断言零残留+HEAD 回 main（评审删修复块变异立红）。**MAJOR-2（根子问题，§1.7 击中：手搓 sweep 前没调研现成解）**——两台机器向同一 tracked append-only 台账 EOF 追加=rebase 结构性必冲突（2fae54d「两天没送达」复发形态）→采业界现成 .gitattributes merge=union（落盘前真实 tmp 仓库 rebase 实验：双侧 divergent append→rc=0 三行俱在零残留；评审独立重做实验+check-attr 验生产生效）；测试双层钉（生产文件含条目+端到端并集）。MINOR×5 全修：白名单精确匹配钉（评审 startswith 变异曾 16 测试全绿=假牙，新钉立红）/byte 墙断言（单 argv MAX_ARG_STRLEN 128KiB，现值 77,067B<110KB；删未查证的「96k CJK<100k tokens」想当然措辞）/gitleaks --config 对齐仓库 allowlist/注释失实改正（cache-miss 才落行）/SIGKILL-mid-rebase 并入 MAJOR-1。

**什么活下来**：campaign+tests/ledger **142 passed 4 skipped**（R19 基线 93→含承接 4+新钉 3+读面重构；评审② 独立复跑一致；4 skip 全环境性既知）；v2 53 passed（提帽对调用方零行为回归）；变异实测有牙（sweep→stash、fail-open、删 abort 块、startswith 全被咬红）；两轮全套跑后生产 state/logs 零测试写入。**回归批评史零复发**：自评判决✓（判决=两 fresh 子代理+复核轮，作者自攻不计）过程指标当结果✓（本轮宣布的 staleness 闭合=活体观测到杀旧起新换代码+真送达 github，非测试绿）编造✓（评审② 逐条对拍注释引用 13:24:38/2fae54d/L-19 全真；我的「死因未明」「CJK token」两处不实/想当然被评审抓出并已更正——如实记）空壳✓ stale✓（VAULT-ZONES 残留当轮清）隐身特性✓（sweep/skip/abort/gitleaks 缺失全响亮事件）茅草屋/AI 腔/市场盲 N/A。

**诚实边界（评审在案）**：① 本轮新改的 supervisor.py（abort-first+union+--config+五件读面）又是「布防」——活 daemon 仍跑 e944acf 旧 sweep 版，我退出后 worker_done→sha 比对→自重载才生效，**R21 开工必须验生效**（见 next_action，沿 R19 口径不提前宣功）；② sweep 的 gitleaks 扫全文件：历史行若撞新 allowlist 之外的模式→sweep 永久 fail-closed、pull 每轮响亮跳过直到人工/后续轮处理（方向保守）；③ --config 分支测试未真走到（tmp 仓库无 .gitleaks.toml；失效方向 fail-closed+响亮事件，有界）；④ merge=union 只保 ledger/permgate.jsonl 一个文件，其它跨机共写台账未覆盖（候选后续轮，先让单点跑实）；⑤ 踢击 pid 绑定防意外不防蓄意 spoof（R19 边界沿用）；⑥ 时点验收顺延 R21：今晚 21:00 v2 窗、明晨 03:10 learning tick（先查 Mac 09:30 pull，合盖睡眠是已知断因勿直判复发）；⑦ 1e24795+本轮 commit 的 push 由 supervisor 收尾代做（本地 .git 落地即交付到位，host_push_fail.jsonl 兜底留痕）。

## R21 — 2026-07-19 15:0x（R20 布防→活体生效四项验收全过；v2 线程 flaky R12 遗留根治=确定性复现+根修；评审驳回我一次错误更正——如实记）

**做了什么**：① **R20 新 supervisor 代码活体生效验收 4/4 全过**（全代码裁判）：(a) events 14:40:50 `supervisor_reload_code_changed` on_disk=93f978d753da + 14:41:20 `supervisor_start` pid=456802；state/supervisor_code_sha=「93f978d7… 456802」且 `cat supervisor.py lib.py | sha256sum` 重算一致、pid=heartbeat pid；(b) 新 daemon 首发车 14:42:12 sweep rc=0 → pull rc=0，零 dirty_skip（R20 前旧 daemon 14:12 那次 dirty_skip 是未提交 R20 代码所致，属旧进程）、零 stale_rebase_abort；(c) permgate 14:41:20 新行 caller=campaign-supervisor DEFER truncated:false elapsed 51.6s（五件读面真 Haiku 重筛，无限额撞墙）；(d) R20 commit 6c95144 已随 14:40:50 push 上 origin/main。**R17 起四轮的「布防≠生效」链条至此全环闭合**。② 巡 0-工作台：12 张行动卡（+README+00-面板 元文件=目录 14 文件，口径记清）全部仍真实等 owner，零新回词（envgrant/spendbook 的 decision:发 为已处理旧词，等手发日期）。③ **主项：v2 test_usage_limit_streak flaky 根治（R12 遗留、R14/R15 两轮「未复现顺延」）**——根因第一手坐实：campaign 全链跑在 ai-architect-campaign.service `TasksMax=136` cgroup 里、**线程计入 pids**（+12 线程实验 pids.current 14→26），旧测试一次 40 裸线程齐飞在全套件负载下偶发 `can't start new thread`；jail 限额是 07-12 事故纪律**不动**，改测试：8 worker × 5 ops + Barrier(timeout=30)——同 40 次更新、1/5 线程占用、齐射进 flock 临界区争抢更强不更弱。

**攻击了什么**：自攻：barrier 挂死面（线程 start 失败→已起线程永卡）→ 加 timeout=30 让挂死退化为响亮 BrokenBarrierError。**确定性复现**：子进程 RLIMIT_NPROC=当前+20 下旧式 40 线程死于历史同款 `can't start new thread`、新式 8 线程过（把三轮偶发 flake 变成可判别实验）。**变异实测**：fcntl.flock 打成 no-op，10/10 trial 抓住丢更新——牙口保留。独立评审（fresh reviewer 子代理）：A1-A4/B1-B5/D 全 CONFIRMED 且关键项独立复现（变异 8/8 命中、rlimit margin 70-80 判别窗独立找到、双套件复跑数字逐字吻合）；总判 HAS-ISSUES：**MAJOR-1=我本轮误判 R20「142 passed」为虚报数字**——我三路证据（算术/函数计数/复跑）全部只覆盖 campaign/tests 这一个 scope 共盲，评审复现 `pytest campaign/tests tests/ledger` = **142 passed 4 skipped 精确成立**（tests/ledger 42 项 parametrize 展开、我 find 只搜 ledger/ 漏了 tests/ledger/），R20 数字自始正确；已撤回误判并本人复跑坐实 142+4。MINOR=巡卡计数口径未交代（已补）。

**什么活下来**：测试重写（v2 53 passed ×3 + 全范围 campaign/tests+tests/ledger 142 passed 4 skipped——与 R20 基线逐字一致=回归零复发）；R20 生效验收四项。**批评史对照**：虚高数字**未落地即被评审拦截**（我的错误更正只存在于工作内存、未写进任何文件，如实记为 near-miss 而非复发）；自评判决✓（判决=评审子代理，本人自攻不计）；编造✓（B2 注释被评审逐字核对）；空壳/stale/隐身特性/茅草屋/AI 腔/市场盲 N/A 或零复发。**教训入账**：承重判断的「三路证据」若共享同一 scope 盲区照样全错——下轮起回归口径钉死为分列命令清单（campaign/tests+tests/ledger=142+4、v2/tests=53、workbench/tests=60、innovation 三套件），杜绝 scope 漂移再产误判。**时点验收顺延 R22**：07-19 21:00 v2 窗 + 07-20 03:10 learning tick（连带 R14 判读指引：FNF 先查 Mac 09:30 pull）。

## R22 — 2026-07-19 15:4x（R20 边界④兑现：跨 clone 共写台账普查+merge=union 推广；intake-queue 重写型冲突根治=纯追加事件折叠；双评审 HAS-ISSUES→全修→双 PASS，其中评审再抓我一次 D-014 错标——如实记）

**做了什么**：① 巡 0-工作台 12 行动卡：全部仍真实等 owner、零新回词（envgrant/spendbook decision:发=已处理旧词等手发日期，宪法四裁决槽位空）。② 时点两项（21:00 v2 窗、07-20 03:10 learning tick）未到点，顺延 R23。③ **主项（决断入档：候选 (b) 优先于 K6，因其是 R20 边界④ 的直接兑现且本轮可闭环）**：普查全部活跃 tracked jsonl（近 7 天有 commit 全集，评审交叉核对无漏项）→ 逐个裁定 union 资格（`ledger/research/2026-07-19-crossclone-ledger-survey.md`，写手×机器×写入模式三维判据+方法有效性边界）。三个 tmp 仓库实验第一手坐实：EXP1 union 治纯追加双侧 append（rc=0 三行俱在）、EXP2 重写+追加无 union 必冲突（R20 送达死亡复发形态）、EXP3 union 硬套重写型=rc=0 但同行矛盾 status 静默共存（比冲突更糟）。**落地**：.gitattributes +3 条（owner-gate=预防性、violations=预防性、intake-queue=改造后）；**doctrine/intake.py 从 write_text 全量重写改造为纯追加事件折叠**（捕获行永不改、status=追加事件行、折叠行序无关且平局方向文档化、坏行不砖工具但 pending rc=3、支持 row_id 寻址、hook 零改动、旧内联 status 兼容免迁移）；result-gate.sh 改调 intake.py pending、rc≠0 回退裸 grep（fail-closed）且不吞 stderr；sweep 白名单有意不动（记理由：白名单语义=daemon 持续写的文件，会话作用域写手不该由 host 代提交）。④ 测试：tests/test_doctrine_intake.py 10 项新校准（含坏行不砖+rc=3、同秒平局两行序、row_id 寻址、12 次 shuffle 行序无关）；campaign union 测试升级为行为面双文件驱动（permgate=脏树 sweep 路径 + owner-gate=已 commit 分叉路径=其生产真实形态）。

**攻击了什么**：自攻（ts 格式混排决定性/回退语义/过渡窗口/重复捕获同 id 等 14 项）不算判决。判决=两 fresh 子代理（reviewer 代码面 + general-purpose 架构面），**初判双 HAS-ISSUES 共 3 MAJOR+5 MINOR 全修**：**MAJOR-1（两路独立各自坐实）=我把 owner-gate「推断」错标「git log 实证两侧 commit」**——真相：全史 commit 身份单侧（20/20 Mac）、box 身份从未碰过该文件，我把 07-12 时代 campaign commit 的作者身份想当然成 box（正控对照：permgate 真是两侧 10+11）——D-014/D-015 红线、批评史「编造」类复发形态**在文档层真实落地过、被评审拦下**，已三处更正+修订痕警示（比 R21 的 near-miss 更进一步，如实记）；MAJOR-2=survey 声称「可复现命令内嵌」但全文零代码块→已内嵌，评审逐字复跑通过；MAJOR-3=坏行（缺 prompt）曾崩死全部子命令=解闸工具变砖+闸永久卡死→load() 容错重写，评审用自己的原始复现场景重验+变异实测（删 sys.exit(3) 测试立红）。MINOR×5（同秒平局未文档未测/重复捕获披露/union 测试只驱动 permgate/ratchet 跨机竞态后果未记/「自愈」措辞夸大+2>/dev/null 吞 WARN）全修。**复核轮双 PASS**：评审① 3/3 变异全被咬红+7 套件独立复跑全对；评审② 亲手活体重演坏行回退链（python rc=3→grep 回退→hook rc=2 响亮拦截、WARN 未被吞）。

**什么活下来**：全仓分列回归与 R21 钉死口径逐字一致：campaign/tests+tests/ledger **142 passed 4 skipped**、v2 **53**、workbench **60**、commercial **57**/prospector **18**/supervisor **13 collected**、doctrine 21（11 旧+10 新）；result-gate 活体演练坏行 rc=2/干净 rc=0；生产队列 pending=0 rc=0、3 条 legacy 行折叠正常。**批评史对照**：编造✗→✓（MAJOR-1 是真复发形态、评审拦截后根除并留防复发警示——本轮不粉饰：这是落进文档的错标，不是 near-miss）；自评判决✓（判决=两 fresh 子代理+双复核轮）；空壳✓（评审变异实测 3/3+7/13 有牙）；过程指标当结果✓（本轮交付=存储契约+真实 rebase 实验+活体闸演练，非测试绿宣功）；stale 卡✓ 隐身特性✓（坏行 WARN/rc 全响亮）茅草屋/AI 腔/市场盲 N/A。

**诚实边界（评审在案）**：① owner-gate/violations 的 union 均**预防性（推断：写手模块两机在装可调）**，无跨 clone 冲突实例；只有 permgate 是事故+两侧身份双实证；② 过渡窗口：Mac pull 到新 intake.py 前旧代码重写可产 EXP3 型双胞胎——无事件双胞胎**不因 pull 自愈**，表现为 pending 复亮+闸响亮阻断、需人工补一次 dismiss（方向=过阻断绝不静默丢直令），窗口量级=一次 courier 同步；③ python3 整体缺失时 grep 回退地板值只增不减（已解决行也计入）=永久过阻断——评审①标注的残留，fail-safe 方向、未声称已解；④ violations/intake-queue 的 union 行为面靠通用实验+泛化论证（union driver 与文件名无关），行为测试只真实驱动 permgate+owner-gate 两文件（survey §5 如实披露）；⑤ 评审②发现 doctrine/check.py --quick 在本 box 有 2 个**先在** FAIL（VAULT-ZONES pin 漂移+Mac 侧 ~/.claude/CLAUDE.md 本机缺失）——与本轮 diff 无关、移交 R23 查（pin 漂移若真=冻结文件未提交改动，承重）；⑥ K6 机械前置码检未动，顺延（决断理由见「做了什么」③）。
