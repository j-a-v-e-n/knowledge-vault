> 归宿：看完 → Projects/AI谋生/

# 说一次制宪法——建成报告（2026-07-19）

**一句话：你以后任何规则只说一次，机器保证它永远在、永远被读到、能焊的焊进铁闸、违规会被抓且抓一次硬一级。你重复第二遍 = 系统缺陷。**

打比方：以前你的规矩像贴在各个房间的便利贴——新来的人只看到自己房间那几张，贴纸多了还互相打架。现在全部誊进**一本编号总账**（`~/ai-architect/doctrine/LEDGER.md`），每个房间门口只留一句"规矩看总账"，而且大门装了刷卡机：谁想改规矩、谁想违规矩，机器直接拦。

## 数字

- **找到规则 66 条**（D-001..D-066），逐字、带日期、带出处、带硬度分级。住处曾散在 9+ 个文件 + 3 个 memory 目录 + 聊天记录；含被困在 ai-trader 单项目 memory 里的全局规则（"不用顺着我"）、藏在 v2/ 里没人指向的两条裁定、07-18 裁决包新冻结的两把尺子。
- **焊接覆盖：41/66 已焊**（23 条=确定性代码闸 L3，18 条=checklist/注入层 L2）；其余 25 条留 prose 的**每一条都写了为什么焊不了**（如"何时算重要任务"是语义判断，没法写成代码）——见 `doctrine/WELD-COVERAGE.md`（机器从台账自动生成，不是手写的）。
- **冲突 4 处**，全部逐字并存、零静默裁决，等你在 0-工作台的卡上回 4 个词：C1 谁是权威（CLAUDE.md vs 自称 canonical 的 RULES.md）、C2 三停条款居然有 4 种措辞连数量都不一样、C3 SPEC 与你 07-01 裁定打架（已按你既有裁定直修、diff 附卡可否决）、C6 金库私人区清单抄了 4 份要收敛成 1 份。

## 铁在哪里（不是口头承诺）

1. **双 commit 闸**：改任何入口文件/台账 → `doctrine/check.py` 六项检查自动跑（编号完整/逐字与源文件字节级一致/每个入口有指针/台账外不许有规则散文/常驻文件总量 ≤200 行——这个预算有论文和 Anthropic 官方依据/冻结件 sha 哨兵）。git 层和 Claude 层各拦一道，检查器自己先被 11 个种植违规测试红→绿校准过才上岗。
2. **直令自动入账**：你在聊天里说【直令】（或"铁律/从今以后"这类话），hook 当场把原文抓进队列；会话不逐字入账就**不许收尾**（Stop 闸真拦）；入账时机器校验台账副本和你的原话逐字一致，抄歪了直接拒绝。
3. **违规棘轮**：抓到违规 → 记账 + 该规则硬度自动升一级并强制补建新闸；同规则同级二次被抓 = 棘轮自身缺陷，出卡。

## 演练与实测（全有日志，`doctrine/drill/2026-07-19/`）

- **种植违规演练**：写了一份故意"自我收缩"的假 brief（最小可行版/砍功能不排期/"机器太累"/可寻址未探测）喂给独立评审员——**5 处全抓、正确引用 D-027/D-062**；棘轮全链（首犯升级→二犯判缺陷）在真台账副本上跑真代码留档。
- **背诵测试（before/after 对照）**：同一道"列出 owner 全部规则"考题——改造前的旧文件网络：**至少 15 条规则完全不可见**（章程、memory 裁定、v2 裁定全在指针链外），对照组自己就诊断出四种病；改造后的**真全新会话**：纯顺指针走到台账，**66 条全数背出、编号出处冲突登记全对**，还顺手抓出台账漏的 1 条（当场补录 D-066）——测试本身在给系统补漏。
- **独立评审（非建造者）**：判 HAS-ISSUES——它抓到一次**真违规**：我有一次提交的说明写了"已修 bug"、但那次提交里其实没带上修复（时序失误）。这恰好违反你的"要证据不要声称"（D-013），**被 D-013 自己的焊点（独立评审）在 merge 前抓获**→ 修复实锤入库实测、记录更正、棘轮开出第一条真实记录、评审员自己暴露的权限缺口（在仓里跑了破坏性 git 命令）也顺手焊死。系统第一天就按设计咬人了——包括咬我。
- 附带修的真 bug 还有：原 commit 闸的早退逻辑让追加检查成死代码（红测抓出）；CLAUDE.md 的"权威声明"不等于"去读的指令"（背诵第 1 轮抓出，已改成明令）。

## 诚实边界

- 新 hook（直令捕获）对已开着的会话不热生效，新会话起全自动；本会话已用直调实测全链路。
- Box（WSL 机）已实探（SSH 通、结构一致、入口面落后 main——下次 pull 自动跟上）；它跑的 v2 周期与本次无冲突。
- "200 行预算"的具体数字是证据支撑的目标带（指令数效应有 A 级论文，行数阈值是业界共识），不是实验钉死的常数——如实标注。
- 本周 owner 卡到第 9 张（贴预算线）：说一次制这张是宪法级、值得，之后回落。

## 你的新习惯（只有一行）

**以后想立规矩，随口说、但带上【直令】两个字**（不带也大概率被"铁律/从今以后"这类词捕获）——之后的入账、布线、焊接、审计全是机器的事。改旧规矩同理：说一次，机器出新 D 条、旧条自动标废。

四项裁决卡：[[【宪法｜说一次】四项裁决-回四个词-2026-07-19]]（0-工作台）。
