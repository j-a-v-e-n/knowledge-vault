# PB010 最小组合基线

状态：`CURRENT_PORTFOLIO_POINTER / BACKTRACKED_FROM_PB060`  
生效时间：`2026-07-28T04:00:49-07:00`

## 北极星

本项目继续回答原始问题：在 AI 能力快速扩张的未来，Javen 应如何组合长期能力、现金流、可控制资产与选择权。机会到交易与投研纪律是两个 workflow-local 试验载体，不是项目总目标。

## 为什么回到 PB010

R1、R2、R3 均未通过。R4 在正式 blind generation 前又复现 admission oracle 可把实际 `SCHEMA_INVALID` 的 candidate 接受为 `VALID_SUCCESSOR_PASS`，并发现 standalone conformance validator 不拒绝重复 JSON key。精确依据见 `evidence/blueprint-r4-pre-admission-no-go-2026-07-28.md`。

这满足 PB060 已声明的 stall 条件。继续给 portfolio control surface 增加 receipt、log、签名或 verifier 层，会重复治理循环。因此选择 PB060-C 的既有分支：回到 PB010，只保留 workflow-local baseline 与人工跨项目选择。

## 当前权威拓扑

- 本文件是 portfolio 层的当前导航，不是可执行权限文件，也不签发 workflow PASS。
- `00-项目北极星与三层结构.md` 和 `13-长期能力投资地图与训练路线.md` 保存战略问题、能力假设与训练路线。
- 机会到交易工作流只由它自己的 current pointer、candidate evidence、Gate 与 owner 任务管理；portfolio 不写其 live root。
- 投研纪律工作流只由它自己的 `STATUS.md`、本地治理与 paper-only/human-final 边界管理；portfolio 不写其 candidate。
- portfolio 只可保存带时间和身份的只读 observation；旧 observation 不授权后续动作，live drift 也不重写历史观察。
- 跨项目优先级由 Javen 最终决定。任何自动 portfolio state、review receipt 或模型 qualification 都不产生 authority。

## 当前可依赖事实

- 长期能力与资产问题仍是总目标；单个现金实验或治理系统不能替代它。
- 机会线的 fresh workflow-local refresh 已记录在 `19-PB010两工作流只读刷新与排序输入.md`。v3.2 FAIL 后，owner 按 complexity falsifier 回退到 D032 低主张只读 convenience sample；查询前 checkpoint `414628970a22ae0e290230ad0ad33b747c82e3b1` 因 active plan 权限矛盾获 `FINAL FAIL C0/M1/m1`。successor `978245169e7b2970fc2993b4f3d151e47dd1d42c` 的控制恢复子范围已 `FINAL PASS C0/M0/m0`，但 notebook 草稿隔离不足获 `FINAL FAIL C0/M1/m0`。最终 draft-gate/source-fidelity candidate `ee0703127dd26eab0005ce4e1fa2e69965de5504` 已分别获得 `FINAL PASS C0/M0/m0` 与 `FINAL PASS C0/M0/m1`：五条公开 permalink 的最小引文和保守语义均忠实，污染项保持 quarantine。首个分离式 semantic-eval-v2 design `addfe09a4dd511f3ff43d6085645098e9555ff9e` 随后获 `FINAL FAIL C0/M2/m1`。v2.1 已把恢复入口、逐行 oracle 与 `NOT_TESTED` construct 闭合，但 design 因 adapter 自签 PASS 获 `FINAL FAIL C0/M1/m0`。v2.2 `c6239a53c9e8cca83ea8390443131ffc1261f9f7` 与 pointer `77853f9e89e0500f3507df6432d284b0db80a925` 又关闭自签 verdict、total algebra 与输入隔离；recovery 为 `FINAL PASS C0/M0/m0`，design 仍因内部哈希链缺不可覆盖外部 trust anchor 获 `FINAL FAIL C0/M1/m0`。final v2.3 仍失败并触发 stall；随后 D034 no-information 回溯 successor `fbedb67631a357fc8f204ebea1e1423b060b165a` 获 `FINAL PASS C0/M0/m0`，明确负结果只能回 D020 重选现实证据，不能自动恢复 model qualification。首个五页现实实验设计 `ec064deac7298e41b7fd195979cfab8a94f37cd5` 为 `FINAL FAIL C0/M3/m0` 且未运行。authority 始终为 `NONE`；不证明模型 qualification、query 执行/覆盖、repeated signal、需求、买方、付费意愿、产品或交易。
- 五页现实实验简化 v2 `77958750b41f45a56781e11982069f0f2cb0ac57` 已关闭 v1 的说话者归属和谓词本体两项 Major，但因 output-level claim ceiling/authority constants 未规范绑定仍为 `FINAL FAIL C0/M1/m0`；页面未运行，authority 为 `NONE`。
- root-authority successor `affcb1653c24d7f02954155444a31e22313b16eb` 已规范关闭上述 constants，但其 predeclared negative case list 不能充当 total machine oracle，合并 verdict 为 `FINAL FAIL C0/M1/m1`。因此回退到 minimal manual-read design，不继续扩建负例 DSL 或递归 manifest 自绑定。
- D035 manual-read candidate `96ce25a7ebebc587509e9211e5a3f774af6de2aa` 的 exact fixture、speaker/predicate/page-state 与无汇总边界通过，但两名 reviewer 均因 fresh review 未绑定 immutable draft identity 和可审计 independence 给出 `FINAL FAIL C0/M1/m0`；页面仍为零运行。
- manual v1.1 successor `297dca9ade9301a18737ede6396213c9e414f16b` 已闭合上述 self-accept route，并获两份 `FINAL PASS C0/M0/m0`。权限只到 fixed-five public read-only opens + `UNREVIEWED_DRAFT` capture；reviewed observation 仍不存在。
- 五份 mutable draft 已生成且 operator 全部暂记 `CAPTURED_SOURCE_MATCH`，但 `git add` 因 Codex 使用额度触顶被拒，immutable batch 不存在；这些哈希只用于恢复，不得被 review 或接受为 observation。
- 投研纪律最近的 portfolio-bound observation 仍是 `design_freeze / blocked`，定位仍为 paper-only、human-final 实验室。moving read-only checkpoint 报告 execution-recorder `156/156`、execution-loop `41/41`，但 foundation recovery 的 `33` 项中仍有 `3` 个失败。R5 moving design SHA-256 `58aa43dd5431d3aaea8b9e0338130c0135bbac1c36be70ccd950970850bfd475` 的两份独立只读审查分别为 `REJECT/REVISE C1/M3/m1` 与 `BACKTRACK_TO_STANDARD_GIT C2/M3/m1`：源历史只在临时 repository，且 custom refs/raw objects 没有产生独立故障域。没有 immutable R5 commit/tree，不推进 R5 实现、恢复、Shadow 或投资动作。
- R4 没有 blind、conformance、adversarial 或 outcome receipt，也没有 R4 PASS。
- 当前没有真实需求、付款、稳定收益、复购或可控制资产已经成立的证据。

## 当前安全动作边界

`PB010-ACT-REFRESH-WORKFLOW-LOCAL-BASELINES` 已完成，结果与当前排序输入保存在 `19-PB010两工作流只读刷新与排序输入.md`。现在 portfolio 只做以下动作：

1. 机会线的 evaluator/caller-ownership signature 已在 v2.1、v2.2、v2.3 连续失败并触发预声明 stall。禁止 v2.4 或第四次 pin patch。D034 回溯逻辑已通过；机器 oracle 失败已触发人工路径回退。D035 manual v1.1 `297dca9ade9301a18737ede6396213c9e414f16b` 已获两份 `FINAL PASS C0/M0/m0`。固定五页已只读打开并写出五份 mutable `UNREVIEWED_DRAFT`，但 Git 冻结因 Codex 使用额度触顶而未完成；当前只允许等待额度恢复后重新核哈希并由机会 owner 仅提交五个 draft。没有 immutable batch 就不启动 source review。direct build-time-owned per-lane evaluator继续只作为 backlog，不实施、不运行。
2. 投研纪律已收拢两份 R5 只读挑战并拒绝继续 patch R5。`STANDARD_GIT_AUDIT_BASELINE_V1` 只是下一 design-only 候选建议：标准 Git commit/tree/tag、独立保留 copy 的 tamper-evidence、fresh-clone/restore drill 与有限负例。额度恢复本身不授权保存或冻结；投研 workflow 必须先从它自己的 exact action gate 取得候选文件与 local commit 的明确权限。当前 custom ref/raw-object/genesis、commit/ref/tag/remote/backup、实现与 activation 都为 false。
3. Portfolio 可以把 workflow owner 的 mutable/moving report 追加为明确低证据等级的 checkpoint，用来暴露新未知；只有新冻结的 immutable observation 才能改变 workflow-bound status、closure、权限或提高证据等级。跨项目排序若因 moving note 暂时调整，必须标为可逆推断；现金目标、损失边界和投研用途仍由 Javen 最终裁决。

这些动作不写两个 workflow，不运行 R4，不调用 portfolio model qualification，不联系任何真人。

## 全局禁止项

在新的精确授权前，以下均为 `false`：

- 外部联系、发送消息、申请、公开发布或部署；
- 访问或创建账户、使用凭据或非公开第三方数据；
- 支付、收款、退款、合同、报价或交付承诺；
- 真实投资、影子投资、broker、funds 或投资风险规则变更；
- portfolio 代替 workflow 声称 candidate PASS、Gate 通过、商业进展或模型可迁移。

## 历史对象与 supersession

- `17-总体蓝图活动状态.json`、`16-总体蓝图闭合合同与状态审计.md` 与 R4 request 是冻结的 R4 失败候选，不再是 current action authority。
- `08-活动状态.json` 继续是更早的 historical predecessor。
- R1–R3 FAIL、R4 pre-admission NO-GO 和两工作流自己的历史失败必须保留，不得被新 baseline 覆盖。

## 重新引入 portfolio automation 的条件

默认不重建。只有出现一个 workflow-local 无法解决、且实际阻塞跨项目选择的具体问题，并能预先说明新机制将减少哪个错误、由什么原始证据验收、失败后如何删除时，才重新评估。不能因为“看起来更严谨”或已有大量协议文件而恢复。
