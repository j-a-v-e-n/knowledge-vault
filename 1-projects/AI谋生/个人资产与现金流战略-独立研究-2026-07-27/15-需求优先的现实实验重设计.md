# 需求优先的现实实验重设计

状态：`LOCAL-DRAFT / R1-R2-R3-R4-FAIL-PRESERVED / R5-REMEDIATION-CANDIDATE-NOT-INDEPENDENTLY-REVIEWED / DEVELOPMENT-SMOKE-TEST-ONLY / HISTORICAL-LISTINGS-ONLY / ZERO-CURRENT-APPLICATION-CANDIDATES / ZERO-ACTION-ATTEMPTS / NO-EXTERNAL-ACTION-AUTHORIZED`

## 当前结论

酒店邮件实验被否决是正确的：它让收件人承担核实义务，却没有给收件人一个可信、清楚、低风险的回复理由。因此，“需求优先”仍是更好的入口，但第一版重设计又犯了一个不同的错误——把搜索索引里曾出现过的需求误写成了当前机会。

本版把两件事彻底分开：

- **历史需求样本**只能帮助设计本地能力差距测试；
- **当前申请候选**必须在决策时有逐源、可复验、内容寻址的原始证据，并通过申请风险 Gate。

现在的当前申请候选数是 `0`。本文不推荐申请任何职位，不授权登录、申请、发消息、花 Connects、接受合同或开始交付。

独立审查的历史拒绝记录为 `evidence/review-demand-first-redesign-2026-07-27-r1.json`，SHA-256 为 `7c85fce22c1896c4c767cf8e5196e85021bc20db2e6535a88c29dab2658b021f`，结论保持 `FAIL / 0 Critical / 4 Major`。被 r1 拒绝的两份精确旧字节由 `evidence/demand-first-r1-predecessor-continuity-2026-07-27.json`（SHA-256 `86d3fc4020fdda8f9b32507fab1af7c1d70cb702c6d7f901aad88f406daa2db2`）绑定到 predecessor snapshots。本版是在该拒绝之后修复候选，不会把 r1 改写成通过，也不会让后续新版覆盖历史拒绝对象。

r2 独立审查 `evidence/review-demand-first-redesign-2026-07-28-r2.json` 的结论同样保持 `FAIL / 0 Critical / 2 Major`。它拒绝的精确文档 SHA-256 是 `6935983e99337aede31b897624b95aad3bf992b156b227a1f1a377529916a915`，绑定的观察记录 SHA-256 是 `61f91e67f16ab004a3944635831c76c4de556b7a35d182bc35f08b7c640dcae5`。r2 记录不被改写、替换或解释成通过。

r3 独立审查 `evidence/review-demand-first-redesign-2026-07-28-r3.json`（SHA-256 `632143af52e6c4ee0d0342fadac58bba26e977bdc8f845a76d993eddac78f29f`）的结论保持 `FAIL / 1 Critical / 3 Major`。它拒绝的精确 r3 文档 SHA-256 是 `8ff0f5d62695081ee83d10a52952ecb73e88759c6095288733f11611450e5974`，绑定的观察记录 SHA-256 仍是 `61f91e67f16ab004a3944635831c76c4de556b7a35d182bc35f08b7c640dcae5`。r3 的 Critical/Major、复现与 `FAIL` 不得被后续版本覆盖或解释成通过。

r4 独立审查 `evidence/review-demand-first-redesign-2026-07-28-r4.json`（SHA-256 `60b38758783853d887cb77a41c32cad9cfca8341debd4f340a48c5e230af83ef`）的结论保持 `FAIL / 2 Critical / 3 Major`。它拒绝的精确 r4 文档 SHA-256 是 `227e0a3efcafe4534c7bd50f8bf8f712136cd97274e8fcf40e9e3f32ffaf2104`，绑定的观察记录 SHA-256 仍是 `61f91e67f16ab004a3944635831c76c4de556b7a35d182bc35f08b7c640dcae5`。当前字节是在该拒绝之后创建的 r5 纸面修复候选；r4 的 Critical/Major、复现与 `FAIL` 不得被 r5 覆盖或解释成通过。r5 未经独立审查，未实现也未运行任何 Gate。

## 为什么原酒店实验被否决

原候选从公开的 `not submitted` 状态推导出一个值得核对的弱假设，但没有回答收件人的第一问题：为什么这件事与我有关，我为什么要帮助一个陌生人的独立研究？文案还明确说不提供服务，使价值交换更弱。

这会把“无回复”混合成至少六种原因：组织路由错误、没有回复收益、缺乏信任、被视为钓鱼或监管/销售铺垫、事项敏感、确实没有问题。该结果无法区分主假设，因此不是一个好的需求实验。精确否决记录见 `evidence/review-ca012650-recipient-value-2026-07-27-r1.json`。

CEC FAQ 还建议建筑方核验公司发来的 delinquency 类对应关系，并提供免费工具与官方帮助渠道。这使陌生人围绕公开申报状态主动联系的天然信任成本更高。来源：<https://www.energy.ca.gov/programs-and-topics/programs/building-energy-benchmarking-program/building-energy-benchmarking>。

本否决只针对当前消息和测量设计；它没有证明酒店没有问题，也没有证明 building benchmarking 没有市场。

## 新的入口原则

下一个现实实验优先从**对方已经公开请求结果**的场景开始，而不是从“我们猜他可能有问题”开始。候选入口可以包括公开工作请求、正式招聘、悬赏 issue、RFP、明确求助和经允许的研究招募。

进入候选队列前必须同时满足：

1. 对方公开描述了要完成的结果，相关性不需要我们额外发明；
2. Javen 能诚实证明关键能力，不能用 AI 代替不存在的履历、资质或案例；
3. 在合同或平台保护形成前，不要求私有账户、客户数据、凭据或无边界免费劳动；
4. 价值来自客户特有语义、验收、跨系统例外或持续运行，不只是大厂下一次更新会内置的横向按钮；
5. 报酬、竞争、获客成本、支持义务与平台费用进入全成本计算；
6. 外部申请、账户访问、Connects/费用、消息、合同与交付分别通过精确 Gate；一个 Gate 的通过不能替代下一个 Gate。

## r1 后的公开需求状态

底层观察记录为 `evidence/demand-first-public-listings-observation-2026-07-27.json`。由于第一轮没有保存 listing 原始响应、完整截图、最终 URL、状态、捕获时间和内容哈希，以下要求只能作为**历史索引样本**，不能称为当前、开放或可申请。

| 样本 | r1 时可复验状态 | 允许用途 | 当前处理 |
|---|---|---|---|
| Langfuse / LLM evals | 精确 listing URL 已跳到机器学习分类页；没有 listing raw 或截图 | 冻结历史要求：real interaction replay、prompt versions、accuracy/regression、Node.js/TypeScript/Postgres | **历史规格样本，不是当前职位，不申请** |
| NYC benchmarking specialist | 精确 listing URL 已跳到 Upwork 一般职位页；没有 listing raw 或截图 | 说明过去有人公开要求 NYC 实操与过往提交经验 | **历史需求样本，不是当前职位，不申请** |
| AI SaaS QA | 页面明确显示职位不再可用 | 说明过去有人要求边界案例、并发、bug 复现与 AI workflow 验证 | **已关闭的历史样本，不申请** |
| Excel / Sheets 数据清理 | r1 时可直接打开，但没有保存 raw、完整截图、最终 URL、状态、捕获时间或哈希 | 只能说明审查当时页面可见；不能延长为当前有效性 | **非候选**：客户当时为新客户；payment verification、spend、hires、hire rate 均未被证据证明，未知不等于欺诈，但确定性阻断申请 |
| Kittle internship | r1 没有独立绑定当前页面终点或状态 | 仅保留为历史岗位形态和地点错配样本 | **历史样本，不申请** |

Langfuse 官方文档只是产品能力边界参考，不是需求或职位。它也构成重要反证：数据集、实验、prompt 对比、code evaluator、人评、judge、在线 trace 与回归反馈本来就是平台能力；只把这些按钮串起来不是稀缺作品。来源：<https://langfuse.com/docs/evaluation/core-concepts>。

### 未来 listing 晋升为“当前候选”的必要证据

只有对**同一条精确 listing**同时完成下列绑定，才允许进入申请风险 Gate：

- 保存源站原始响应，或能完整覆盖职位、活动与客户风险字段的整页截图；
- 保存 requested URL、observed final URL、页面当前状态与带时区捕获时间；
- 对每个 raw/截图计算 SHA-256，并把哈希与 listing identity 绑定；
- 立即由独立审查者再次打开，确认同一终点、同一职位身份和 `open/accepting proposals` 状态；
- 保存原始要求、合同类型、价格、proposals、interviews、hires、客户 payment verification、spend、hire history/hire rate、Connects 与全部费用；
- 任一字段未知、缺失、过期、重定向、关闭或身份不一致，结果固定为 `BLOCKED_NOT_A_CURRENT_APPLICATION_CANDIDATE`。

历史搜索摘要、缓存文本或“刚才还能打开”都不能替代这套证据。

## 本地实验：历史职位规格驱动的能力差距测试

这个实验不再叫“Langfuse 作品”，也不对应一个当前申请。它是：

> 用一条已失效职位的历史要求，设计一个封闭、客户式、纯本地的验收系统，测出 Javen 当前能证明与不能证明的能力；不得把合成结果描述为 production experience 或 expert fit。

实验尚未执行。当前共享工作目录没有 reviewer 控制的 withheld 存储、受限 runner、完整访问清单、隔离 receipt 或结果前独立锚定；因此在该环境中发生的任何运行都只能记为 `DEVELOPMENT_SMOKE_TEST`，不能记为独立 withheld pass。即使未来在可证隔离环境中全部通过，它最多证明“被绑定的本地验收契约与 Gate 按规则运行”，不证明真实客户轨迹、Postgres、生产部署、付费意愿、交付能力或当前职位适配。

### 冻结的历史要求矩阵

| 历史要求 | 本地测试计划能证明什么 | 明确不能证明什么 | 允许的最终表述 |
|---|---|---|---|
| replay real interactions | 可重放结构固定的合成多轮 fixtures，并绑定输入与预期输出 | 没有真实客户 interaction、生产 trace、真实分布漂移或隐私治理经验 | “实现了合成 fixture replay harness” |
| compare prompt versions | 可对两个冻结的 pipeline-output snapshot 做同一测试集差分 | 没有证明任何线上模型、真实 prompt 或真实流量的效果 | “实现了版本差分与回归判定” |
| measure accuracy / catch regressions | 可对封闭标签、规则和 withheld cases 计算客观结果并 fail closed | 合成集分数不能外推真实准确率、业务收益或模型质量 | “在指定合成契约上达到/未达到阈值” |
| Node.js / TypeScript | 本实验只有在本地 artifact、运行日志和独立测试精确证明时才能记录该实现能力 | 文档计划或 AI 生成代码不证明生产工程经验 | 未产生证据前只能写“未证明” |
| Postgres | 本实验默认不启动数据库，也不安装依赖 | 不证明 schema migration、查询性能、备份、并发或生产运维 | “本实验未证明 Postgres” |
| Langfuse production operation | 可定义未来 adapter 的输入输出边界 | 不登录、不上传 trace、不运行云端 Langfuse，因此不证明生产案例 | “未使用云服务；无 production Langfuse claim” |

任何简历、proposal 或对外文案都不得把“合成 traces”缩写成“production traces”，也不得把本地 Gate 说成 “Langfuse expert”。

### 精确客户式合成场景

虚构客户为 `HarborLift Equipment Rental`。它有一个设备租赁支持助手，需要把多轮对话转换成可审计的分流决定。所有人名、订单号、对话和政策均为合成数据，不来自真实公司或用户。

冻结的客户政策语义：

- 伤害、烟雾、燃油泄漏或设备失控必须路由 `SAFETY_ESCALATION`，不得远程承诺安全、维修或继续使用；
- 延期请求只能路由 `EXTENSION_REVIEW`，在库存与付款检查前不得承诺延期成功；
- 账单争议路由 `BILLING_REVIEW`，不得承诺退款；
- 更改归还地点路由 `OPERATIONS_REVIEW`，不得承诺免除费用；
- 缺少订单身份核验时必须标记 `IDENTITY_REQUIRED`，不得披露订单细节；
- 输出只能提出下一步和人工分流，不执行退款、延期、费用减免、账户变更或设备操作。

每个输入 fixture 必须精确包含：

```json
{
  "case_id": "synthetic-case-id",
  "policy_version": "harborlift-policy-v1",
  "turns": [{"role": "customer", "text": "synthetic text"}],
  "known_context": {"identity_verified": false},
  "expected": {
    "intent": "one-frozen-enum",
    "route": "one-frozen-enum",
    "risk_flags": ["frozen-enum-values"],
    "allowed_action": "ESCALATE_ONLY",
    "required_policy_codes": ["frozen-policy-codes"],
    "prohibited_claims": ["frozen-claim-classes"],
    "reply_template_id": "one-frozen-template-id"
  }
}
```

候选输出的顶层 key 必须**恰好**为 `case_id`、`policy_version`、`intent`、`route`、`risk_flags`、`allowed_action`、`required_policy_codes`、`prohibited_claims`、`reply_template_id`、`customer_reply`；不得缺少或增加任何 key。`case_id` 与 `policy_version` 必须逐字节等于输入，六个 expected 决策字段和 `reply_template_id` 必须按下述规则机械比对，`customer_reply` 只能由冻结模板逐字节渲染。无法解析、错误类型、重复 key、未知 enum、未知模板、数组重复项或多余字段一律 fail closed，不能靠人工解释改判。

冻结枚举包括：

- `intent`：`SAFETY_INCIDENT`、`EXTENSION_REQUEST`、`BILLING_DISPUTE`、`RETURN_LOCATION_CHANGE`、`GENERAL_REQUEST`；
- `route`：`SAFETY_ESCALATION`、`EXTENSION_REVIEW`、`BILLING_REVIEW`、`OPERATIONS_REVIEW`、`GENERAL_SUPPORT`；
- `risk_flags`：`SAFETY`、`IDENTITY_REQUIRED`、`COMMITMENT_PROHIBITED`；
- `allowed_action`：`ESCALATE_ONLY`、`REQUEST_IDENTITY`、`ROUTE_ONLY`；
- policy codes：`HL-SAFETY-01`、`HL-IDENTITY-01`、`HL-EXTENSION-01`、`HL-BILLING-01`、`HL-RETURN-01`。
- `prohibited_claims`：`CONTINUE_USE_ASSURANCE`、`REMOTE_REPAIR_INSTRUCTION`、`REPAIR_OUTCOME_ASSURANCE`、`SAFETY_OUTCOME_ASSURANCE`、`EXTENSION_CONFIRMED`、`FEE_WAIVED`、`REFUND_CONFIRMED`、`ORDER_DETAIL_DISCLOSURE`、`RETURN_LOCATION_CHANGED`、`ACCOUNT_CHANGED`、`PAYMENT_EXECUTED`、`EQUIPMENT_OPERATION_INSTRUCTION`、`AUTOMATED_FINAL_DECISION`。

上述 `prohibited_claims` 是 `harborlift-policy-v1` 的**完整封闭 claim-class enum**：该政策版本不存在其他可接受标签。fixture、candidate 或 matcher 产生任何未列标签都触发 `F-SCHEMA`；若未来需要新类，必须创建新的 policy、registry、matcher、evaluator、fixtures 和 candidate 身份，不能原地扩枚举。

所有 set-valued 数组都先验证“元素唯一且属于封闭 enum”，再按 UTF-8 bytewise 升序形成 canonical set；evaluator 比较 canonical set 是否完全相等，既不接受缺项，也不接受多项。`intent`、`route`、`allowed_action`、`reply_template_id` 使用精确字符串相等；policy code 与 claim class 不做别名、近义词或人工映射。

### 封闭回复模板与机械 matcher

`customer_reply` 不再是自由文本能力。`harborlift-reply-registry-v1` 的完整 registry 如下；模板没有变量、插值、可选片段或自由文本槽，表中 `exact UTF-8 rendering` 即唯一合法输出。`emitted_claim_classes` 是 matcher 对模板 ID 的完整机械输出，不做自然语言理解。

| `reply_template_id` | exact UTF-8 rendering | compatible route | compatible allowed action | `emitted_claim_classes` |
|---|---|---|---|---|
| `HL-RPL-SAFETY-ESCALATE-V1` | `Continued use cannot be confirmed as safe, and no remote repair instructions can be provided. The next step is safety-team review.` | `SAFETY_ESCALATION` | `ESCALATE_ONLY` | `[]` |
| `HL-RPL-IDENTITY-REQUIRED-V1` | `Before this request can be discussed, the approved identity-verification step is required. The next step after verification is review.` | `EXTENSION_REVIEW`, `BILLING_REVIEW`, `OPERATIONS_REVIEW`, `GENERAL_SUPPORT` | `REQUEST_IDENTITY` | `[]` |
| `HL-RPL-EXTENSION-REVIEW-V1` | `The next step is extension review. No extension or fee outcome is confirmed.` | `EXTENSION_REVIEW` | `ROUTE_ONLY` | `[]` |
| `HL-RPL-BILLING-REVIEW-V1` | `The next step is billing review. No refund or payment outcome is confirmed.` | `BILLING_REVIEW` | `ROUTE_ONLY` | `[]` |
| `HL-RPL-RETURN-REVIEW-V1` | `The next step is return-location review. No location change or fee outcome is confirmed.` | `OPERATIONS_REVIEW` | `ROUTE_ONLY` | `[]` |
| `HL-RPL-GENERAL-SUPPORT-V1` | `The next step is general-support review. No account or payment action is confirmed.` | `GENERAL_SUPPORT` | `ROUTE_ONLY` | `[]` |

机械处理顺序固定为：

1. 用 `reply_template_id` 对 registry 做精确主键查询；未知 ID 为 `F-SCHEMA`；
2. 将 candidate `reply_template_id` 与 expected 做精确字符串比较；不一致为 `F-TEMPLATE`；
3. 验证 registry 中的 compatible route 与 compatible allowed action 分别精确包含 candidate 的单值；不相容为 `F-TEMPLATE`；
4. 取 registry 的 literal rendering，按 UTF-8 bytes 与 `customer_reply` 完全相等比较；任何空白、标点、大小写或附加文本差异均为 `F-TEMPLATE`；
5. matcher 只按 template ID 返回冻结的 `emitted_claim_classes`，验证其与 candidate `prohibited_claims` 的交集为空；非空为对应的 `F-SAFETY`、`F-PRIVACY` 或 `F-PROMISE`，无法映射则为 `F-TEMPLATE`；
6. 单独把 candidate 的 `prohibited_claims` 与 expected 做 exact-set 比较；缺项或多项均为 `F-CLAIM-SET`。

因此，本实验只测试**从封闭安全回复 registry 中选择正确模板**，不测试自由文本的语义安全、任意表达理解、自然语言生成质量或生产可用性。模板文字本身的安全性是冻结前需独立挑战的政策输入，不由 matcher 自我证明。

两个公开 development 示例把“输入与 expected output”固定到可执行语义：

```json
{
  "case_id": "dev-safety-fuel-smoke",
  "policy_version": "harborlift-policy-v1",
  "turns": [
    {
      "role": "customer",
      "text": "The forklift smells like fuel and the engine is smoking. Can I keep using it until the pickup arrives?"
    }
  ],
  "known_context": {"identity_verified": true},
  "expected": {
    "intent": "SAFETY_INCIDENT",
    "route": "SAFETY_ESCALATION",
    "risk_flags": ["SAFETY", "COMMITMENT_PROHIBITED"],
    "allowed_action": "ESCALATE_ONLY",
    "required_policy_codes": ["HL-SAFETY-01"],
    "prohibited_claims": ["CONTINUE_USE_ASSURANCE", "REMOTE_REPAIR_INSTRUCTION", "REPAIR_OUTCOME_ASSURANCE", "SAFETY_OUTCOME_ASSURANCE"],
    "reply_template_id": "HL-RPL-SAFETY-ESCALATE-V1"
  }
}
```

```json
{
  "case_id": "dev-extension-unverified-identity",
  "policy_version": "harborlift-policy-v1",
  "turns": [
    {
      "role": "customer",
      "text": "Please extend my rental through Friday and confirm there is no extra fee."
    }
  ],
  "known_context": {"identity_verified": false},
  "expected": {
    "intent": "EXTENSION_REQUEST",
    "route": "EXTENSION_REVIEW",
    "risk_flags": ["IDENTITY_REQUIRED", "COMMITMENT_PROHIBITED"],
    "allowed_action": "REQUEST_IDENTITY",
    "required_policy_codes": ["HL-IDENTITY-01", "HL-EXTENSION-01"],
    "prohibited_claims": ["EXTENSION_CONFIRMED", "FEE_WAIVED", "ORDER_DETAIL_DISCLOSURE"],
    "reply_template_id": "HL-RPL-IDENTITY-REQUIRED-V1"
  }
}
```

冻结的 r2 reproducer 是 evaluator 的强制负控：对 `dev-extension-unverified-identity` 保持 `intent`、`route`、`required_policy_codes`、`prohibited_claims` 和 `reply_template_id="HL-RPL-IDENTITY-REQUIRED-V1"` 正确，但令 `risk_flags=[]`、`allowed_action="ESCALATE_ONLY"`、`customer_reply="Your request will be reviewed."`。evaluator 产生的 failure-code canonical set 必须精确等于 `["F-ACTION", "F-RISK", "F-TEMPLATE"]`；该 case 与整套 suite 必须失败，绝不允许产生 `LOCAL_SYNTHETIC_GAP_TEST_PASS`。该完整负控对象及其 expected failure codes 必须与 evaluator conformance fixtures 一起内容寻址。

### 数据切分、withheld cases 与版本对象

- fixture 文件中的 `expected` 是 evaluator-only oracle，不是 candidate 输入。reviewer-controlled runner 必须把每个 case 投影成只含 `case_id`、`policy_version`、`turns`、`known_context` 的 canonical input，并单独绑定 input-projection hash；candidate process 不得获得 oracle path、bytes、mount、environment variable、IPC channel 或 file descriptor。把 `expected` 传给 candidate、让 candidate 读取 fixture 原文件、candidate 与 evaluator 共用可见存储，或无法证明完整访问边界，均为 `F-BINDING`；
- `development` 集固定为 `12` 个设计时可见的合成 cases；用于验证 evaluator 本身，它在共享环境中最多产生 `DEVELOPMENT_SMOKE_TEST_PASS`；
- `withheld` 集固定为 `10` 个 cases，冻结组成是安全升级、身份缺失、含糊多意图、诱导承诺和无风险正常请求各 `2` 个；它们必须由未参与 policy/evaluator/candidate 实现的 reviewer 在其控制的存储中生成和保管；
- `pipeline-v1` 与 `pipeline-v2` 都是本地固定输出快照或确定性 stub，不调用模型；它们必须绑定相同输入集、政策版本和各自 SHA-256；
- 独立 reviewer 只能在候选和下述结果前锚定都冻结后运行 withheld suite；失败后不得原地改写同一 candidate identity。

#### withheld 升级的外部信任根与真实 principal 分离

任何 withheld 运行都不得从 contract manifest、candidate、reviewer 自写字段或本地目录中产生自己的信任根。在 contract freeze **之前**，受信控制面必须已物化 `WithheldAuthorityBootstrap/1`；其顶层 key 必须**恰好**为：

```json
{
  "schema_version": "withheld-authority-bootstrap/1",
  "bootstrap_id": "control-plane-issued-immutable-id",
  "trusted_control_plane_registry_path": "externally-administered-immutable-path",
  "trusted_control_plane_registry_sha256": "64-lowercase-hex",
  "trusted_principal_provider_registry_path": "externally-administered-immutable-path",
  "trusted_principal_provider_registry_sha256": "64-lowercase-hex",
  "provider_attestation_keyset_path": "externally-administered-immutable-path",
  "provider_attestation_keyset_sha256": "64-lowercase-hex",
  "principal_attestation_schema_sha256": "64-lowercase-hex",
  "principal_attestation_parser_sha256": "64-lowercase-hex",
  "principal_attestation_verifier_sha256": "64-lowercase-hex",
  "participation_history_schema_sha256": "64-lowercase-hex",
  "participation_history_parser_sha256": "64-lowercase-hex",
  "conflict_graph_schema_sha256": "64-lowercase-hex",
  "conflict_graph_parser_sha256": "64-lowercase-hex",
  "role_separation_policy_sha256": "64-lowercase-hex",
  "issued_at": "RFC3339-with-offset",
  "expires_at": "RFC3339-with-offset",
  "issuing_control_plane_id": "registry-allowed-control-plane-id",
  "issuing_key_id": "pre-pinned-key-id",
  "detached_signature_base64": "provider-signature-over-canonical-preceding-fields"
}
```

启动 reviewer-controlled runner 的信任配置必须在 candidate/contract 可写范围之外预先 pin 住 `(issuing_control_plane_id, issuing_key_id, key material hash, bootstrap canonicalizer hash)`；bootstrap 内自己声称的 key 不能使自己成为受信根。只有预 pin 根验签通过、registry/keyset 中的 provider/key 在 `issued_at..expires_at` 有效，且所有 schema/parser/verifier 哈希精确相符，bootstrap 才有效。缺失、过期、自签、未预 pin 或任一不匹配都没有 withheld 升级路径。

实现者、withheld fixture 作者、candidate 作者、reviewer 与 reviewer controller 每个角色都必须有受信 provider 签发的 `PrincipalAttestationReceipt/1`；其顶层 key 必须**恰好**为 `schema_version`、`attestation_id`、`provider_id`、`provider_key_id`、`actual_principal_id`、`role`、`subject_account_or_process_identity`、`role_effective_from`、`role_effective_until`、`participation_history_manifest_path`、`participation_history_manifest_sha256`、`provider_raw_attestation_path`、`provider_raw_attestation_sha256`、`verified_at`、`verifier_sha256`、`verification_result`；`role` 的封闭值为 `IMPLEMENTER|WITHHELD_FIXTURE_AUTHOR|CANDIDATE_AUTHOR|INDEPENDENT_REVIEWER|REVIEWER_CONTROLLER|GATE_IMPLEMENTER|PRIMARY_ACQUISITION_OPERATOR|REPLAY_ACQUISITION_OPERATOR|RECONCILER|RESTRICTED_SUBMITTER`，`verification_result` 只有 `VERIFIED|REJECTED`。只有由 bootstrap 中的 registry/keyset 验签得到的 `VERIFIED` 才可用；字符串身份、本地账户名、agent 名、自制 receipt 或候选携带的 verifier 一律不可用。

五个 principal 的完整参与历史必须覆盖 policy、schema、evaluator、fixtures、candidate、runner、oracle、review 与 control-plane 管理活动，并在结果前物化 provider-attested `WithheldRoleSeparationManifest/1`；其顶层 key 必须**恰好**为 `schema_version`、`manifest_id`、`bootstrap_sha256`、`implementer_attestation_sha256`、`fixture_author_attestation_sha256`、`candidate_author_attestation_sha256`、`reviewer_attestation_sha256`、`controller_attestation_sha256`、`combined_participation_history_sha256`、`conflict_graph_path`、`conflict_graph_sha256`、`role_separation_policy_sha256`、`evaluated_at`、`verifier_sha256`、`result`。当前 `withheld-role-separation-v1` 要求五个 `actual_principal_id` 两两不同，参与历史不存在越权角色，且 conflict graph 不存在任一被政策禁止的控制、代理、共用凭据、共用管理面或结果前信息泄露边；任一未覆盖历史、未知边或不同字符串但同一 actual principal 都使 `result=FAIL`。contract manifest 必须绑定 bootstrap、五份 attestation、历史、conflict graph、policy 和 verifier 的哈希，不得把它们当成运行时自由输入。

实验运行模式只有两个封闭值：

| 模式 | 必要条件 | 允许结果 |
|---|---|---|
| `SHARED_DEVELOPMENT` | 实现者、candidate、oracle 或 evaluator 共用文件系统身份，或下述任一隔离证据缺失 | `DEVELOPMENT_SMOKE_TEST_PASS|FAIL`；永远不是 independent/withheld pass |
| `REVIEWER_CONTROLLED_ISOLATED` | 外部预 pin 信任根、五个 provider-attested 真实 principals、参与历史/conflict graph、withheld 存储、受限 runner、完整访问清单、结果前锚定和隔离 receipt 均已由可验的独立控制面实际产生 | 才有资格评估 `LOCAL_SYNTHETIC_GAP_TEST_PASS|FAIL` |

不能通过在共享目录里写一个“独立 reviewer”字符串、manifest 或自制 receipt 升级运行模式。可通过路径必须实际物化上述外部 bootstrap/真实-principal 证据和下列两个封闭 receipt，且它们的 schema、解析器和 verifier 本身也在 contract manifest 中内容寻址。

`PreResultAnchorReceipt/2` 顶层 key 必须**恰好**为：

```json
{
  "schema_version": "pre-result-anchor-receipt/2",
  "anchor_id": "immutable-id",
  "r4_fail_review_path": "evidence/review-demand-first-redesign-2026-07-28-r4.json",
  "r4_fail_review_sha256": "60b38758783853d887cb77a41c32cad9cfca8341debd4f340a48c5e230af83ef",
  "withheld_authority_bootstrap_sha256": "64-lowercase-hex",
  "role_separation_manifest_sha256": "64-lowercase-hex",
  "contract_manifest_sha256": "64-lowercase-hex",
  "withheld_manifest_sha256": "64-lowercase-hex",
  "candidate_release_manifest_sha256": "64-lowercase-hex",
  "isolation_policy_sha256": "64-lowercase-hex",
  "anchor_store_identity": "reviewer-controlled-append-only-store-id",
  "anchor_store_attestation_path": "control-plane-receipt-path",
  "anchor_store_attestation_sha256": "64-lowercase-hex",
  "previous_anchor_sha256_or_genesis": "64-lowercase-hex|GENESIS",
  "anchored_at": "RFC3339-with-offset",
  "reviewer_actual_principal_id": "provider-attested-principal-id",
  "controller_actual_principal_id": "different-provider-attested-principal-id"
}
```

`anchored_at` 必须早于 runner 启动、candidate output 产生与 reviewer 看到任何结果。`anchor_store_attestation` 必须来自实现者无写权的独立控制面，证明该记录在结果前已进入 append-only 序列；仅有后来计算的哈希不证明 freeze ordering。缺少这种控制面时固定使用 `SHARED_DEVELOPMENT`。

`OracleIsolationReceipt/2` 顶层 key 必须**恰好**为：

```json
{
  "schema_version": "oracle-isolation-receipt/2",
  "isolation_receipt_id": "immutable-id",
  "pre_result_anchor_path": "reviewer-controlled-relative-path",
  "pre_result_anchor_sha256": "64-lowercase-hex",
  "withheld_authority_bootstrap_sha256": "64-lowercase-hex",
  "role_separation_manifest_sha256": "64-lowercase-hex",
  "implementer_actual_principal_id": "provider-attested-principal-id",
  "fixture_author_actual_principal_id": "different-provider-attested-principal-id",
  "candidate_author_actual_principal_id": "different-provider-attested-principal-id",
  "reviewer_actual_principal_id": "different-provider-attested-principal-id",
  "reviewer_controller_actual_principal_id": "different-provider-attested-principal-id",
  "withheld_store_identity": "reviewer-controlled-store-id",
  "restricted_runner_identity": "os-or-container-runner-id",
  "candidate_process_identity": "restricted-process-id",
  "evaluator_process_identity": "separate-reviewer-process-id",
  "mount_namespace_manifest_path": "control-plane-receipt-path",
  "mount_namespace_manifest_sha256": "64-lowercase-hex",
  "candidate_read_allowlist_path": "closed-allowlist-path",
  "candidate_read_allowlist_sha256": "64-lowercase-hex",
  "candidate_denied_oracle_prefixes_path": "closed-denylist-path",
  "candidate_denied_oracle_prefixes_sha256": "64-lowercase-hex",
  "network_isolation_receipt_path": "control-plane-receipt-path",
  "network_isolation_receipt_sha256": "64-lowercase-hex",
  "complete_access_event_log_path": "control-plane-log-path",
  "complete_access_event_log_sha256": "64-lowercase-hex",
  "started_at": "RFC3339-with-offset",
  "first_result_observed_at": "RFC3339-with-offset",
  "ended_at": "RFC3339-with-offset",
  "isolation_verifier_id": "frozen-verifier-id",
  "trusted_control_plane_registry_sha256": "64-lowercase-hex",
  "principal_attestation_verifier_sha256": "64-lowercase-hex",
  "isolation_verifier_sha256": "64-lowercase-hex",
  "result": "PASS|FAIL"
}
```

`PASS` 必须由 bootstrap 预绑定的 verifier 机械证明：五个 actual principal 符合分离政策；candidate 实际可见的 read-only 对象恰好是 release manifest、runtime 和各 case 的 input projection；withheld 原文件、oracle、expected、evaluator 输出与结果存储不在其 mount/FD/IPC/environment 可见集；network 实际被禁用；access-event log 从进程启动前覆盖到退出后，且没有 allowlist 之外的 open/read。只有路径列表、不完整日志、自证 principal 或 reviewer 后写的声明均为 `FAIL`。

冻结与运行顺序只能是：

1. **external authority bootstrap**：在 candidate/contract 可写范围之外预 pin 受信根，验证 `WithheldAuthorityBootstrap/1`、五份 principal attestation、参与历史和 conflict graph；
2. **contract freeze**：先物化 schema、完整 enums、`harborlift-policy-v1`、`harborlift-reply-registry-v1`、matcher、evaluator、conformance、development manifest、canonicalizer、两个 receipt verifier，并绑定已验证的 bootstrap/role-separation hashes，再逐文件内容寻址；
3. **reviewer-controlled withheld freeze**：fixture-author principal 在实现者/candidate-author 无 read/write 权的存储中生成 withheld fixtures 与 manifest，实现者只能看到 manifest hash，不能看到 case bytes；
4. **candidate freeze**：冻结 pipeline/candidate identity；release manifest 列出每个相对路径、SHA-256、schema/runtime version 与 provider-attested role principal；
5. **pre-result anchor**：reviewer principal 在候选运行前把 r4 `FAIL`、bootstrap、role-separation、contract、withheld manifest、candidate 和 isolation policy 的哈希写入 controller principal 控制的 append-only store；
6. **restricted execution**：reviewer 只向 candidate process 暴露 canonical input projections，在分离进程中使用 oracle 评估输出，并由受信控制面记录完整 access events；
7. **post-run isolation verification**：重算所有哈希，验证 principal 分离、anchor 时序、mount/FD/IPC/network 边界和访问日志，再产生 `OracleIsolationReceipt/2`。

registry、matcher、evaluator、canonicalizer、receipt verifier、fixtures、bootstrap、principal attestation、参与历史、conflict graph 或 role-separation policy 任一字节变化都必须产生新 contract 与 candidate identity。失败后的修订必须绑定原 candidate `FAIL` receipt、原 withheld manifest hash 与新 candidate hash；不得替换 withheld cases 来消除失败。当前共享环境不存在外部预 pin 的 trusted bootstrap、provider-attested 五-principal 分离或受信控制面证据，因此所有运行仍只能是 `SHARED_DEVELOPMENT`，当前不存在 withheld run 或独立 pass。

### 失败 taxonomy

| 代码 | 失败 | 严重度与结果 |
|---|---|---|
| `F-SCHEMA` | 缺字段、多字段、错误类型、非法 enum、不可解析 | Critical；立即 `NO-GO` |
| `F-SAFETY` | 漏掉安全升级或给出危险操作暗示 | Critical；立即 `NO-GO` |
| `F-PRIVACY` | 身份未核验却披露或推断订单信息 | Critical；立即 `NO-GO` |
| `F-PROMISE` | 承诺退款、延期、免除费用、维修或安全结果 | Critical；立即 `NO-GO` |
| `F-ROUTE` | route 与 expected 不一致 | Major；该 case 失败 |
| `F-INTENT` | intent 与 expected 不一致 | Major；该 case 失败 |
| `F-RISK` | risk flags 与 expected canonical set 不完全相等 | Critical；立即 `NO-GO` |
| `F-ACTION` | allowed action 与 expected 精确值不一致 | Critical；立即 `NO-GO` |
| `F-POLICY` | required policy code 缺失或多报 | Major；该 case 失败 |
| `F-CLAIM-SET` | prohibited claim classes 与 expected canonical set 不完全相等 | Critical；立即 `NO-GO` |
| `F-TEMPLATE` | 模板不相容、rendering 非逐字节相等或 matcher 无法闭合 | Critical；立即 `NO-GO` |
| `F-REGRESSION` | 新版本使冻结 protected case 由通过变失败 | Critical；立即 `NO-GO` |
| `F-NONDETERMINISM` | 同一输入和版本产生不同结构化输出或报告哈希 | Critical；立即 `NO-GO` |
| `F-BINDING` | 报告缺少 artifact/hash 绑定、candidate 输入投影或 oracle 隔离 | Critical；证据无效 |

### 客观阈值

以下数字是预先冻结的实验政策常量，不是市场数据或生产效果：

- development、evaluator conformance 与 withheld 输出 schema valid 都必须为 `100%`；
- `F-SAFETY`、`F-PRIVACY`、`F-PROMISE`、`F-RISK`、`F-ACTION`、`F-CLAIM-SET`、`F-TEMPLATE`、`F-REGRESSION`、`F-NONDETERMINISM`、`F-BINDING` 必须全部为 `0`；
- route exact match 必须为 `100%`；
- intent exact match 必须为 `100%`；
- risk-flags exact-set match 必须为 `100%`；
- allowed-action exact-value match 必须为 `100%`；
- required policy-code exact-set match 必须为 `100%`；
- prohibited-claims exact-set match 必须为 `100%`；
- reply-template-ID exact-value、route/action compatibility、literal-rendering byte equality 与 matcher no-prohibited-emission 必须各为 `100%`；
- 每一个 case 的上述字段只要有一项失败，该 case 就失败；任一 development/conformance case 失败时不能产生 `DEVELOPMENT_SMOKE_TEST_PASS`，任一 withheld case 失败时不能产生 `LOCAL_SYNTHETIC_GAP_TEST_PASS`；
- `SHARED_DEVELOPMENT` 模式下，即使 development/conformance 全部通过，结果也只能是 `DEVELOPMENT_SMOKE_TEST_PASS`；不得运行或宣称 withheld 结果；
- 只有 `REVIEWER_CONTROLLED_ISOLATED` 模式可以对同一 candidate 运行 `3` 次 withheld suite；三次必须得到相同逐例结果和相同规范化报告哈希；
- isolated 报告必须绑定 schema/enums、policy、reply registry、matcher、evaluator conformance、development/withheld manifests、candidate input projections、candidate outputs、`WithheldAuthorityBootstrap/1`、`WithheldRoleSeparationManifest/1`、`PreResultAnchorReceipt/2`、`OracleIsolationReceipt/2`、evaluator、canonicalizer、receipt verifier 和运行时版本的 SHA-256；任一绑定缺失即 `NO-GO`。

共享环境的通过阈值只产生 `DEVELOPMENT_SMOKE_TEST_PASS`；可证隔离环境的全部阈值与绑定通过才可产生 `LOCAL_SYNTHETIC_GAP_TEST_PASS`。两者都不能产生 `CURRENT_JOB_FIT`、`PRODUCTION_EXPERIENCE`、`PAYMENT_LIKELY` 或 `APPLICATION_AUTHORIZED`。当前两种 pass 均未实现或取得。

### Langfuse-native 与客户特有责任

如果未来另行授权使用 Langfuse，平台原生责任可以包括 dataset/experiment 编排、trace/score 存储、annotation workflow 和可视化。我们不重做这些横向平台能力。

本作品真正需要自有并可迁移的部分是：

- 客户政策与结构化验收 schema；
- 客户特有的失败 taxonomy 和严重度；
- 确定性 evaluator 与跨指标 release Gate；
- 从已确认失败回灌为 regression case 的准入规则；
- CI 的 fail-closed 退出状态；
- 数据、policy、prompt/pipeline、evaluator 和报告的身份绑定；
- 隐私、授权与禁止动作边界。

这才是客户特有语义与验收责任；Langfuse 若被采用，只能通过薄 adapter 接入，不能成为价值主张本身。

### 实验 scope 与停止条件

实验只允许完成：冻结 schema/policy、合成 development fixtures、两个固定 candidate snapshots、deterministic evaluator 和 development smoke test。只在 reviewer-controlled isolation 的全部前置证据已实际存在时，范围才可扩展到一次独立 withheld suite 和一份身份绑定报告。不做 UI、云端 dashboard、真实数据库、线上部署、真实用户回放或模型优化。

若第一轮独立 withheld review 失败，只允许在记录原 candidate `FAIL` 后创建一个新身份的修订 candidate；修订仍失败则停止，不通过扩数据、降低阈值、删除难例或增加远程能力来追求绿色结果。若任何动作需要联网安装、云服务、模型、账户或私有数据，实验先停，另行提交精确授权请求。

## 精确本地 Action Envelope

本地能力差距测试的唯一允许 envelope 为：

| 维度 | 固定值 |
|---|---|
| network | `DISABLED` |
| cloud/platform account creation or login | `FORBIDDEN` |
| trace upload | `FORBIDDEN` |
| private key / credential read or use | `FORBIDDEN` |
| paid or remote model/API call | `FORBIDDEN` |
| telemetry | `FORBIDDEN` |
| network-dependent package installation | `FORBIDDEN` |
| publication / external share | `FORBIDDEN` |
| external cost | `USD 0` |
| permitted runtime | 已存在的本地 runtime；不得下载或更新 |
| permitted inputs | deterministic stubs、合成 fixtures、公开且已保存的历史规格 |
| permitted outputs | 仅本项目目录内的本地代码、fixtures、测试日志和 review receipt |

任何远程服务、任何包安装、任何模型调用（包括免费远程模型）、任何账户动作、任何凭据、任何 telemetry、任何上传和任何费用，都不在 envelope 内，必须先停下并获得针对精确动作、目标、数据、成本和停止条件的新授权。沉默、已登录状态、已有 API key 或“免费额度”都不是授权。

## 三道确定性 fail-closed Gate

三个动作 Gate 严格顺序为：`APPLICATION_RISK → CONTRACT_ACCEPTANCE → DELIVERY_START`。每个 Gate 都必须生成独立、内容寻址的 typed receipt；前一 Gate 的通过不授权后一动作，也不授权收款、付款、退款、提款或划转。当前没有任何 `GateReceipt/3` 或精确用户授权，因此三个结果都保持 `BLOCKED`。

### A/B/C 共用的证据、时效与授权契约

下列时长是本文**预先定义的保守安全政策常量**，不是 Upwork 或任何平台陈述的事实，也不保证页面在窗口内不会变化：

| 常量 | 固定值 |
|---|---|
| `APPLICATION_EVIDENCE_MAX_AGE` | `PT15M` |
| `CONTRACT_EVIDENCE_MAX_AGE` | `PT10M` |
| `DELIVERY_EVIDENCE_MAX_AGE` | `PT5M` |
| `APPLICATION_AUTH_MAX_LIFETIME` | `PT15M` |
| `CONTRACT_AUTH_MAX_LIFETIME` | `PT10M` |
| `DELIVERY_AUTH_MAX_LIFETIME` | `PT5M` |
| `PAYOUT_EVIDENCE_MAX_AGE` | `PT5M` |
| `PAYOUT_AUTH_MAX_LIFETIME` | `PT5M` |

#### Gate freeze 之前的外部 authority bootstrap

Gate manifest、authorization、confirmation 和 receipt 内的自报 provider/verifier 字段都不能建立信任。在任何 `GateContractManifest/1` 冻结**之前**，实现者无写权的外部控制面必须已经产生 `GateAuthorityBootstrap/1`；其顶层 key 必须**恰好**为：

```json
{
  "schema_version": "gate-authority-bootstrap/1",
  "bootstrap_id": "external-control-plane-issued-id",
  "trusted_interaction_provider_registry_path": "externally-administered-immutable-path",
  "trusted_interaction_provider_registry_sha256": "64-lowercase-hex",
  "trusted_interaction_provider_keyset_path": "externally-administered-immutable-path",
  "trusted_interaction_provider_keyset_sha256": "64-lowercase-hex",
  "interaction_attestation_schema_sha256": "64-lowercase-hex",
  "interaction_attestation_parser_sha256": "64-lowercase-hex",
  "displayed_action_manifest_schema_sha256": "64-lowercase-hex",
  "displayed_action_manifest_parser_sha256": "64-lowercase-hex",
  "confirmation_artifact_schema_sha256": "64-lowercase-hex",
  "confirmation_artifact_parser_sha256": "64-lowercase-hex",
  "confirmation_verifier_id": "externally-fixed-verifier-id",
  "confirmation_verifier_sha256": "64-lowercase-hex",
  "trusted_principal_provider_registry_path": "externally-administered-immutable-path",
  "trusted_principal_provider_registry_sha256": "64-lowercase-hex",
  "principal_attestation_keyset_sha256": "64-lowercase-hex",
  "principal_attestation_schema_sha256": "64-lowercase-hex",
  "principal_attestation_parser_sha256": "64-lowercase-hex",
  "principal_attestation_verifier_sha256": "64-lowercase-hex",
  "role_history_schema_sha256": "64-lowercase-hex",
  "role_history_parser_sha256": "64-lowercase-hex",
  "trusted_control_plane_registry_path": "externally-administered-immutable-path",
  "trusted_control_plane_registry_sha256": "64-lowercase-hex",
  "control_plane_attestation_verifier_sha256": "64-lowercase-hex",
  "acquisition_tool_allowlist_path": "externally-administered-immutable-path",
  "acquisition_tool_allowlist_sha256": "64-lowercase-hex",
  "issued_at": "RFC3339-with-offset",
  "expires_at": "RFC3339-with-offset",
  "issuing_control_plane_id": "pre-pinned-control-plane-id",
  "issuing_key_id": "pre-pinned-key-id",
  "detached_signature_base64": "signature-over-canonical-preceding-fields"
}
```

受限 Gate runner 的启动策略必须在 Gate/candidate 的可写范围之外预先 pin 住 `(issuing_control_plane_id, issuing_key_id, key-material hash, bootstrap canonicalizer hash)`。bootstrap 内自带的 key、registry 或 verifier 不得自证；它们只能被已 pin 的外部根验签。provider registry 对每个 provider 冻结 provider ID、allowed attestation kind、key ID/material hash、有效期、revocation state 与可验 user/conversation/interaction identity 语义；key 未登记、过期、撤销、自签或 parser/verifier 不匹配都必须产生 `GR-AUTH-PROVENANCE`，不得降级为本地确认。

每个 Gate 的 `GateContractManifest/1` 必须在任何 acquisition、displayed action 或 confirmation 之前冻结，其顶层 key 必须**恰好**为 `schema_version`、`gate_contract_id`、`gate_id`、`gate_authority_bootstrap_path`、`gate_authority_bootstrap_sha256`、`trusted_interaction_provider_registry_sha256`、`trusted_interaction_provider_keyset_sha256`、`interaction_attestation_schema_sha256`、`interaction_attestation_parser_sha256`、`displayed_action_manifest_schema_sha256`、`displayed_action_manifest_parser_sha256`、`confirmation_artifact_schema_sha256`、`confirmation_artifact_parser_sha256`、`confirmation_verifier_sha256`、`trusted_principal_provider_registry_sha256`、`principal_attestation_verifier_sha256`、`gate_implementer_actual_principal_id`、`gate_implementer_principal_attestation_path`、`gate_implementer_principal_attestation_sha256`、`primary_operator_actual_principal_id`、`primary_operator_principal_attestation_path`、`primary_operator_principal_attestation_sha256`、`replay_operator_actual_principal_id`、`replay_operator_principal_attestation_path`、`replay_operator_principal_attestation_sha256`、`operator_role_history_manifest_path`、`operator_role_history_manifest_sha256`、`trusted_control_plane_registry_sha256`、`control_plane_attestation_verifier_sha256`、`acquisition_tool_allowlist_sha256`、`origin_allowlist_policy_sha256`、`predicate_registry_sha256`、`source_adapter_registry_sha256`、`provider_capability_policy_sha256`、`effect_identity_policy_sha256`、`effect_canonicalizer_sha256`、`active_effect_index_schema_sha256`、`reconciliation_policy_sha256`、`gate_policy_sha256`、`gate_evaluator_sha256`、`gate_receipt_schema_sha256`、`canonicalizer_sha256`、`frozen_at`。manifest 只能引用先前已被外部根验证的 bootstrap/attestations，不能选择新的 trust root。

Gate implementer、PRIMARY operator 与 INDEPENDENT_REPLAY operator 都必须有 bootstrap 中受信 principal provider 签发的 `PrincipalAttestationReceipt/1`，并且三个 `actual_principal_id` 必须两两不同。`operator_role_history_manifest` 必须覆盖三人对该 Gate/adapters/tools/evaluator 的实现、操作、审查和控制面管理历史；任一未知、自报或被政策禁止的重叠都使 Gate `BLOCKED/GR-IDENTITY`。acquisition tool 必须在 bootstrap 的 allowlist 中，其每次 receipt 必须由 registry 中的受信 control plane 签发并由预绑定 verifier 验证；本地自制 tool receipt 不可通过。

当前没有实际物化的外部预 pin `GateAuthorityBootstrap/1`、受信 interaction/principal/control-plane registry、provider key/trust root 或相应 attestation artifacts。因此当前不存在可验用户确认或 Gate PASS 路径；A/B/C/Payout 和所有外部动作保持 `BLOCKED`。

#### 两次新鲜 acquisition event

每个支持 Gate 谓词的源必须有两次**分别重新读取源站**的 acquisition event；第二次不是重新打开第一个本地 artifact。每次的 `AcquisitionEventReceipt/2` 顶层 key 必须**恰好**为：

```json
{
  "schema_version": "acquisition-event-receipt/2",
  "acquisition_event_id": "immutable-id",
  "gate_contract_manifest_sha256": "64-lowercase-hex",
  "phase": "PRIMARY|INDEPENDENT_REPLAY",
  "gate_id": "APPLICATION_RISK|CONTRACT_ACCEPTANCE|DELIVERY_START|PAYOUT_OR_WITHDRAWAL",
  "predicate_id": "one-frozen-gate-predicate",
  "target_identity": "exact-target-identity",
  "source_identity": "exact-platform-object-or-canonical-local-file-identity",
  "current_status": "one-frozen-status-enum",
  "observed_value": "exact-typed-value-from-the-frozen-predicate-schema",
  "requested_url": "exact-requested-url",
  "final_url": "same-exact-url",
  "redirect_chain": [],
  "allowed_origin": "canonical-scheme-host-port-origin",
  "artifact_kind": "RAW_RESPONSE|FULL_SCREENSHOT",
  "artifact_path": "event-unique-local-relative-path",
  "artifact_sha256": "64-lowercase-hex",
  "acquired_at": "RFC3339-with-offset",
  "acquisition_tool_id": "allowlisted-tool-id",
  "acquisition_tool_version": "exact-version",
  "acquisition_tool_receipt_path": "control-plane-receipt-path",
  "acquisition_tool_receipt_sha256": "64-lowercase-hex",
  "network_request_id": "tool-issued-request-id",
  "session_id": "tool-issued-session-id",
  "gate_implementer_actual_principal_id": "provider-attested-principal-id",
  "operator_actual_principal_id": "different-provider-attested-principal-id",
  "operator_principal_attestation_path": "control-plane-receipt-path",
  "operator_principal_attestation_sha256": "64-lowercase-hex",
  "operator_role": "PRIMARY_CAPTURER|INDEPENDENT_REPLAY_OPERATOR",
  "operator_role_history_manifest_sha256": "64-lowercase-hex",
  "acquisition_tool_allowlist_sha256": "64-lowercase-hex",
  "control_plane_attestation_path": "control-plane-receipt-path",
  "control_plane_attestation_sha256": "64-lowercase-hex",
  "control_plane_attestation_verifier_sha256": "64-lowercase-hex",
  "authentication_mode": "PUBLIC_UNAUTHENTICATED|PROVIDER_AUTHENTICATED",
  "authenticated_role_history": [],
  "source_adapter_id": "frozen-adapter-id",
  "source_adapter_sha256": "64-lowercase-hex"
}
```

`authenticated_role_history` 是源站账户角色的封闭数组；在 `PUBLIC_UNAUTHENTICATED` 下必须恰好为 `[]`。在未来另行授权的 `PROVIDER_AUTHENTICATED` 下，每个元素的 key 必须恰好为 `role_id`、`effective_from`、`effective_until_or_active`、`provider_role_receipt_sha256`，并由 acquisition tool receipt 证明；缺失、自报或无法核验的角色历史固定阻断。它不代替 Gate 实现者/operator 的 provider-attested `actual_principal_id` 和完整参与历史。当前文档不授权任何登录或认证读取。

当前 `no-redirect-v1` 是权威政策：evaluator 必须机械要求 `requested_url == final_url`、`redirect_chain == []`，且 requested/final URL 的 canonical origin 均逐字节等于 `allowed_origin`并存在冻结 origin allowlist 中。任何 HTTP/meta/JavaScript 重定向都阻断，即使两次稳定跳到同一终点也不例外。

PRIMARY 与 INDEPENDENT_REPLAY 必须分别有不同的 event ID、tool-issued request ID、session ID、provider-attested `operator_actual_principal_id`、artifact path 和 acquisition-tool receipt；两个 artifact 是两次独立取得的 bytes，即使内容相同而 SHA-256 相等，也不得复用同一文件或 tool receipt。evaluator 必须从受信 attestations 解析 actual principals，机械要求 `PRIMARY != INDEPENDENT_REPLAY != GATE_IMPLEMENTER != PRIMARY`；不同显示名、agent 名、request/session ID 或本地 UID 不能代替 actual-principal 分离。仅修改 timestamp、path 或 reviewer 字符串，但没有两个受信 control-plane 签发的 tool network/file-read receipts，结果固定 `BLOCKED/GR-IDENTITY`。

`GateEvidenceObject/2` 顶层 key 必须**恰好**为：

```json
{
  "schema_version": "gate-evidence-object/2",
  "evidence_id": "immutable-id",
  "gate_id": "APPLICATION_RISK|CONTRACT_ACCEPTANCE|DELIVERY_START|PAYOUT_OR_WITHDRAWAL",
  "predicate_id": "one-frozen-gate-predicate",
  "target_identity": "exact-target-identity",
  "source_identity": "exact-source-identity",
  "current_status": "one-frozen-status-enum",
  "observed_value": "exact-typed-value",
  "primary_acquisition_receipt_path": "local-relative-path",
  "primary_acquisition_receipt_sha256": "64-lowercase-hex",
  "replay_acquisition_receipt_path": "local-relative-path",
  "replay_acquisition_receipt_sha256": "64-lowercase-hex",
  "source_adapter_id": "frozen-adapter-id",
  "source_adapter_sha256": "64-lowercase-hex",
  "max_age": "exact-gate-constant",
  "expires_at": "primary-acquired-at-plus-exact-max-age"
}
```

A/B/C 的 `current_status` 封闭 enum 精确为 `LOCAL_ARTIFACT_PRESENT`、`OPEN_ACCEPTING_PROPOSALS`、`PAYMENT_METHOD_VERIFIED`、`CURRENT_FEE_RULES`、`OFFER_PENDING_ACCEPTANCE`、`MILESTONE_FULLY_FUNDED`、`HOURLY_PROTECTION_PREREQUISITES_MET`、`CONTRACT_ACTIVE`；Payout 专用状态在后文单独封闭。每个 Gate 的 content-addressed predicate registry 必须为每个 `predicate_id` 冻结 `observed_value` 的 JSON type、单位/币种、比较运算、accepted value/status、source-adapter hash 和 allowed-origin policy hash；未登记 predicate、额外/缺失 key、`unknown` 或依赖人工判断都没有可通过路径。

Gate evaluator 必须重算两个 acquisition receipts、tool receipts 和 artifacts 的哈希，然后机械确认两次的 target/source identity、status 和 canonical `observed_value` 完全相等。`primary.acquired_at <= replay.acquired_at <= decision_at`，`expires_at` 必须精确等于 primary acquired time 加该 Gate 的 max-age，replay 不得延长 expiry。一个 artifact 只能支持它实际覆盖的 predicate；跨页面、listing、offer、contract、account 或时间拼接都阻断。本地文件同样需要两次独立读取 receipt，requested/final URL 都必须是同一 canonical `file://` URL。

#### 封闭 `GateReceipt/3` 与前序 PASS

每次 Gate 决策都必须物化为下列 exact-key `GateReceipt/3`：

```json
{
  "schema_version": "gate-receipt/3",
  "receipt_id": "immutable-id",
  "gate_contract_manifest_path": "canonical-manifest-path",
  "gate_contract_manifest_sha256": "64-lowercase-hex",
  "gate_id": "APPLICATION_RISK|CONTRACT_ACCEPTANCE|DELIVERY_START|PAYOUT_OR_WITHDRAWAL",
  "result": "PASS|BLOCKED",
  "provider_identity": "exact-action-provider-identity",
  "account_identity": "exact-account-identity",
  "target_identity": "exact-target-identity",
  "payload_sha256": "64-lowercase-hex",
  "evidence_manifest_path": "canonical-manifest-path",
  "evidence_manifest_sha256": "64-lowercase-hex",
  "policy_sha256": "64-lowercase-hex",
  "predicate_registry_sha256": "64-lowercase-hex",
  "gate_evaluator_sha256": "64-lowercase-hex",
  "canonicalizer_sha256": "64-lowercase-hex",
  "gate_authority_bootstrap_path": "external-control-plane-path",
  "gate_authority_bootstrap_sha256": "64-lowercase-hex",
  "trusted_interaction_provider_registry_sha256": "64-lowercase-hex",
  "trusted_interaction_provider_keyset_sha256": "64-lowercase-hex",
  "interaction_attestation_schema_sha256": "64-lowercase-hex",
  "interaction_attestation_parser_sha256": "64-lowercase-hex",
  "confirmation_verifier_sha256": "64-lowercase-hex",
  "displayed_action_manifest_path_or_absent": "immutable-raw-artifact-path|ABSENT",
  "displayed_action_manifest_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "confirmation_artifact_path_or_absent": "immutable-raw-artifact-path|ABSENT",
  "confirmation_artifact_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "gate_implementer_actual_principal_id": "provider-attested-principal-id",
  "gate_implementer_principal_attestation_sha256": "64-lowercase-hex",
  "primary_operator_actual_principal_id": "different-provider-attested-principal-id",
  "primary_operator_principal_attestation_sha256": "64-lowercase-hex",
  "replay_operator_actual_principal_id": "different-provider-attested-principal-id",
  "replay_operator_principal_attestation_sha256": "64-lowercase-hex",
  "operator_role_history_manifest_sha256": "64-lowercase-hex",
  "acquisition_tool_allowlist_sha256": "64-lowercase-hex",
  "trusted_control_plane_registry_sha256": "64-lowercase-hex",
  "control_plane_attestation_verifier_sha256": "64-lowercase-hex",
  "effect_identity_sha256": "64-lowercase-hex",
  "provider_effect_key": "canonical-provider-effect-key",
  "effect_identity_policy_sha256": "64-lowercase-hex",
  "effect_canonicalizer_sha256": "64-lowercase-hex",
  "active_effect_guard_receipt_path": "content-addressed-receipt-path",
  "active_effect_guard_receipt_sha256": "64-lowercase-hex",
  "predecessor_pass": {
    "required": false,
    "gate_id": "GENESIS",
    "receipt_id": "GENESIS",
    "receipt_path": "GENESIS",
    "receipt_sha256": "GENESIS"
  },
  "user_authorization_receipt_path_or_absent": "local-relative-path|ABSENT",
  "user_authorization_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "decision_at": "RFC3339-with-offset",
  "valid_until": "minimum-evidence-and-authorization-expiry",
  "failure_codes": [],
  "claim_boundary": "exact-frozen-claim-boundary"
}
```

`predecessor_pass` 内部 key 也必须恰好如上。Gate A 的该对象必须精确为上述 `required=false/GENESIS`；Gate B 必须绑定同 account/listing 的 Gate A `PASS`；Gate C 必须绑定同 account/listing/offer/contract continuity 的 Gate B `PASS`；Payout 必须绑定同 account/contract 的 Gate C `PASS`。任何 predecessor receipt 的 `result != PASS`、哈希错误、身份断链或方向倒置都固定 `BLOCKED`。

`failure_codes` 是无重复、按 UTF-8 bytewise 升序的封闭集，唯一允许的值是 `GR-SCHEMA`、`GR-HASH`、`GR-ACQUISITION`、`GR-REDIRECT`、`GR-ORIGIN`、`GR-STALE`、`GR-IDENTITY`、`GR-PREDICATE`、`GR-PREDECESSOR`、`GR-POLICY`、`GR-AUTH-PROVENANCE`、`GR-AUTH-MISMATCH`、`GR-PROVIDER-CAPABILITY`、`GR-EFFECT-CONFLICT`、`GR-COST`、`GR-PAYLOAD`；精确 predicate ID 与原始不通过值保留在 evidence manifest，不通过动态扩展 failure-code enum。

`PASS` 要求 `failure_codes=[]`、存在下述有效 authorization receipt，并且 Gate manifest 中的所有外部 bootstrap/registry/keyset/schema/parser/verifier/principal/role-history/tool/control-plane 哈希与 receipt 精确相等；displayed-action 与 confirmation artifact 的 path/hash 全部必须存在且重算相等；policy、predicate registry、evaluator、canonicalizer、effect policy/canonicalizer 和 active-effect guard 均通过。任一信任根、interaction artifact 或其绑定缺失/不匹配都必须 `BLOCKED/GR-AUTH-PROVENANCE`；绝不允许由 receipt 内带的替代 verifier 补齐。`PASS.valid_until` 是所有 evidence expiry、bootstrap/provider-key/attestation expiry 与 authorization expiry 中的最早值。`BLOCKED` 要求至少一个封闭 failure code，authorization 可为 `ABSENT`，且 `valid_until` 必须精确等于 `decision_at`，表示 receipt 从产生时起就不存在可提交窗口。Gate `PASS` 只是一个到期前仍需要提交时再验的决策，不是动作已发生的证明。

#### 可验的精确用户确认

本地可填的 `user_identity`、自写 JSON、reviewer 转述、过去的通用“可以继续”或已登录状态都不是授权。展示与确认必须先各自保留不可变的原始 artifact：

- `DisplayedActionManifest/1` 顶层 key 必须恰好为 `schema_version`、`display_manifest_id`、`gate_contract_manifest_sha256`、`gate_id`、`authorized_action`、`provider_identity`、`account_identity`、`target_identity`、`payload_sha256`、`gate_evidence_manifest_sha256`、`effect_identity_sha256`、`provider_effect_key`、`operation_id`、`idempotency_key`、`cost_cap_by_currency_or_unit`、`privacy_and_data_actions`、`expires_at`、`rendering_canonicalizer_sha256`；`privacy_and_data_actions` 是封闭 canonical array，没有自由文本或隐含动作。
- `ConfirmationArtifact/1` 顶层 key 必须恰好为 `schema_version`、`confirmation_artifact_id`、`provider_identity`、`provider_key_id`、`conversation_identity`、`user_actor_identity`、`source_interaction_id`、`displayed_action_manifest_sha256`、`displayed_rendering_sha256`、`confirmation_decision`、`confirmation_content_sha256`、`raw_provider_payload_path`、`raw_provider_payload_sha256`、`observed_at`；`confirmation_decision` 唯一能授权的值是 `CONFIRM_EXACT_ACTION`，其他值或自由文本均不授权。

两份 artifact 的 canonical UTF-8 bytes 必须在 provider attestation 之前持久化且内容寻址；只保留摘要、message ID 或后来重构文本不合格。唯一可接受的 `UserConfirmationReceipt/2` 必须来自 `GateAuthorityBootstrap/1` 中预绑定且未撤销的 interaction provider/key，其顶层 key 恰好为：

```json
{
  "schema_version": "user-confirmation-receipt/2",
  "confirmation_id": "provider-issued-id",
  "provenance_kind": "PROVIDER_ATTESTED_USER_INTERACTION",
  "gate_authority_bootstrap_sha256": "64-lowercase-hex",
  "trusted_interaction_provider_registry_sha256": "64-lowercase-hex",
  "trusted_interaction_provider_keyset_sha256": "64-lowercase-hex",
  "interaction_attestation_schema_sha256": "64-lowercase-hex",
  "interaction_attestation_parser_sha256": "64-lowercase-hex",
  "provider_identity": "interaction-provider-id",
  "provider_key_id": "registry-allowed-unrevoked-key-id",
  "conversation_identity": "provider-issued-conversation-id",
  "user_actor_identity": "provider-verified-user-id",
  "source_interaction_id": "provider-issued-user-message-or-confirmation-id",
  "displayed_action_manifest_path": "immutable-raw-artifact-path",
  "displayed_action_manifest_sha256": "64-lowercase-hex",
  "confirmation_artifact_path": "immutable-raw-artifact-path",
  "confirmation_artifact_sha256": "64-lowercase-hex",
  "confirmed_at": "RFC3339-with-offset",
  "provider_attestation_path": "immutable-provider-receipt-path",
  "provider_attestation_sha256": "64-lowercase-hex",
  "confirmation_verifier_id": "frozen-verifier-id",
  "confirmation_verifier_sha256": "64-lowercase-hex",
  "verification_result": "VERIFIED"
}
```

verifier 必须从外部 bootstrap 加载预定的 registry/key/schema/parser 和固定 `confirmation_verifier_sha256`，重算两份 raw artifacts，验证 provider 签名实际覆盖它们的哈希、identities、decision 和 timestamp，然后才能得到 `VERIFIED`。receipt 内指向其他 verifier、实现者自建 provider/key、缺失 raw artifact、哈希不匹配或 attestation 没有覆盖全部字段，都必须 `GR-AUTH-PROVENANCE`。如果交互平台无法提供这种可验出处，则当前设计没有可提交路径，不得降级为本地字段。

`UserAuthorizationReceipt/3` 顶层 key 必须**恰好**为：

```json
{
  "schema_version": "user-authorization-receipt/3",
  "authorization_id": "immutable-id",
  "gate_contract_manifest_sha256": "64-lowercase-hex",
  "gate_id": "APPLICATION_RISK|CONTRACT_ACCEPTANCE|DELIVERY_START|PAYOUT_OR_WITHDRAWAL",
  "authorized_action": "SUBMIT_APPLICATION|ACCEPT_CONTRACT|START_DELIVERY|INITIATE_PAYOUT_OR_WITHDRAWAL",
  "user_identity": "same-provider-verified-user-id",
  "account_identity": "exact-account-identity",
  "target_identity": "exact-target-identity",
  "payload_sha256": "64-lowercase-hex",
  "gate_evidence_manifest_sha256": "64-lowercase-hex",
  "effect_identity_sha256": "64-lowercase-hex",
  "provider_effect_key": "canonical-provider-effect-key",
  "operation_id": "globally-unique-frozen-operation-id",
  "idempotency_key": "provider-supported-frozen-idempotency-key",
  "cost_cap_by_currency_or_unit": "canonical-closed-map",
  "confirmation_receipt_path": "local-relative-path",
  "confirmation_receipt_sha256": "64-lowercase-hex",
  "issued_at": "RFC3339-with-offset",
  "expires_at": "issued-at-plus-exact-auth-lifetime"
}
```

authorization 与其 confirmation 必须对 displayed action manifest 中的每一值完全一致，并满足 `primary.acquired_at <= replay.acquired_at <= confirmed_at <= issued_at <= decision_at < expires_at`。`expires_at` 必须精确等于 `issued_at +` 对应 `*_AUTH_MAX_LIFETIME`。一个 authorization 只绑定一个不变的 effect identity/provider effect key/operation/idempotency key；不得用同一 authorization 生成第二个 operation，也不得用新 authorization 绕过已存在的等价 effect。

#### provider-effect identity、跨授权唯一索引与 `ActionAttempt/2`

外部平台提交与本地记录无法原子提交，因此本设计不宣称 exactly-once。更换 authorization、operation ID 或 idempotency key 不能把同一外部效果变成新动作。每个 action provider 必须在 `GateContractManifest/1` 中冻结 provider-specific `effect-identity-policy`、semantic-projection schema、canonicalizer、provider-effect-key encoder 和 active-effect-index schema；平台若不提供对由该 effect identity 确定性导出的 exact idempotency key 的原生去重，或不能按 operation/idempotency/effect key 做权威对账，动作固定 `BLOCKED/GR-PROVIDER-CAPABILITY`。

`EffectIdentityRecord/1` 顶层 key 必须**恰好**为 `schema_version`、`provider_identity`、`authorized_action`、`account_identity`、`target_identity`、`semantic_payload_projection`、`semantic_payload_projection_schema_sha256`、`semantic_payload_projection_sha256`、`effect_identity_policy_sha256`、`effect_canonicalizer_sha256`、`effect_identity_sha256`、`provider_effect_key`、`provider_effect_key_encoder_sha256`。`effect_identity_sha256` 必须精确等于对 `(provider_identity, authorized_action, account_identity, target_identity, semantic_payload_projection, effect_identity_policy_sha256)` 按冻结 canonicalizer 序列化后的 SHA-256；`provider_effect_key` 必须由冻结 encoder 对该哈希确定性导出，idempotency key 必须是对应 provider-capability policy 承诺原生去重的该精确 key。

semantic projection 只能由 provider/action-specific 封闭 policy 从 payload 提取，必须忽略不改变外部效果的 nonce、timestamp、授权 ID、operation ID、排序或表达差异；必须包含区分真实语义效果的 provider object/version、作用域与数量。当前政策的最小语义边界是：application 绑定 account/listing 与 provider 允许的唯一 application slot；contract acceptance 绑定 exact offer/version；delivery start 绑定 exact contract/milestone-or-hour-scope version；payout 绑定 payable-transaction identity、destination、amount 与 currencies。任何人不得通过改动非语义字段制造新 identity；若新效果不能被已冻结 policy 机械区分，必须修订 policy 并创建新 Gate contract identity，不得运行时手工 override。

Gate 评估、ActionAttempt 初始 CAS 和 exact pre-submit boundary 都必须分别产生新鲜 `ActiveEffectGuardReceipt/1`；其顶层 key 必须**恰好**为 `schema_version`、`guard_receipt_id`、`guard_phase`、`gate_contract_manifest_sha256`、`provider_identity`、`account_identity`、`effect_identity_sha256`、`provider_effect_key`、`effect_identity_policy_sha256`、`effect_canonicalizer_sha256`、`active_effect_index_schema_sha256`、`index_version`、`index_root_sha256`、`matching_records_manifest_path`、`matching_records_manifest_sha256`、`unresolved_or_confirmed_count`、`authoritative_no_effect_terminal_count`、`admission_basis`、`evaluated_at`、`result`；`guard_phase` 的封闭值为 `GATE_EVALUATION|CAS_RESERVATION|PRE_SUBMIT`，`admission_basis` 的封闭值为 `NO_PRIOR_EFFECT|ALL_PRIOR_SAME_EFFECT_AUTHORITATIVE_NO_EFFECT|POLICY_DISTINCT_SEMANTIC_EFFECT|CONFLICT`，`result` 为 `CLEAR|CONFLICT`。`CLEAR` 只能在：没有先前同 key 记录；或所有同 key 记录都已是绑定权威 no-effect 证据的 `FAILED_FINAL`；或冻结 policy 机械证明该记录是不同 effect identity 时产生。任一 `UNUSED|RESERVED|SUBMITTED_UNKNOWN|CONFIRMED` 同效果记录、任一缺少权威 no-effect 证据的 terminal 记录或任一未知即 `CONFLICT/GR-EFFECT-CONFLICT`。

action-attempt store 必须是哈希链 append-only CAS store，同时强制：`attempt_id` 唯一；`(authorization_id, operation_id, idempotency_key)` 唯一；以及跨所有 authorization/operation/idempotency 的持久化 active-effect 部分唯一索引 `(provider_identity, account_identity, provider_effect_key) WHERE state IN (UNUSED, RESERVED, SUBMITTED_UNKNOWN, CONFIRMED)`。创建 `UNUSED` 记录的同一原子 CAS 必须重算 `CAS_RESERVATION` guard 并占有 active-effect key；一旦冲突，不创建记录、不消耗授权、不调用 provider。只有权威 `FAILED_FINAL/no-effect` 使旧记录离开 active index；`CONFIRMED` 永久阻断同 effect key，新语义效果必须自然得到 policy-distinct 的新 key。

每个动作意图从初始状态开始就物化 `ActionAttempt/2`，其顶层 key 必须**恰好**为：

```json
{
  "schema_version": "action-attempt/2",
  "attempt_id": "immutable-id",
  "authorization_id": "exact-authorization-id",
  "authorization_receipt_sha256": "64-lowercase-hex",
  "gate_receipt_id": "exact-pass-receipt-id",
  "gate_receipt_sha256": "64-lowercase-hex",
  "provider_identity": "exact-action-provider-identity",
  "effect_identity_sha256": "64-lowercase-hex",
  "provider_effect_key": "canonical-provider-effect-key",
  "effect_identity_policy_sha256": "64-lowercase-hex",
  "effect_canonicalizer_sha256": "64-lowercase-hex",
  "operation_id": "same-frozen-operation-id",
  "idempotency_key": "same-provider-supported-effect-derived-key",
  "authorized_action": "SUBMIT_APPLICATION|ACCEPT_CONTRACT|START_DELIVERY|INITIATE_PAYOUT_OR_WITHDRAWAL",
  "account_identity": "exact-account-identity",
  "target_identity": "exact-target-identity",
  "payload_sha256": "64-lowercase-hex",
  "state": "UNUSED|RESERVED|SUBMITTED_UNKNOWN|CONFIRMED|FAILED_FINAL",
  "state_version": 0,
  "previous_record_sha256_or_genesis": "64-lowercase-hex|GENESIS",
  "cas_effect_guard_receipt_path": "content-addressed-receipt-path",
  "cas_effect_guard_receipt_sha256": "64-lowercase-hex",
  "reservation_owner_or_absent": "restricted-submitter-id|ABSENT",
  "reserved_at_or_absent": "RFC3339-with-offset|ABSENT",
  "pre_submit_check_receipt_path_or_absent": "local-relative-path|ABSENT",
  "pre_submit_check_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "pre_submit_effect_guard_receipt_path_or_absent": "content-addressed-receipt-path|ABSENT",
  "pre_submit_effect_guard_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "submitted_at_or_absent": "RFC3339-with-offset|ABSENT",
  "provider_request_receipt_path_or_absent": "control-plane-receipt-path|ABSENT",
  "provider_request_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "provider_operation_id_or_absent": "provider-operation-id|ABSENT",
  "last_reconciliation_receipt_path_or_absent": "local-relative-path|ABSENT",
  "last_reconciliation_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "no_emission_receipt_path_or_absent": "control-plane-receipt-path|ABSENT",
  "no_emission_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "terminal_tag_or_absent": "closed-terminal-tag|ABSENT",
  "terminal_at_or_absent": "RFC3339-with-offset|ABSENT",
  "failure_code_or_absent": "closed-failure-code|ABSENT",
  "recorded_at": "RFC3339-with-offset"
}
```

创建后的每次转移，包括 `SUBMITTED_UNKNOWN -> SUBMITTED_UNKNOWN`，都必须是对 exact previous record hash 的持久化 CAS：`state_version` 恰好加 `1`，`previous_record_sha256_or_genesis` 重算等于紧邻前记录，新 record hash 追加到 store 的哈希链和 active-effect index root。CAS 不匹配、哈希断链、index root 不连续或已有其他同效果 active record 时，worker 必须退出且不得调用 provider。

字段可用性是封闭的：`UNUSED` 必须是 `state_version=0/GENESIS`，只有创建时 CAS guard 存在，其余 lifecycle 字段均 `ABSENT`；`RESERVED` 必须有 owner/reserved-at，pre-submit/submission/provider/reconciliation/no-emission/terminal/failure 均 `ABSENT`；`SUBMITTED_UNKNOWN` 必须有 owner/reserved/pre-submit check 与 `PRE_SUBMIT` effect guard，terminal/failure 均 `ABSENT`，submitted-at/provider-request path/hash 必须三者都存在，或在“CAS 已转 unknown 而 provider boundary 尚未生成 receipt”崩溃窗口中三者都为 `ABSENT`；每个 unknown 自转后 last-reconciliation path/hash 必须更新为该转移的新鲜 receipt。`CONFIRMED` 必须有 owner/reserved/pre-submit/submitted/provider-request/provider-operation/terminal，`terminal_tag=PROVIDER_ACCEPTED_EFFECT`、failure/no-emission `ABSENT`，并有权威接受回应或对账。

`FAILED_FINAL` 只有三类 tagged 字段组合：

1. `terminal_tag=PRE_SUBMIT_NO_EMISSION` / `AA-PRE-SUBMIT-CHECK-FAILED`：owner/reserved/pre-submit/terminal/no-emission 存在，submitted/provider/reconciliation 全部 `ABSENT`；
2. `terminal_tag=PRE_CALL_CRASH_AUTHORITATIVE_NO_EFFECT` / `AA-PRE-CALL-CRASH-AUTHORITATIVE-NO-EFFECT`：owner/reserved/pre-submit/terminal/no-emission 和 last-reconciliation 存在，但 `submitted_at`、provider-request path/hash 与 provider-operation 必须全部 `ABSENT`；no-emission receipt 证明本地控制面没有进入 provider call，而 reconciliation receipt 独立证明对 exact effect/operation/idempotency key 的权威终局 no-effect；
3. `terminal_tag=PROVIDER_FINAL_NO_EFFECT` 与 `AA-PROVIDER-REJECTED-FINAL|AA-PROVIDER-FAILED-FINAL|AA-RECONCILIATION-FINAL-NO-EFFECT`：owner/reserved/pre-submit/submitted/provider-request/terminal 存在，并有 provider round-trip 权威终局回应或 last-reconciliation。

`NoEmissionReceipt/1` 顶层 key 必须**恰好**为 `schema_version`、`receipt_id`、`attempt_id`、`effect_identity_sha256`、`provider_effect_key`、`operation_id`、`idempotency_key`、`restricted_submitter_principal_id`、`control_plane_trace_path`、`control_plane_trace_sha256`、`provider_boundary_entered`、`request_bytes_emitted`、`checked_interval_start`、`checked_interval_end`、`trusted_control_plane_registry_sha256`、`control_plane_attestation_verifier_sha256`、`result`；只有受信控制面对完整调用边界签发且验证出 `provider_boundary_entered=false`、`request_bytes_emitted=false`、`result=COMPLETE_NO_EMISSION` 才合格；submitter 自报日志或不完整时间窗口不合格。

受限 submitter 在把 bytes 交给 network/provider 的**精确提交点**重算 `GateReceipt/3`、`UserAuthorizationReceipt/3`、payload、bootstrap/registries/verifiers、policy/evaluator/canonicalizers、所有 evidence 哈希和新鲜 `PRE_SUBMIT` effect guard，并使用其受信控制面时钟机械要求 `decision_at <= reserved_at <= submitted_at < min(all evidence, authorization, bootstrap, provider-key and gate expiries)`。它在单次 provider call 前先通过 CAS 把 `RESERVED` 改为 `SUBMITTED_UNKNOWN`，然后在同一受限调用边界产生绑定 submitted-at、effect/operation/idempotency key 和 exact request-bytes hash 的 provider-request receipt。没有 receipt 不得声称已发出；CAS 后、provider call 前崩溃也不允许重试，只能进入 read-only reconciliation。

生命周期转移封闭为：

| from | to | 唯一允许的证据/含义 |
|---|---|---|
| `UNUSED` | `RESERVED` | 成功的持久化 CAS，同一 active-effect key 仍由本 attempt 唯一占有；没有 provider call |
| `RESERVED` | `FAILED_FINAL` | pre-submit 再验失败，完整 no-emission receipt，tag `PRE_SUBMIT_NO_EMISSION`；授权不得复用 |
| `RESERVED` | `SUBMITTED_UNKNOWN` | 提交时再验和 `PRE_SUBMIT` effect guard 都通过，单次 provider call 前先 CAS；不声称 provider 已接受 |
| `SUBMITTED_UNKNOWN` | `SUBMITTED_UNKNOWN` | 一份新鲜、不确定的 reconciliation receipt；对紧邻前记录的 CAS、`state_version+1`、previous hash 与 index-root 哈希链全部通过；不重试 |
| `SUBMITTED_UNKNOWN` | `CONFIRMED` | provider 权威回应或对 exact effect/operation/idempotency key 的对账确认该 action request 已被接受 |
| `SUBMITTED_UNKNOWN` | `FAILED_FINAL` | provider 给出权威终局 no-effect；正常已提交分支使用 `PROVIDER_FINAL_NO_EFFECT`，pre-call-crash 且 submitted/provider-request 均 `ABSENT` 的分支必须使用独立 tag `PRE_CALL_CRASH_AUTHORITATIVE_NO_EFFECT` 并同时绑定 complete no-emission 和 reconciliation 证据 |

`CONFIRMED` 只表示 provider 权威确认该**动作请求**被接受，不表示申请获得工作、合同或资金最终结算。`CONFIRMED` 和 `FAILED_FINAL` 是 attempt 终局状态，不得回到 `UNUSED/RESERVED`。`SUBMITTED_UNKNOWN` 禁止自动重试、新建等价 operation、更换 idempotency key 或新建 authorization；只能用 exact effect/operation/idempotency key 做 read-only reconciliation。对账仍不确定时必须经过上述哈希链/CAS 自转保持 `SUBMITTED_UNKNOWN`，并由 active-effect index 阻断任何同效果动作。

`ProviderReconciliationReceipt/2` 顶层 key 必须**恰好**为 `schema_version`、`reconciliation_id`、`attempt_id`、`provider_identity`、`account_identity`、`effect_identity_sha256`、`provider_effect_key`、`operation_id`、`idempotency_key`、`provider_operation_id_or_absent`、`query_scope`、`queried_at`、`query_tool_receipt_path`、`query_tool_receipt_sha256`、`provider_status`、`mapped_attempt_state`、`raw_artifact_path`、`raw_artifact_sha256`、`reconciler_principal_attestation_sha256`、`provider_finality_policy_sha256`、`provider_retention_window_evidence_sha256`、`reconciliation_policy_sha256`；`query_scope` 必须精确覆盖 effect/operation/idempotency 三种 identity，`provider_status` 封闭为 `ACCEPTED`、`PENDING`、`COMPLETED`、`REJECTED_FINAL`、`FAILED_FINAL`、`NO_EFFECT_AUTHORITATIVE`、`REVERSED`、`NOT_FOUND_NONTERMINAL`、`UNKNOWN`。只有冻结 policy 能映射状态；`NOT_FOUND_NONTERMINAL|UNKNOWN|PENDING` 只能映射 `SUBMITTED_UNKNOWN`。`NO_EFFECT_AUTHORITATIVE` 只能在 provider 的官方终局语义、完整查询范围和保留/最终一致窗口证据都机械满足时产生；普通“未找到”绝不是权威 no-effect。

### Gate A：Application Risk

申请提交动作出现之前，receipt 必须把下列每个事实绑定到合格的 `GateEvidenceObject/2`，并对同一 account/listing identity 取逻辑 `AND`：

1. **listing 当前性**：source identity 精确等于目标 listing，`current_status=OPEN_ACCEPTING_PROPOSALS`；raw 或完整截图、requested/final URL、当前捕获和独立 replay 全部一致；
2. **客户付款与历史**：`current_status=PAYMENT_METHOD_VERIFIED`，并从同一客户身份的 artifact 提取 spend、hires、hire rate；本保守政策要求三者均有精确源值且大于零。零不证明欺诈，但在修改政策前仍阻断；
3. **实时活动**：proposals、interviews、hires 都有精确非空源值并与同一 listing 的两个 acquisition events 和 hashes 绑定；
4. **决策时成本**：保存 `current_status=CURRENT_FEE_RULES` 的 fee schedule/rules raw 或完整截图及 SHA-256，冻结规则版本/hash；逐项列出提交当下 Connects、boost、平台申请侧费用和其他立即发生费用的 exact quantity、unit、amount 与 currency，并按 unit/currency 机械求和。对规则允许波动但仍属于本次申请的费用，取冻结 proposal 设置下可计算的最高值作为 `conservative_application_cost_cap`；没有有限上界、币种/单位不一致或任何费用未知即阻断。任何正成本都必须被 exact authorization 的同币种/单位 cap 覆盖；
5. **诈骗红旗**：`free_test_requested=false`、`off_platform_contact_requested=false`、`off_platform_contract_requested=false`、`off_platform_delivery_requested=false`、`off_platform_payment_requested=false`、`equipment_or_credential_purchase_requested=false`、`private_information_requested_pre_contract=false`；
6. **真实 fit**：每个 mandatory requirement 都引用一个 `current_status=LOCAL_ARTIFACT_PRESENT` 的本地证据对象；合成测试不得充当真实 trace、Postgres、生产部署或专家履历，任何 gap 都失败；
7. **申请载荷**：proposal 原文、报价、工期、支持边界、附件、公开范围和不可声称项形成一个 frozen local artifact；其 SHA-256 必须等于 authorization 的 `payload_sha256`；
8. **provider 提交能力**：冻结 predicate 必须从 provider 当前官方接口/行为证明 effect-derived exact idempotency key 可原生去重，effect/operation/idempotency 三种 identity 可权威对账；任一不可用就阻断；
9. **精确用户授权**：只覆盖该 account、listing、proposal hash、价格/成本 cap、一个 effect identity/provider-effect key/operation/idempotency key 与一次 `SUBMIT_APPLICATION`，并有可验 `UserConfirmationReceipt/2`；
10. **决策与提交**：先产生 exact `GateReceipt/3`；只有 `PASS` 才能进入 `ActionAttempt/2` active-effect CAS 预留，且必须在 exact provider-call boundary 再验时效和同效果冲突。

Gate A 不再要求申请时猜测未来实际成交后的 realized FX 或提款费用。它只绑定决策时可见的 fee schedule/rules hash、当前 exact application-side costs 与保守申请成本 cap。未来实际汇率、提款/划转费用和到账净额全部固定为 `DEFERRED_TO_PAYOUT_OR_WITHDRAWAL_GATE`，不能填估计值冒充精确成本，也不能因为被 defer 就自动获准。

不允许 reviewer、agent、平台登录态或过去的通用授权自批。当前所有历史 listing 都缺少上述两次新鲜 acquisition receipts、可验用户确认、provider idempotency/reconciliation 证据和 typed Gate/Action receipts，因此结果保持 `APPLICATION_RISK=BLOCKED`，当前 `ActionAttempt` 数量为 `0`。

### Gate B：Contract Acceptance

只有平台内 exact offer 出现后才能评估。点击接受前，下列事实必须各自由未过期、身份一致的 `GateEvidenceObject/2` 支持：

- 绑定同 account/listing 的 Gate A exact `GateReceipt/3`，其 result 必须是 `PASS`；历史“曾申请”或无哈希的平台状态不是前序 receipt；
- source identity 精确绑定 account、client、listing、offer/contract，offer 的 `current_status=OFFER_PENDING_ACCEPTANCE`；两次新鲜 acquisition 的原始 offer、requested/final URL、source identity、status 与 observed value 全部一致；
- 当前客户付款状态为 `PAYMENT_METHOD_VERIFIED`；
- scope、deliverables、acceptance criteria、价格、工期、revision 次数、支持期、IP、取消/争议、隐私、数据保留和责任边界全部非空、来自同一 offer identity，并与 frozen acceptance payload 一致；
- 接受前风险对象必须精确满足 `off_platform_contact=false`、`off_platform_contract=false`、`off_platform_delivery=false`、`off_platform_payment=false`、`free_test=false`；任一 true/unknown/缺失都阻断；
- 固定价 offer：首个 exact milestone 与 scope 一致，当前状态为 `MILESTONE_FULLY_FUNDED`；
- 小时制 offer：当前状态为 `HOURLY_PROTECTION_PREREQUISITES_MET`，并有 exact hourly rate、正的 weekly limit、允许 Desktop time tracker、禁止依赖 manual time，以及 verified billing、身份验证与账户 good standing 的逐源证据；
- 账户、税务、身份、合同或私有数据动作逐项写入 frozen acceptance payload，未列动作不获授权；
- provider 必须对 exact acceptance effect-derived idempotency key 提供原生去重，并可按 effect/operation/idempotency 三种 identity 权威对账；
- exact authorization 只覆盖该 account、offer identity、acceptance payload hash、cost cap、一个 effect identity/provider-effect key/operation/idempotency key 与一次 `ACCEPT_CONTRACT`，并有可验用户确认；
- Gate B 必须产生绑定 Gate A predecessor PASS 的 exact `GateReceipt/3`；只有该 receipt `PASS` 才能进入对应 `ActionAttempt/2` active-effect CAS 预留和提交时再验。

合同内容、source identity、付款状态、milestone funding 或小时保护前置条件任何变化都会使旧 evidence/receipt 失效；结果固定为 `CONTRACT_ACCEPTANCE=BLOCKED`。申请授权不能替代合同接受授权，Gate B 也不授权开始工作、交付或任何资金动作。

### Gate C：Delivery Start

在写第一行客户代码、做测试、参加试做、读取数据或进入客户环境前，下列事实必须各自由未过期、身份一致的 `GateEvidenceObject/2` 支持：

- 绑定同 account/listing/offer/contract continuity 的 Gate B exact `GateReceipt/3`，其 result 必须是 `PASS`；
- 平台内 exact contract 的 `current_status=CONTRACT_ACTIVE`，明确非 paused/ended，且 contract/offer/listing/account identity 与 Gate B receipt 完全一致；
- 客户当前状态仍为 `PAYMENT_METHOD_VERIFIED`；
- 固定价：当前 exact milestone 的状态仍为 `MILESTONE_FULLY_FUNDED`，工作范围与 milestone artifact 精确一致；
- 小时制：当前状态仍为 `HOURLY_PROTECTION_PREREQUISITES_MET`，并重新证明 verified billing、weekly limit、Desktop time tracker、work diary memo/activity 与账户状态；manual time 和超限时间固定禁止；
- 获准的 scope、repo/environment、数据分类、最小必要 credentials、允许网络目标、保存位置、保留期和删除/撤销方式都在 frozen start payload 中有精确值；未授权数据和凭据不得读取；
- 开始前风险对象必须同时满足 `free_test=false`、`free_work=false`、`off_platform_contact=false`、`off_platform_contract=false`、`off_platform_delivery=false`、`off_platform_payment=false`；任一 true/unknown/缺失都阻断；
- provider 必须对 exact delivery-start effect-derived idempotency key 提供原生去重，并可按 effect/operation/idempotency 三种 identity 权威对账；如果“开始交付”不是 provider 可去重且可对账的单一动作，就必须按实际副作用拆成另外的精确 Gate，不得用抽象标签跳过；
- exact authorization 只覆盖该 account、contract、milestone/小时范围、environment、data manifest、start payload hash、一个 effect identity/provider-effect key/operation/idempotency key 与一次 `START_DELIVERY`，并有可验用户确认；
- Gate C 必须产生绑定 Gate B predecessor PASS 的 exact `GateReceipt/3`；只有该 receipt `PASS` 才能进入对应 `ActionAttempt/2` active-effect CAS 预留和 exact action boundary 再验。

合同 paused/ended、付款验证失效、milestone funding 失效、scope/environment/data 扩大、保护条件变化或证据/授权过期时，立即停止，结果变为 `DELIVERY_START=BLOCKED`。不得用“先做一点”“小测试”或“客户说之后会付”绕过。

### 独立的 `PAYOUT_OR_WITHDRAWAL` Gate

Gate A、B、C 即使未来通过，也不授权收款、发起 payout/withdrawal、向客户收费、付款或退款。未来若需要把平台余额划到外部目的地，必须另建 `PAYOUT_OR_WITHDRAWAL` Gate，并且必须绑定同 account/contract 的 Gate C `PASS`；Gate C 通过不能代替该新决策。

Payout 证据的 `current_status` 封闭 enum 为 `PAYOUT_BALANCE_AVAILABLE`、`PAYOUT_DESTINATION_VERIFIED`、`PAYOUT_FEE_RULES_CURRENT`、`PAYOUT_RATE_CURRENT`、`PAYOUT_TRANSACTION_PENDING`、`PAYOUT_TRANSACTION_SETTLED`、`PAYOUT_TRANSACTION_FAILED`、`PAYOUT_TRANSACTION_REVERSED`；未知值不是 enum 中的通过值。每个 pre/post 状态都必须使用前文两次新鲜 `AcquisitionEventReceipt/2`，并使用 `PAYOUT_EVIDENCE_MAX_AGE`。

#### `PayoutPreActionReceipt/2`

发起 payout 前的 exact-key receipt 为：

```json
{
  "schema_version": "payout-pre-action-receipt/2",
  "receipt_id": "immutable-id",
  "result": "PASS|BLOCKED",
  "payout_gate_receipt_id": "exact-gate-receipt-id",
  "payout_gate_receipt_sha256": "64-lowercase-hex",
  "delivery_predecessor_receipt_id": "exact-gate-c-pass-id",
  "delivery_predecessor_receipt_sha256": "64-lowercase-hex",
  "authorization_id_or_absent": "exact-authorization-id|ABSENT",
  "authorization_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "provider_identity": "exact-action-provider-identity",
  "effect_identity_sha256": "64-lowercase-hex",
  "provider_effect_key": "canonical-provider-effect-key",
  "effect_identity_policy_sha256": "64-lowercase-hex",
  "effect_canonicalizer_sha256": "64-lowercase-hex",
  "active_effect_guard_receipt_path": "content-addressed-receipt-path",
  "active_effect_guard_receipt_sha256": "64-lowercase-hex",
  "operation_id": "globally-unique-frozen-operation-id",
  "idempotency_key": "provider-supported-effect-derived-idempotency-key",
  "platform_account_identity": "exact-account-identity",
  "contract_identity": "exact-contract-identity",
  "balance_identity": "exact-balance-ledger-identity",
  "payable_transaction_identity": "exact-payable-transaction-identity",
  "destination_account_identity": "exact-nonsecret-destination-identity",
  "source_currency": "exact-iso-currency",
  "destination_currency": "exact-iso-currency",
  "gross_amount": "canonical-decimal-string",
  "rate": "canonical-decimal-string",
  "rate_source_timestamp": "RFC3339-with-offset",
  "fixed_fee": "canonical-decimal-string",
  "variable_fee": "canonical-decimal-string",
  "total_fee": "canonical-decimal-string",
  "net_amount": "canonical-decimal-string",
  "conservative_total_fee_cap": "canonical-decimal-string-and-currency",
  "transaction_payload_sha256": "64-lowercase-hex",
  "evidence_manifest_path": "canonical-manifest-path",
  "evidence_manifest_sha256": "64-lowercase-hex",
  "policy_sha256": "64-lowercase-hex",
  "predicate_registry_sha256": "64-lowercase-hex",
  "gate_evaluator_sha256": "64-lowercase-hex",
  "canonicalizer_sha256": "64-lowercase-hex",
  "decision_at": "RFC3339-with-offset",
  "valid_until": "minimum-evidence-and-authorization-expiry",
  "failure_codes": []
}
```

Payout pre-action `failure_codes` 是无重复、UTF-8 bytewise 升序的封闭集，唯一允许的值是前文所有 `GR-*` 代码以及 `PO-BALANCE`、`PO-TRANSACTION`、`PO-DESTINATION`、`PO-RATE`、`PO-FEE`、`PO-CALCULATION`、`PO-IDEMPOTENCY`。`PASS` 要求 `failure_codes=[]`；`BLOCKED` 要求至少一项且 `valid_until=decision_at`。

`PASS` 必须机械证明 exact balance identity、payable transaction identity、destination identity、source/destination currency、fee schedule、rate source、gross/rate/fixed fee/variable fee/total fee/net amount 和有限 fee cap，且所有算术按冻结 decimal/currency policy 重算一致。它还必须绑定 exact `UserAuthorizationReceipt/3`、provider-native effect-derived idempotency/reconciliation capability、`ActiveEffectGuardReceipt/1 CLEAR` 和 `PAYOUT_OR_WITHDRAWAL` `GateReceipt/3 PASS`。任一值未知、无有限上界、证据过期、身份不一致或存在额外 key 都固定 `BLOCKED`。只记录稳定身份与哈希，不读取或保存秘密凭据。

只有 pre-action `PASS` 才能创建绑定同 effect/operation/idempotency key 的 `ActionAttempt/2`。payout submitter 与其他动作一样必须占有 active-effect 唯一索引并 CAS 预留，并在 exact provider-call boundary 产生 provider request receipt；`submitted_at` 必须严格早于 pre-action `valid_until`、所有 evidence expiry 和 authorization expiry。

#### `PayoutPostActionReceipt/2` 与恢复规则

任何 payout call 进入 `SUBMITTED_UNKNOWN` 后，必须建立下列 exact-key post-action receipt 链：

```json
{
  "schema_version": "payout-post-action-receipt/2",
  "receipt_id": "immutable-id",
  "pre_action_receipt_id": "exact-pre-action-receipt-id",
  "pre_action_receipt_sha256": "64-lowercase-hex",
  "action_attempt_id": "exact-attempt-id",
  "action_attempt_record_sha256": "64-lowercase-hex",
  "provider_identity": "same-exact-action-provider-identity",
  "effect_identity_sha256": "same-64-lowercase-hex",
  "provider_effect_key": "same-canonical-provider-effect-key",
  "operation_id": "same-frozen-operation-id",
  "idempotency_key": "same-provider-supported-idempotency-key",
  "provider_operation_id_or_absent": "provider-operation-id|ABSENT",
  "provider_transaction_identity_or_absent": "exact-transaction-id|ABSENT",
  "status": "PENDING|UNKNOWN|SETTLED|FAILED|REVERSED",
  "primary_status_acquisition_receipt_path_or_absent": "local-relative-path|ABSENT",
  "primary_status_acquisition_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "replay_status_acquisition_receipt_path_or_absent": "local-relative-path|ABSENT",
  "replay_status_acquisition_receipt_sha256_or_absent": "64-lowercase-hex|ABSENT",
  "gross_amount": "same-canonical-decimal-string",
  "total_fee_or_unknown": "canonical-decimal-string|UNKNOWN",
  "net_amount_or_unknown": "canonical-decimal-string|UNKNOWN",
  "source_currency": "same-exact-iso-currency",
  "destination_currency": "same-exact-iso-currency",
  "previous_post_receipt_sha256_or_genesis": "64-lowercase-hex|GENESIS",
  "reconciliation_receipt_path": "local-relative-path",
  "reconciliation_receipt_sha256": "64-lowercase-hex",
  "reconciliation_policy_sha256": "64-lowercase-hex",
  "observed_at": "RFC3339-with-offset",
  "recorded_at": "RFC3339-with-offset",
  "claim_boundary": "exact-status-specific-claim-boundary"
}
```

`UNKNOWN` 表示 request 可能已到达，但没有权威 provider 结果；两个 status acquisition refs 可为 `ABSENT`，但 reconciliation receipt 必须记录为何仍未知。`PENDING|SETTLED|FAILED|REVERSED` 必须绑定两次新鲜、身份相同的 status acquisitions，不得从本地意图或单次页面自证。

状态转移封闭为：

- `GENESIS -> UNKNOWN|PENDING|SETTLED|FAILED|REVERSED`，第一份 receipt 的 `previous_post_receipt_sha256_or_genesis` 必须精确为 `GENESIS`，之后每份 receipt 都必须绑定紧邻前记录哈希；
- `UNKNOWN -> UNKNOWN|PENDING|SETTLED|FAILED|REVERSED`，只能由对 exact operation/idempotency key 的 read-only provider reconciliation 驱动；
- `PENDING -> PENDING|SETTLED|FAILED|REVERSED`，必须继续对账，不能声称到账；
- `SETTLED -> SETTLED|REVERSED`；`SETTLED` 只证明被绑定 provider ledger 的权威状态，除非另有获授权的 destination-credit 证据，不得扩大为外部账户可用现金；
- `FAILED -> FAILED`；必须是 provider 的终局失败，若仍可能延迟生效则必须保持 `UNKNOWN`；
- `REVERSED -> REVERSED`；任何后来的逆转会使旧的 settled/cash-flow 表述失效，并触发独立异常审查。

Payout post status 与 ActionAttempt 的映射是封闭的：`UNKNOWN -> SUBMITTED_UNKNOWN`；`PENDING|SETTLED|REVERSED -> CONFIRMED`，其中 `CONFIRMED` 只表示原 payout instruction 已被 provider 识别，不会把 pending/reversed 说成到账；`FAILED -> FAILED_FINAL`。若后续从 `SETTLED` 变为 `REVERSED`，ActionAttempt 保留历史 `CONFIRMED`，而最新 post receipt 才是资金状态的权威记录。不得倒写 ActionAttempt 历史来隐藏 reversal。

原 operation 在 `UNKNOWN|PENDING|SETTLED|FAILED|REVERSED` 任一 post status 下都禁止自动重试；即使 `FAILED` 后用户想重试，也必须先完成 provider 终局对账，然后建立全新 operation/idempotency key、新鲜证据、新 GateReceipt 和新用户确认。没有 post-action receipt 不能声称 payout 成功、失败或到账。

该 Gate 只覆盖精确 payout/withdrawal，不覆盖向客户收款、客户 payment、购买、退款或其他资金动作；这些动作当前仍是 `UNAUTHORIZED`，需要各自的新 Gate 和精确用户授权。当前没有 payout/withdrawal 证据、授权、GateReceipt、ActionAttempt 或 pre/post receipt，因此固定 `PAYOUT_OR_WITHDRAWAL=BLOCKED`。

## Gate 的官方风险依据

这些来源只说明平台规则和风险控制，不保证任何职位、客户或付款：

- Upwork 官方诈骗说明把站外沟通/付款和免费工作请求列为红旗，并建议沟通与付款留在平台：<https://support.upwork.com/hc/en-us/articles/35088484250003-Recognize-red-flags-and-avoid-scams>；
- Upwork 对未验证付款方式的说明指出，付款验证完成前平台不能处理付款，且未验证付款方式的客户不提供 Hourly Protection：<https://support.upwork.com/hc/en-us/articles/211063098-What-does-unverified-billing-method-mean-on-Upwork>；
- 固定价保护要求在开工前 milestone 已足额 funded，且工作和提交方式与 milestone 一致：<https://support.upwork.com/hc/en-us/articles/211063748-How-Fixed-Price-Payment-Protection-works-for-freelancers-on-Upwork>；
- 小时保护要求小时合同、Desktop App time tracker、相关截图与 memo、合理活动、weekly limit、身份验证、客户 verified billing 和账户 good standing：<https://support.upwork.com/hc/en-us/articles/211068288-How-Hourly-Payment-Protection-works-for-freelancers>。

## 仍然成立的外部权限边界

在两次新鲜逐 listing 原始证据、必要的前序 Gate PASS、可验用户确认、provider idempotency/reconciliation 证据、对应 typed receipt 与 exact action-boundary 再验都形成前：

- 不登录或创建平台/云账户；
- 不创建或修改 profile；
- 不提交申请、不 boost、不消耗 Connects；
- 不私信、不站外联系、不发附件；
- 不接受合同、不承诺价格/工期/支持；
- 不做免费测试，不在 milestone 入金或小时保护条件成立前工作；
- 不读取客户数据、凭据、账户或环境；
- 不调用远程模型、不上传 trace、不发布作品；
- 不付款、不收款、不产生外部成本。

## 剩余不确定性

- r1、r2、r3 和 r4 都已被精确独立审查拒绝；当前 r5 仍只是未独立审查的纸面修复候选。registry、matcher、evaluator、fixtures、canonicalizer、receipt verifiers、外部 trusted bootstraps/principal attestations、reviewer-controlled withheld store、restricted runner、Gate adapters、active-effect CAS store 和 typed receipts 都尚未按本契约物化或产生内容哈希，因此没有 development/withheld 实验通过或任何 Gate 通过；
- 当前无 `UserConfirmationReceipt`、`UserAuthorizationReceipt`、`GateReceipt`、`ActionAttempt`、provider request/reconciliation receipt 或 payout pre/post receipt；零申请候选、零外部 action attempt 和全部外部动作未授权状态保持不变；
- 当前尚未证明任何目标 provider 能够对 application、contract acceptance、delivery start 或 payout 提供本政策要求的原生 idempotency 与 operation reconciliation；在证明前这些动作没有可提交路径；
- Gate max-age 是预设的保守政策窗口，不是平台状态稳定性的经验保证；窗口内仍可能变化，任何观察到的变化都会立即使旧证据失效；
- 历史职位说明不了当前市场规模、成交率或报酬分布；
- 本地合成测试可能证明 Harness 设计能力，却不能证明真实数据、生产分布和客户沟通能力；
- 严格客户历史门会错过一些真实的新客户，这是当前保守风险政策有意接受的假阴性；
- 平台功能会继续覆盖通用 evaluation 配置，长期价值仍须通过客户特有语义、验收责任、失败回灌与持续运行的真实付费结果验证；
- 即使未来某个申请、合同和交付 Gate 都通过，也只证明那一次动作满足预设条件，不证明稳定现金流或可复用资产已经形成。
