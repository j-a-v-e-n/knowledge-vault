# PB010 两工作流只读刷新与排序输入

状态：`READ_ONLY_REFRESH_COMPLETE / NO_WORKFLOW_AUTHORITY`

本记录完成 `18-PB010最小组合基线.md` 要求的同口径只读刷新。它帮助做跨项目取舍，但不替任何 workflow 签发 PASS，也不授权写入、Shadow、外联、交易或投资。

## 新鲜事实

### 机会到交易

- Milestone A2 最小控制面已通过，但权限效果只到 `CONTROL_PLANE_ACCEPTED_ONLY`；C8、Shadow 与外部动作仍是 false。
- D022 选择了比完整 C8 修复更短的 Observation → Interpretation → Hypothesis baseline。
- exact synthetic predecessor `a1cb1a7699acd8eff59d4eaf0dfad9fb5edb4bc9` 的独立结论为 `FINAL FAIL`：`Critical 0 / Major 1 / Minor 1`。它把 synthetic fixture 表示为 public quotation，且错误 enum 类型可触发未捕获异常。
- successor `faf2ec7e9e7b4a8de729da08eceea8192f3c9c39` 已闭合这两个 finding，但 fresh review 仍为 `FINAL FAIL`：`Critical 0 / Major 1 / Minor 0`。唯一 Major 是 `fixture://`、`fixture://   ` 与 `fixture:///` 没有可定位目标仍返回 `valid=true`。
- 第二 successor `bf8969418faa77898ff3c0ad12a98394b863ad40` 已闭合空目标 locator，并获 fresh `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`。独立矩阵为 `58` 项攻击、`58` 项按预期拒绝、`0` unexpected accept、`0` crash；另一本任务完整入口矩阵为 `96/96` 符合预期。
- 该 PASS 只接受 exact synthetic structural baseline，权限效果仍为 `NONE`。下一阶段必须另行设计并审查 real-public-input fixture；本记录不授权它自动开始。
- public-input Gate candidate `7c99d2ab310d71c61bdf00c4aab68cc07b2e398b` 随后获 fresh `FINAL FAIL`：`Critical 0 / Major 3 / Minor 0`。父提交中的先验字节、硬哈希与双改拒绝均正确；三个 blocker 是 public-host classifier 会接受非公共 locator、严格 RFC3339 会接受 `24:00:00Z`、以及无效 CLI 结果仍 exit `0`。
- 当前没有持久正向 `PUBLIC_OBSERVATION` fixture，这符合“Gate PASS 前不放入真实材料”的阶段规则，不是第四个 finding；不得用 synthetic 内容冒充公共引用填补它。
- exact successor `71b50434051c7ad46fbfc9596914c045e2ecfd33` 已关闭上述三个 blocker，并获 fresh `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`。独立 reviewer 共做 `194` 项检查，`0` unexpected accept、`0` crash；portfolio 交叉矩阵为 `554` 项，其中 `547` 项预期拒绝、`7` 项受控正例，仍为 `0` unexpected accept、`0` crash。`17/17` 个 predecessor host 向量均在完整 `PUBLIC_OBSERVATION` 路径拒绝，`24:00:00Z` 拒绝，CLI 对 invalid input 非零退出。
- 该 PASS 的权限效果只到 `EXACT_OFFLINE_GATE_REVIEW_PASS_ONLY`：它允许机会线在**后续 immutable successor** 中让此 Gate 评估第一条真正捕获的只读公开记录，但本身没有接纳任何真实记录，也没有生成需求、购买意愿或商业证据。
- 机会 owner 随后先在 parent commit `149886e87eaa1a93e8b1436a6af8911d302a2ab6` 固定 source universe、eligibility 与 stopping rule，再在 successor `d06221014731d0223579c06c28fc20f77053a914` 捕获 GitHub issue `SillyTavern/SillyTavern#1400`。record-level fresh review 为 `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`，只接纳 exact record SHA-256 `a1c72f95f08da9ed84c3ef04e41497312c0a0c587501290906ff8124200f0ee9` 为 `OBSERVATION_ONLY`。
- 原文核对确认保存的是 `13` 个英文单词的 verbatim minimal quotation；没有保存作者身份、不必要个人数据或长篇 source copy。`WORKAROUND` 与原文上下文一致；解释仍明确把 prevalence、buyer identity、willingness to pay、persistence today、feasibility 与 transaction path 保持为 unknown，hypothesis 仍是 `OPPORTUNITY_HYPOTHESIS_UNVALIDATED`，`commercial_claim=false`。
- selection trail 只被接受为过去时、受限的 operator report；它不证明固定 ordinal rank、搜索结果完整性或未来可复现性。current/future search ranking drift 继续是 nonclaim。
- discovery batch v1 exact candidate `0d41f83ff0c6418bc2fff3fec31edbf038dc4f71` 的结构验证仍为 `valid=true / OBSERVATION_ONLY / OPPORTUNITY_HYPOTHESIS_UNVALIDATED / external_authority=false`，`15` 项单元测试通过；四条最小引用均为 verbatim，分别为 `15 / 21 / 14 / 18` 个英文单词，hash 全部匹配。
- 但 batch-level fresh review 为 `FINAL FAIL / NO-GO`：`Critical 0 / Major 2 / Minor 0`。第一项 Major 是 Reddit 选样污染：r/smallbusiness 是自述嵌入 pain-point market-research prompt，r/Entrepreneur 是自述嵌入自建工具/result story；结果记录却声称已排除这些来源类型。第二项 Major 是语义过度合并：inbound lead follow-up/scheduling/missed-lead management 与 outbound prospect research/personalization/cold-email preparation 只共享宽泛的 customer-acquisition + manual-work 主题，不满足预声明的 specific actor/context/current-state/desired-outcome 合并门。
- 最小修订不是删除所有原始材料：隔离两条污染 Reddit selection，不把它们用于 repeated-signal；qBittorrent 与 Gradle 两条干净 observation 继续保持互相分离；删除 `HYP-BATCH-OUTREACH-CONTINUITY-001` advisory merge，并记录 `NO_REPEATED_PATTERN_IN_BATCH`。本轮没有 repeated signal、需求、产品或商业权限。
- interpretation-only successor `956286dfd4731462f154d97ff4ecceb0d8657936` 已关闭上述两个 Major，并获 fresh `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`。四个 raw observation object 与 parent 完全一致，URL、excerpt、hash 为 `4/4` 未改且重算匹配；两条 Reddit interpretation 均显式记录污染来源并从 aggregation 隔离，任何 hypothesis 都不再引用它们。
- `HYP-BATCH-OUTREACH-CONTINUITY-001` 已删除，结果明确区分 inbound follow-up/scheduling/missed-lead management 与 outbound prospect research/personalization/cold-email preparation，并记录 `NO_REPEATED_PATTERN_IN_BATCH`。qBittorrent 与 Gradle 仍是两条 separate、single-source、`OPPORTUNITY_HYPOTHESIS_UNVALIDATED` hypothesis；predecessor 的 `FINAL FAIL / NO-GO` 历史保留在 active plan。
- 该 PASS 权限效果只到 `INTERPRETATION_LAYER_CORRECTION_ACCEPTED_ONLY`：接受四条 raw `OBSERVATION_ONLY` records、两条 quarantined interpretations、两条 clean separate unvalidated hypotheses，以及无重复 pattern 的结论；不授权 competitor mapping、产品选择、C8、Shadow 或任何外部/商业动作。
- 为避免在污染样本上继续推导，owner 随后预声明了六个只读 replication queries，但在运行前连续冻结并审查协议。v1 `883465f416d968b833d8780c69bcc809df48461a` 为 `FINAL FAIL`（`Critical 0 / Major 3 / Minor 0`）；v2 `4a1e3e5544034fd9d1363eeb6832ba42b8668660`、early-stop successor `a7af3856e0e56cfa4017026c44243e4dbb6fa72a` 和 final narrow successor `dc57e8c4c21d5aa8daa07ad5b00fa5b561899924` 均为 `FINAL FAIL`（各 `Critical 0 / Major 1 / Minor 0`）。四次 review 时六个 query 均为零，`authority_effect=NONE`。
- `dc57e8...` 已正确关闭 unvisited suffix，却仍要求完整 `web.open` 失败的 `EVALUATED / UNAVAILABLE_CONTEXT` row 提供不存在的 final-URL-derived canonical permalink。这复现了同一 trail-totality failure signature，正式触发已预声明的 stall：回到 `D026`，不再追加局部字段补丁，改为按 query/open outcome 划分互斥 closed trail states。owner 正在构造的 successor 是 mutable work，不是 portfolio-bound checkpoint；在新的 exact immutable review PASS 前不得运行任何 query 或 candidate-page open。
- 回退后的首个 closed-contract exact candidate `cc14d48f0edcd187ff925595a47b84a3cf33a54e` 随后获 `FINAL FAIL / NO-GO`（`Critical 0 / Major 4 / Minor 0`）。四个 blocker 是：caller-selected 替换协议可重算 hash 后通过且 protocol/contract vocabulary 与 author-independence 状态未闭合；跨 lane `ELIGIBLE_*` 可被接受；`stop_rank` 不必是第一条 target eligible；synthetic fixture 只改 `record_mode` 就能冒充 execution trail。该候选 `authority_effect=NONE`，六个 query 继续为零。
- schema-root successor `0ae297bf124d50c7d662e2152cdb87a9408848aa` 已关闭上述四项，但 fresh archive-only review 仍为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 2 / Minor 0`）。execution URL profile 未要求 absolute HTTPS/nonempty host，也未规范化 DNS terminal dot，因此 `fixture.invalid.`、hostless 或 non-HTTPS 形式可绕过 mode split；`datetime.strptime` 还接受 `2026-7-28T2:0:0Z` 这类不满足 exact lexical grammar 的时间。另有一个不计入该 trail verdict 的 inherited blocker：完整控制面 verifier 仅因 `D027-A` 使用不受支持的 `REJECTED_STALL` state 而 `valid=false`。两者都必须在查询前关闭。
- exact successor `426c3e18a22d8f610f4d35e6fe7d53668d2ea3cc` 已关闭 terminal-dot/basic URL、两个 timestamp 字段和 inherited `D027-A` blocker；完整控制面 `13/13` checks、`Ran 21 tests; OK`，trail `Ran 19 tests; OK`。但 fresh archive-only review 仍为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 1 / Minor 0`）：`urlsplit().hostname` 被当成完整 URL validator，没有严格 host grammar、percent-encoding 与 port 校验；`https://fixture%2einvalid/x`、`https://fixture.invalid%2e/x`、空白 host、非法或越界 port 在 `returned_url` 和 `canonical_permalink` 中仍可返回 `valid=true`。根任务完整入口矩阵为 `107` 项，`97` 项符合预期、`10` 个 false accepts、`0` crashes；十个 false accept 均归并为同一 URL host-grammar 根因。六个 query 仍为零，review authority effect 为 `NONE`。
- URL-host-grammar 窄 successor `68ef499cde3f7fa47a0bdef4b7687f77e135ff02` 已关闭上述 parent false accepts，保留 mixed-case、IDNA、terminal-dot 与 valid-port 正例；但 fresh review 仍为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 1 / Minor 0`）。同一 grammar 未区分 absent port 与 explicit empty port，且只拒绝 ASCII C0/DEL，没有拒绝 raw Unicode whitespace/control/format；`https://public.vendor.dev:/x`、U+0085、U+00A0 等表示在 `returned_url` 与 `canonical_permalink` 中仍会返回 `valid=true`。根任务矩阵为 `64` 项、`58` 项符合预期、`6` 个 false accepts、`0` crashes；独立矩阵为 `119` 项、`113` 项符合预期、`6` 个 false accepts、`0` crashes。六个 query 继续为零，review authority effect 为 `NONE`。
- 第三个 same-signature successor `5bd2cbf74167a8cb9c20477849de7c3561d6efc9` 已关闭 empty-port 与 raw Unicode category false accepts；全 Unicode property check 覆盖 `959303` 个 whitespace/Cc/Cf/Cs/Co/Cn 字符，端口 property matrix 覆盖 `131096` 项，均为零偏差。但 fresh review 仍为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 1 / Minor 0`）：代码先去除 raw ASCII terminal dot，再做 IDNA，导致 U+3002、U+FF0E、U+FF61 三种 IDNA-equivalent terminal separators 映射为 ASCII dot 后形成 empty terminal label；三个字符乘两个 URL 字段产生 `6/6` adjacent-valid false rejects。独立 targeted matrix 为 `90` 项、`84` 项符合预期、`6` 个 false rejects、`0` crashes。该候选达到预声明的第三次同签名 stall boundary，必须回到 `D027` 比较复用已审 public-locator validator/component 与简化 execution URL representation，不再进行第四次 per-string/per-separator patch。
- D027 回溯后的 protocol-only candidate `8f0c6d9b76424f1277bc1581b1baf3ee8fd81e2a` 选择 opaque search-result ref + separately validated capture receipt，并精确 pin reviewed Gate validator/test bytes；archive identity、四文件 delta、控制面 `13/13`、`Ran 21 tests; OK`、Gate `Ran 15 tests; OK` 均复现。但 fresh review 为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 3 / Minor 0`）。三个上游 blocker 是：`result_ref/open_input_ref/capture/receipt` 可整体一致替换而没有独立 search-response/open-action provenance；capture outcome 只列 accepted/rejected 却另行使用未定义 ambiguity 状态，且 pinned callable 会接受 explicit-empty port 与空 query/fragment delimiter；content evidence 没有可重算 artifact，locator receipt 也不绑定 contamination、4D job、author independence、disposition、stop 或 count。同一有效 locator receipt 因而仍可支撑互相冲突的 eligibility 和 early stop。该 review 的 `authority_effect=NONE`，六个 query 继续为零。
- v3.1 protocol-only successor `509862d28712caed93e67cf35e6e632d3d30f8d7` 已关闭 D028 的方向性根因：ref 明确只作 nonauthoritative correlation；固定窗口取消 semantic early stop；capture exact file bytes 拥有 excerpt/paraphrase evidence；Gate outcome 只有 accepted/rejected；accepted identity 是 exact raw final-locator UTF-8 SHA，不再 canonicalize；缺任何 semantic receipt 时 batch 无 outcome。但 fresh archive-only review 仍为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 3 / Minor 0`）。第一项 Major 是 v2 query/surface/window/J1/J2 signatures 只按 path 继承、Gate exact path也未写明、historical raw set无 manifest，因此保持 v3.1 SHA 不变仍可漂移 source universe。第二项是“前十个带 ref/display 的 rows”对先取十项后过滤与扫到十个合格项有两种解释，missing-field/duplicate-ref entry没有唯一 invalid/incomplete state。第三项是 semantic record 只有一组通用 4D status，没有 per-job assignment derivation、closed source-fidelity enum、reviewer/request/independence identity或 cross-field→terminal disposition table；同一合法字段向量仍可自由选 eligible job并获得自签式 PASS receipt。该 review 的 `authority_effect=NONE`，六个 query 继续为零。
- v3.2 protocol-bundle successor `1f6517968f4426bc43ea6dd0a8c4d66396b4b182` 已使当前 protocol/v2/Gate/tests/historical manifest hashes全部匹配；历史 source commit/path/SHA、四个 member IDs/URLs 与 raw hashes均重算一致；first-ten改为 no-filter/no-fill，missing ref/display与duplicate ref会使 query window invalid。但 fresh review仍为 `FINAL FAIL / NO-GO`（`Critical 0 / Major 4 / Minor 1`）。四个 Major 是：protocol仍从未进入bundle的v3.1继承capture schema/outcome/terminal enum；一边signature完整而另一边仍含AMBIGUOUS时会误落eligible，且author first/later的NOT_APPLICABLE/DIFFERENT映射不总量；request没有candidate-writer thread identity或candidate外presented-output envelope，writer仍可自签；QUERY_UNAVAILABLE或open-success漏capture时complete-batch guard可真空通过并输出`NO_CLEAN_REPLICATION`。Minor是structured raw entry hash未定义exact serialization/encoding。该 review的`authority_effect=NONE`，六个query继续为零。
- v3.2 后按 D029 complexity falsifier 回退到 D025/D032，不再把通用 schema completion 当作取得现实材料的前提。查询前 exact checkpoint `414628970a22ae0e290230ad0ad33b747c82e3b1`（tree `f76729a3d0793764f8fb0ef1f89bb8f998d9dd67`）预声明六条一次性只读 query，只允许 `PUBLIC_OBSERVATION_CANDIDATES`、`QUARANTINED` 或 scope-limited negative label，禁止 rank/coverage/count/pattern/demand/产品/价格/交易与全部 external write。但 active plan 第 101 行把查询设为 `IN_PROGRESS`，第 103 行仍要求任何 query 等 implementation successor PASS；Decision Log 也仍把 D026/D027 写成 current active。按 README 的 `STALE_STATE` 规则，fresh review 为 `FINAL FAIL`（`Critical 0 / Major 1 / Minor 1`，`authority_effect=NONE`）。Minor 是 `NO_USABLE_SOURCE_FOUND` 没有区分 tool unavailable、零结果、打不开、全 quarantine 与只检查子集，应降为 `NO_USABLE_SOURCE_CAPTURED_IN_INSPECTED_SUBSET` 并记录原因。该 checkpoint 冻结时六个 query 为零；后续 live 网页材料仍是未验收草稿。
- D032 control successor `978245169e7b2970fc2993b4f3d151e47dd1d42c` 已把旧 implementation-PASS gate 标为 `SUPERSEDED_BY_D032` 并恢复唯一当前动作，控制子范围获 `FINAL PASS C0/M0/m0`；但 notebook 自身未携带 draft status，full-commit evidence ceiling 为 `FINAL FAIL C0/M1/m0`。narrow successor `ee0703127dd26eab0005ce4e1fa2e69965de5504` 在文件顶部声明 `UNREVIEWED_DRAFT_CAPTURE / authority_effect=NONE`，draft gate 获 `FINAL PASS C0/M0/m0`。随后两组 reviewer 只读核对五条 public permalinks：J1 两条、J2 三条的 excerpts 均逐字存在且不超过二十五个英文词，context/job-boundary 合比例；两条 r/smallbusiness 新帖与历史 pain-point solicitation、HubSpot vendor release、无 permalink/open-failed 项均保持 quarantine。合并 source-fidelity verdict 为 `FINAL PASS C0/M0/m1`，唯一 Minor 是 `Are cold emails dead??` 在 notebook 中少一个问号。authority 只为 `OBSERVATION_CANDIDATES_AND_QUARANTINE_ONLY`；六查询实际执行和 coverage 没有独立 process proof。
- 两个 job 分离后的 semantic-eval-v2 design candidate `addfe09a4dd511f3ff43d6085645098e9555ff9e`（tree `b7537a0323d5cb23a2f047d7f59f5b0992963a45`）随后接受两项独立 archive-only 审查。合并 verdict 为 `FINAL FAIL C0/M2/m1`、`authority_effect=NONE`。第一项 Major 是根 README 与 active plan 仍把 v1 恢复为当前 prototype，而 v2 README 同时声明 v1 unaccepted、v2 为新候选，按项目自己的规则形成 `STALE_STATE`。第二项 Major 是只冻结了 task 与 fixture，没有在实现前冻结每条 record 的唯一 gold、cross-field truth table 和 J1/J2 各自的 verdict contract，未来 verifier 仍可事后挑选正确答案。Minor 是 fixture context 已泄露目标解释，因此即使未来 PASS 也最多证明 `EXACT_CONTEXT_CONFORMANCE`，不能证明模型独立解释公开来源。最小 successor 只能先原子更新 current pointer，并冻结 evaluator-only、彼此不传播的 J1/J2 oracle；在此之前不得实现或运行模型。
- v2.1 design `86f34bac8885dd6baa63fa1cfb8806160fd6f858` 与 recovery pointer `d40bc9acc6d07daf32a381bb855d14864312e7e5` 随后关闭了旧 current pointer、逐行 gold 与 construct overclaim。recovery fresh review 为 `FINAL PASS C0/M0/m0`，只说明无旧聊天可唯一恢复 v2.1、旧 v1 与过早实现 `04882e...` 保持 historical NO-GO、J1/J2 不传播且下一动作仅是 design review；`authority_effect=NONE`。但 design fresh review 仍为 `FINAL FAIL C0/M1/m0`：TASK 让 adapter 自己输出 `EXACT_CONTEXT_CONFORMANCE_PASS`，oracle 也只保存 expected PASS，没有 evaluator-owned 的 total verdict algebra 规定“精确依赖、完整 rows、authority 与 oracle 全等才派生 PASS，其余全部 FAIL_CLOSED”。同一 ownership 根因还留下 adapter-visible bundle/gold isolation 与 manifest path base/SHA 未冻结。下一 design successor 必须先删除或忽略模型自签 verdict、冻结 evaluator-owned iff/FAIL_CLOSED 合同与输入隔离；不得先实现或运行模型。
- v2.2 design `c6239a53c9e8cca83ea8390443131ffc1261f9f7` 与 pointer `77853f9e89e0500f3507df6432d284b0db80a925` 已删除 adapter verdict、冻结完整 row/oracle 全等、closed failure、input isolation design 与 J1/J2 non-propagation。recovery review 再次为 `FINAL PASS C0/M0/m0`。但 design review 仍为 `FINAL FAIL C0/M1/m0`：内部 `DESIGN_BUNDLE → design manifest → contract/oracle/adapter manifest → TASK/fixture` 哈希链全部正确，trust root 却仍来自 `PINNED_CONTRACT_PRESENTED_BY_CALLER`。caller 可整套替换 bundle、contract 与 oracle 后重算内部哈希并自洽通过；README 写出 root hash 但没有把它定义为 evaluator 不可覆盖的 build/review-time constant。下一窄 successor 只能固定不可由 caller 提供的 exact root bundle SHA/length，并从该固定 archive-relative root 验整链。该 evaluator-ownership signature 已连续失败两次；若下一次仍复现，不再加 pin 字段，回退到每 lane 最小硬编码 evaluator 或停止 model qualification 支线。
- final same-signature v2.3 design `c222f1fcc312b8d96235a265eba6f420c04d4f9d` 与 pointer `a58825e1157431cb8c508b3b22e9738a09f0ecf8` 把 build-time root literal、source-relative resolver 和 caller surface 冻结后，recovery 仍为 `FINAL PASS C0/M0/m0`，design 仍为 `FINAL FAIL C0/M1/m0`。唯一 Major 是新 caller contract 与继承的 exact v2.2 algebra 直接矛盾：v2.3 禁止 caller 呈示 contract，但 root-bound v2.2 contract 仍把 `PINNED_CONTRACT_PRESENTED_BY_CALLER` 作为 PASS 必要条件；严格遵守两者时不存在合法 PASS path。该 finding 属于同一 evaluator/caller-ownership signature，第三次失败正式触发 stall：禁止 v2.4 或第四次 pin patch，回到 D020 比较直接 build-time-owned per-lane evaluator 与停止 model qualification 支线。portfolio 建议选择后者，因为 exact-context conformance 不减少 source interpretation、需求、买方或 WTP 未知。
- D034 首个 backtrack state `85040835eb2bba61226bd61c4c9cfa77dc4383bf` 因 `NO_INFORMATION` 后仍可在重选现实证据与恢复 evaluator 之间自由分支而获 `FINAL FAIL C0/M1/m0`。narrow successor `fbedb67631a357fc8f204ebea1e1423b060b165a` 已获 `FINAL PASS C0/M0/m0`：若 reviewed experiment 没有减少任何预声明商业未知，唯一记录是 `NO_INFORMATION → D020 → 重选现实证据源`；负结果不得单独恢复 model qualification/per-lane evaluator，只有新现实证据证明 evaluator indispensable 才能新建 decision 并 fresh review。该 PASS 只关闭 recovery 逻辑，`authority_effect=NONE`。
- 五页 `public-trade-proximity-audit-v1` design `ec064deac7298e41b7fd195979cfab8a94f37cd5` 的 source bindings、最小引用、NOT_OBSERVED scope 和无搜索/无外联边界通过窄检查，但整体为 `FINAL FAIL C0/M3/m0`。三个 Major 是：其他参与者或 vendor/promo commenter 可驱动 direct-target 分支；TIME/MONEY/VOLUME 与 payment/price/budget/purchase/hire 没有形成闭合类型；状态代数不能区分 source drift、ambiguous ownership、partial visibility，并缺 exact duplicate/type fail-closed。页面为零运行，结果与商业权限均不存在。
- 简化 v2 design `77958750b41f45a56781e11982069f0f2cb0ac57`（tree `fe157f04c5c32b7b2989f50f551ffc5384882d70`）已关闭 speaker ownership 与 predicate ontology 两项 Major，但 fresh review 仍为 `FINAL FAIL C0/M1/m0`。唯一 Major 是 `RESULT_CONTRACT.json` 只把 identity/claim-ceiling 字段约束成 string、把 authority 约束成 object；末尾 intended constants 没有 exact-equal 绑定到 result root，nested authority 也没有 closed exact-false schema。因此 otherwise conforming result 可自报 demand/WTP/buyer/pattern/page execution verified，并把 external/evidence/C8/Shadow/product authority 设为 true。当前只允许固定这组常量与负例；页面仍为零运行，`authority_effect=NONE`。
- root-authority successor `affcb1653c24d7f02954155444a31e22313b16eb`（tree `071d2649a9fa5921607fe0068cd9f475b6409605`）已把四个 root constants、五个 exact-false authority keys 与非真空 all-PAGE_UNAVAILABLE witness 关闭。两名 reviewer 的原始 verdict 分别为 `FINAL FAIL C0/M2/m1` 与 `FINAL FAIL C0/M1/m0`；root 合并后为 `FINAL FAIL C0/M1/m1`。唯一当前 Major 是选择性 `NEGATIVE_CASES.json` 被 protocol 提升为完整 implementation oracle，却没有 coverage derivation，错误 validator 仍可接受 string/null/list/object 或 one/one-point-zero aliases并通过全部 declared cases。Minor 是 protocol 写了不存在的 `root_exact_values`，实际路径为 `root_shape.exact_values`。manifest self-hash finding 不计入当前 blocker：exact Git commit/tree 已闭合 manifest self 与四 payload，禁止引入递归 self-hash。由于失败已从实验语义转移到负例 DSL 元层，当前按 stall/backtrack 不做 v2.2 field patch，改为 minimal manual-read design：每页先产出 `UNREVIEWED_DRAFT / authority_effect=NONE`，独立 reviewer 再打开原页核对 speaker/excerpt/literal predicate；无跨页 count/pattern 或商业推导。页面仍为零运行。
- D035 manual-read exact candidate `96ce25a7ebebc587509e9211e5a3f774af6de2aa`（tree `f459a031382b58ac6a98b6924891edfd6963fcaa`）只新增人工协议并追加 decision，确实删除 validator、negative DSL、self-manifest、model PASS、count/rank/score/aggregation 前置。exact fixture、五页、九 predicates、source-only container、四 page states、其他 participant/vendor 零传播、无汇总与 `NO_INFORMATION → D020` 均通过两名独立检查。但两名 reviewer 都给出 `FINAL FAIL C0/M1/m0`：review PASS 没有绑定 immutable draft commit/tree/path/blob，也没有 reviewer/thread identity 与未参与 draft 的可审计声明；draft A 可 PASS 后改成 B 沿用结论，或 executor 自写 reviewed conclusion。最小 successor 只需 exact Git draft identity + plain human-readable independent review note；任一 FAIL/冲突保持 no observation，不允许 latest-wins/PASS-wins。不得重建 receipt DSL。页面继续零运行。
- manual v1.1 successor `297dca9ade9301a18737ede6396213c9e414f16b`（tree `daea3756cf1b45ec03706a081683eb5913933937`）仅修改同一 protocol，并获两份 fresh `FINAL PASS C0/M0/m0`。draft 现在以 exact commit/tree/path/blob/file-SHA/record identity 冻结；plain review note 绑定 exact draft/fixture、writer/reviewer thread identities、未参与声明、原页 page-state/predicate/excerpt/hash 与 verdict，note 自身也冻结。任何 FAIL、冲突或 independence 不可核对都 no observation，禁止 latest/PASS/多数票，解决只能新 immutable draft successor。未引入 validator/DSL/manifest。权限只到按固定顺序打开五个 exact public pages、写五个 `UNREVIEWED_DRAFT` 并冻结 batch；reviewed observation 仍不存在。
- owner 随后按该权限只读打开五页并写出五个独立 `UNREVIEWED_DRAFT`。checkpoint 逐字记录：`fixed_page_count=5`、五条 operator state 均为 `CAPTURED_SOURCE_MATCH`、`predicate_count_per_draft=9`、`excerpt_word_limit=25`、authority 全 false、aggregation forbidden，并把 protocol review binding 与五个 owner-reported SHA 保存为 mutable recovery identity。冻结时 Codex 使用额度触顶，`git add` 审批被拒；checkpoint 没有提供新的 HEAD 身份，也不存在 draft batch commit/tree/blob identity。它不能启动 source review或产生 observation；恢复后必须重新核五个 SHA，并由机会 owner 仅提交五个 draft。
- 本轮 bounded review 没有把 canonical exact serialization 与 historical/in-run duplicate identity 计分，也没有宣告它们通过；这两项保留为后续可审计 review scope，不能被本次窄修复静默视为 closure。

精确证据：`evidence/opportunity-d022-observation-baseline-review-2026-07-28.json`；`evidence/opportunity-d022-observation-baseline-successor-review-2026-07-28.json`；`evidence/opportunity-d022-observation-baseline-final-pass-2026-07-28.json`；`evidence/opportunity-public-input-gate-review-2026-07-28.json`；`evidence/opportunity-public-input-gate-successor-final-pass-2026-07-28.json`；`evidence/opportunity-first-real-public-observation-final-pass-2026-07-28.json`；`evidence/opportunity-discovery-batch-v1-final-fail-2026-07-28.json`；`evidence/opportunity-discovery-batch-v1-correction-final-pass-2026-07-28.json`；`evidence/opportunity-replication-protocol-lineage-final-stall-2026-07-28.json`；`evidence/opportunity-d032-convenience-sample-source-fidelity-pass-2026-07-28.json`；`evidence/opportunity-semantic-eval-v2-design-final-fail-2026-07-28.json`；`evidence/opportunity-semantic-eval-v2-1-design-review-2026-07-28.json`；`evidence/opportunity-semantic-eval-v2-2-design-review-2026-07-28.json`；`evidence/opportunity-semantic-eval-v2-3-stall-backtrack-2026-07-28.json`；`evidence/opportunity-d034-backtrack-and-trade-proximity-v1-review-2026-07-28.json`；`evidence/opportunity-trade-proximity-v2-design-final-fail-2026-07-28.json`。

补充精确证据：`evidence/opportunity-d034-backtrack-and-trade-proximity-v1-review-2026-07-28.json`；`evidence/opportunity-trade-proximity-v2-design-final-fail-2026-07-28.json`；`evidence/opportunity-trade-proximity-v2-1-review-and-backtrack-2026-07-28.json`；`evidence/opportunity-d035-manual-protocol-final-fail-2026-07-28.json`；`evidence/opportunity-d035-manual-v1-1-final-pass-2026-07-28.json`；`evidence/opportunity-five-draft-mutable-checkpoint-2026-07-28T091742-0700.json`。

### 投研纪律（portfolio-bound 快照 + moving read-only checkpoint）

- integrated project 仍报告 `design_freeze / blocked`，边界仍是 paper-only、human-final。
- isolated recovery tree 仍是 moving state，没有 immutable exact candidate；历史 review 绑定旧 bytes，不能授权当前漂移后的 bytes。
- execution-recorder full-serial 报告 `156/156`、`0` failures、`0` errors、`2751.795` seconds；execution-loop cross-module 报告 `41/41`、`45.977` seconds。这两个结果只证明各自子范围，不证明 foundation recovery 或系统恢复。
- foundation recovery 声明 `33` 项测试并仍有 `3` 个失败：successor packet contract 与旧 S1 ledger binding 不一致；后续 pre-review/tool-error/genesis evidence 不在 transition exact write set；独立 main worktree 缺 archive anchor ref。因此整体仍非 PASS。
- owner 已拒绝改写既有 S1 ledger，并把 tracked moving diff 迁移到独立 main worktree、保留旧 source worktree。当前 R5 moving design SHA-256 为 `58aa43dd5431d3aaea8b9e0338130c0135bbac1c36be70ccd950970850bfd475`，方向是 production restore-only、以 type+OID 固定 raw Git objects，而不把 nondeterministic bundle pack bytes当规范 authority。
- owner 随后自查出 Critical：R5 把 “origin” 当 durable authority，但所需历史 commit 只存在于临时 recovery repository，knowledge-base main repository 并不含它；临时目录消失后将无法恢复声称要保留的历史。因此当前 R5 是 `INTERNAL_NO_GO`，且尚无 immutable commit/tree 或实现。
- Codex 使用额度拒绝了把该 Critical 写入隔离候选的尝试；没有绕过。根任务随后对相同 design SHA 发起两份互不写入的只读挑战。exact-design review 为 `REJECT/REVISE C1/M3/m1`：除临时 origin Critical 外，还发现旧 transition 没授权新 cutover targets、restore-only search 未绑定且已 stale、crash manifests 身份/拓扑未闭合。第一性原理路线比较为 `BACKTRACK_TO_STANDARD_GIT C2/M3/m1`：custom namespace 没有形成独立 trust/failure domain，proof surface 与 recovery path 反而扩大。
- 根任务的只读裁决采纳上游回退，不继续补 R5 projection/genesis。`STANDARD_GIT_AUDIT_BASELINE_V1` 目前只是下一 design-only 候选建议：durable standard Git commit/tree/tag、ledger checkpoint、excluded/private-artifact manifest、backup-set identity、fresh-clone/restore test 与有限负例。额度恢复不授权保存、冻结或提交；投研 workflow 必须先通过自己的 exact action gate。当前 local commit、ref/tag、remote、backup、implementation 与 activation 写入全部为 false。

该工作流 owner task 后续仍在移动候选中处理独立审查发现；在它发布新的 closed inventory、commit/tree 或等价 immutable snapshot 前，portfolio 不把 live 进展改写为已绑定 closure，也不冻结其工作。

精确证据：`evidence/investment-workflow-fresh-observation-2026-07-28T042104-0700.json`；`evidence/investment-moving-r5-readonly-checkpoint-2026-07-28T092105-0700.json`；`evidence/investment-r5-independent-review-and-architecture-backtrack-2026-07-28T093132-0700.json`。

## 同口径比较

| 维度 | 机会到交易 | 投研纪律 |
|---|---|---|
| 当前最大未知 | 五份 `UNREVIEWED_DRAFT` 能否冻结为 exact immutable batch 并通过逐页独立原页复核；即使通过也仍未知 repeated signal、prevalence、persistence today、buyer identity、willingness to pay、feasibility 与 transaction path | 小型 standard-Git audit baseline 能否在不重建 R5 复杂度的前提下提供真实 durable copy、tamper-evidence 与 restore drill，以及其余 `3` 个 foundation failures 能否随后关闭 |
| 当前最小动作 | 等额度恢复后重新核五个 draft hash，由机会 owner 只提交这五个文件；取得 commit/tree/path/blob/SHA 后才启动独立 source review | 将 `STANDARD_GIT_AUDIT_BASELINE_V1` 保持为 design-only 建议；先由投研 workflow 的 exact action gate 判断是否授权本地候选文件/commit，额度恢复本身不放行任何写入 |
| 可逆性 | 高：当前只需冻结已存在的五份本地 draft，且不重开搜索、不接触真人或市场；失败可保持 no observation | 高：当前只读审设计；但后续实现触及同一 runtime 安全面，审查成本更高 |
| 距现实反馈 | 相对更近：固定五页已只读打开，但内容仍是 mutable `UNREVIEWED_DRAFT`，尚未形成 reviewed observation，更未到真人需求、付款或交付 | 更远；当前首先解决 runtime 恢复安全，而且 paper-only 边界不允许用收益检验 |
| 当前资产学习 | 训练 evidence ceiling、provenance、需求假设分层与现实输入接口 | 训练高保证恢复、权限根、CAS、不可变候选与独立验收 |
| 现金流证据 | 无 | 无；也无稳定收益或已证 edge |

## 当前组合判断

从“以最低不可逆成本减少最大决策未知”看，机会线的下一步更小、离现实输入更近；投研线的下一步适合作为并行只读架构审查。这个排序是当前证据下的可逆推断，不是永久资源配置，也不是“机会线已经有需求”。

因此当前组合建议是：

1. 机会线作为近期现实校准主线，D032 已从架构循环回到真实公开材料；`ee07031...` 的五条 permalink 忠实度与污染边界获 `FINAL PASS C0/M0/m1`。v2.3 触发同签名 stall，禁止继续 v2.4；D034 回溯已通过。D035 manual v1.1 `297dca9ade9301a18737ede6396213c9e414f16b` 已获两份 `FINAL PASS C0/M0/m0`。固定五页只读 open 与 draft 编写已完成，但 Codex 使用额度阻止 Git 冻结；当前停在 mutable checkpoint，不能启动 source review。恢复后只重新核五个哈希并由机会 owner 冻结 batch，不重跑搜索或扩大页面。仍不按候选数量制造 repeated pattern，也不跳到联系真人、报价、产品开发或需求结论。
2. 投研线作为后台高保证系统训练线，`156/156` 与 `41/41` 只记为两个局部测试组通过；foundation recovery 仍有 `3` 个失败。R5 已被两份独立只读审查拒绝，当前路线建议回退到小型 standard-Git audit baseline。portfolio 不签发候选保存/commit、实现、backup、remote write 或恢复权限；是否物化 design-only candidate 先由 workflow exact action gate 决定。
3. 两线均不得把内部 PASS、代码量、测试数量或文档完整度记为现金流、资产或真实商业进展。

## 仍会真正改变排序的 owner 输入

当前已知暑期时间充足，因此“每周可投入时间”暂不构成阻塞。仍缺三个会改变组合顺序的输入：

- 现金目标：是否存在必须在某个日期前达到的最低月度净现金流；
- 损失边界：可承受的现金损失、法律/隐私/声誉责任上限；
- 投研用途：它首先是可靠性训练场，还是在保持 paper-only 的同时为未来个人投资决策做准备。

这些输入只影响优先级，不改变当前所有高影响权限仍为 false。

## 下一检查点

- 机会线：replication lineage直到v3.2及 D032 predecessor FAIL 均保留；`9782451...` 控制子范围 PASS 与 notebook FAIL 也分开保留。`ee07031...` 的 draft gate 与 source-fidelity 已 PASS，五条页面只被接受为 observation candidates/quarantine。v2.3、现实实验 v1/v2/v2.1 与 D035 manual v1 的 FAIL 历史均保留。manual v1.1 已获两份 `FINAL PASS C0/M0/m0`；五个 mutable draft 已写但 immutable batch 因额度限制不存在，source review 未开始。最高仍为`OPPORTUNITY_HYPOTHESIS_UNVALIDATED`；没有模型 qualification、query coverage proof、repeated signal、competitor mapping、产品或外部权限。
- 投研线：最后一次 portfolio-bound 快照仍是 `design_freeze / blocked`。moving R5 design `58aa43...` 没有 immutable commit/tree，foundation recovery 仍有 `3` 个失败；两份只读审查现已分别给出 `REJECT/REVISE C1/M3/m1` 与 `BACKTRACK_TO_STANDARD_GIT C2/M3/m1`。standard-Git baseline 仍只是 design-only 建议；额度恢复不放行 local commit/ref/tag/remote/backup/implementation，不从局部测试 PASS 或 mutable commentary 推断系统 PASS。
- portfolio：可以记录 owner 明确标注的 mutable/moving checkpoint 以暴露新未知，但不得据此改变 workflow-bound status、closure、authority 或提高证据等级；只有新的 immutable observation 才能做这些改变。不持续监控 mutable live root，不重建 R4 控制面。
