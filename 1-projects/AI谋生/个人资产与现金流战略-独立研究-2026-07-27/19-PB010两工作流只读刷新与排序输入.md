# PB010 两工作流只读刷新与排序输入

状态：`READ_ONLY_REFRESH_COMPLETE / NO_WORKFLOW_AUTHORITY`

本记录完成 `18-PB010最小组合基线.md` 要求的同口径只读刷新。它帮助做跨项目取舍，但不替任何 workflow 签发 PASS，也不授权写入、Shadow、外联、交易或投资。

## 新鲜事实

### 机会到交易

- Milestone A2 最小控制面已通过，但权限效果只到 `CONTROL_PLANE_ACCEPTED_ONLY`；C8、Shadow 与外部动作仍是 false。
- D022 选择了比完整 C8 修复更短的 Observation → Interpretation → Hypothesis baseline。
- exact synthetic predecessor `a1cb1a7699acd8eff59d4eaf0dfad9fb5edb4bc9` 的独立结论为 `FINAL FAIL`：`Critical 0 / Major 1 / Minor 1`。它把 synthetic fixture 表示为 public quotation，且错误 enum 类型可触发未捕获异常。
- successor `faf2ec7e9e7b4a8de729da08eceea8192f3c9c39` 已闭合这两个 finding，但 fresh review 仍为 `FINAL FAIL`：`Critical 0 / Major 1 / Minor 0`。唯一 Major 是 `fixture://`、`fixture://   ` 与 `fixture:///` 没有可定位目标仍返回 `valid=true`。
- 第二 successor `bf8969418faa77898ff3c0ad12a98394b863ad40` 已闭合空目标 locator，并获 fresh `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`。独立矩阵为 `58` 项攻击、`58` 项按预期拒绝、`0` unexpected accept、`0` crash；另一本任务完整入口矩阵为 `96/96` 符合预期。
- 该 PASS 只接受 exact synthetic structural baseline，权限效果仍为 `NONE`。下一阶段必须另行设计并审查 real-public-input fixture；本记录不授权它自动开始。
- public-input Gate candidate `7c99d2ab310d71c61bdf00c4aab68cc07b2e398b` 随后获 fresh `FINAL FAIL`：`Critical 0 / Major 3 / Minor 0`。父提交中的先验字节、硬哈希与双改拒绝均正确；三个 blocker 是 public-host classifier 会接受非公共 locator、严格 RFC3339 会接受 `24:00:00Z`、以及无效 CLI 结果仍 exit `0`。
- 当前没有持久正向 `PUBLIC_OBSERVATION` fixture，这符合“Gate PASS 前不放入真实材料”的阶段规则，不是第四个 finding；不得用 synthetic 内容冒充公共引用填补它。
- exact successor `71b50434051c7ad46fbfc9596914c045e2ecfd33` 已关闭上述三个 blocker，并获 fresh `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`。独立 reviewer 共做 `194` 项检查，`0` unexpected accept、`0` crash；portfolio 交叉矩阵为 `554` 项，其中 `547` 项预期拒绝、`7` 项受控正例，仍为 `0` unexpected accept、`0` crash。`17/17` 个 predecessor host 向量均在完整 `PUBLIC_OBSERVATION` 路径拒绝，`24:00:00Z` 拒绝，CLI 对 invalid input 非零退出。
- 该 PASS 的权限效果只到 `EXACT_OFFLINE_GATE_REVIEW_PASS_ONLY`：它允许机会线在**后续 immutable successor** 中让此 Gate 评估第一条真正捕获的只读公开记录，但本身没有接纳任何真实记录，也没有生成需求、购买意愿或商业证据。
- 机会 owner 随后先在 parent commit `149886e87eaa1a93e8b1436a6af8911d302a2ab6` 固定 source universe、eligibility 与 stopping rule，再在 successor `d06221014731d0223579c06c28fc20f77053a914` 捕获 GitHub issue `SillyTavern/SillyTavern#1400`。record-level fresh review 为 `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`，只接纳 exact record SHA-256 `a1c72f95f08da9ed84c3ef04e41497312c0a0c587501290906ff8124200f0ee9` 为 `OBSERVATION_ONLY`。
- 原文核对确认保存的是 `13` 个英文单词的 verbatim minimal quotation；没有保存作者身份、不必要个人数据或长篇 source copy。`WORKAROUND` 与原文上下文一致；解释仍明确把 prevalence、buyer identity、willingness to pay、persistence today、feasibility 与 transaction path 保持为 unknown，hypothesis 仍是 `OPPORTUNITY_HYPOTHESIS_UNVALIDATED`，`commercial_claim=false`。
- selection trail 只被接受为过去时、受限的 operator report；它不证明固定 ordinal rank、搜索结果完整性或未来可复现性。current/future search ranking drift 继续是 nonclaim。
- discovery batch v1 exact candidate `0d41f83ff0c6418bc2fff3fec31edbf038dc4f71` 的结构验证仍为 `valid=true / OBSERVATION_ONLY / OPPORTUNITY_HYPOTHESIS_UNVALIDATED / external_authority=false`，`15` 项单元测试通过；四条最小引用均为 verbatim，分别为 `15 / 21 / 14 / 18` 个英文单词，hash 全部匹配。
- 但 batch-level fresh review 为 `FINAL FAIL / NO-GO`：`Critical 0 / Major 2 / Minor 0`。第一项 Major 是 Reddit 选样污染：r/smallbusiness 是自述嵌入 pain-point market-research prompt，r/Entrepreneur 是自述嵌入自建工具/result story；结果记录却声称已排除这些来源类型。第二项 Major 是语义过度合并：inbound lead follow-up/scheduling/missed-lead management 与 outbound prospect research/personalization/cold-email preparation 只共享宽泛的 customer-acquisition + manual-work 主题，不满足预声明的 specific actor/context/current-state/desired-outcome 合并门。
- 最小修订不是删除所有原始材料：隔离两条污染 Reddit selection，不把它们用于 repeated-signal；qBittorrent 与 Gradle 两条干净 observation 继续保持互相分离；删除 `HYP-BATCH-OUTREACH-CONTINUITY-001` advisory merge，并记录 `NO_REPEATED_PATTERN_IN_BATCH`。本轮没有 repeated signal、需求、产品或商业权限。
- interpretation-only successor `956286dfd4731462f154d97ff4ecceb0d8657936` 已关闭上述两个 Major，并获 fresh `FINAL PASS`：`Critical 0 / Major 0 / Minor 0`。四个 raw observation object 与 parent 完全一致，URL、excerpt、hash 为 `4/4` 未改且重算匹配；两条 Reddit interpretation 均显式记录污染来源并从 aggregation 隔离，任何 hypothesis 都不再引用它们。
- `HYP-BATCH-OUTREACH-CONTINUITY-001` 已删除，结果明确区分 inbound follow-up/scheduling/missed-lead management 与 outbound prospect research/personalization/cold-email preparation，并记录 `NO_REPEATED_PATTERN_IN_BATCH`。qBittorrent 与 Gradle 仍是两条 separate、single-source、`OPPORTUNITY_HYPOTHESIS_UNVALIDATED` hypothesis；predecessor 的 `FINAL FAIL / NO-GO` 历史保留在 active plan。
- 该 PASS 权限效果只到 `INTERPRETATION_LAYER_CORRECTION_ACCEPTED_ONLY`：接受四条 raw `OBSERVATION_ONLY` records、两条 quarantined interpretations、两条 clean separate unvalidated hypotheses，以及无重复 pattern 的结论；不授权 competitor mapping、产品选择、C8、Shadow 或任何外部/商业动作。

精确证据：`evidence/opportunity-d022-observation-baseline-review-2026-07-28.json`；`evidence/opportunity-d022-observation-baseline-successor-review-2026-07-28.json`；`evidence/opportunity-d022-observation-baseline-final-pass-2026-07-28.json`；`evidence/opportunity-public-input-gate-review-2026-07-28.json`；`evidence/opportunity-public-input-gate-successor-final-pass-2026-07-28.json`；`evidence/opportunity-first-real-public-observation-final-pass-2026-07-28.json`；`evidence/opportunity-discovery-batch-v1-final-fail-2026-07-28.json`；`evidence/opportunity-discovery-batch-v1-correction-final-pass-2026-07-28.json`。

### 投研纪律（最后一次 portfolio-bound 快照）

- integrated project 仍报告 `design_freeze / blocked`，边界仍是 paper-only、human-final。
- isolated recovery tree 是 dirty moving tree，没有 immutable exact candidate。历史 review 绑定旧 recorder/test bytes，不能授权当前漂移后的 bytes。
- direct-entry 局部修复已存在，但当前 candidate-bound review 仍未完成；clone/genesis Critical 仍开放。
- capability/provisioning 架构分支已被 `REJECT/REVISE`；当前最窄候选是 SHA-256 `16f6145324207912038b4d3a8bf7b796e3c9f01054e432db001eec740398be57` 的 strict restore-only design，状态是等待独立架构挑战。
- 下一安全动作是先冻结并只读挑战这份设计；通过后再一次实现，并把 direct、CAS、genesis 放入同一个 immutable successor 统一审查。

该工作流 owner task 后续仍在移动候选中处理独立审查发现；在它发布新的 closed inventory、commit/tree 或等价 immutable snapshot 前，portfolio 不把 live 进展改写为已绑定 closure，也不冻结其工作。

精确证据：`evidence/investment-workflow-fresh-observation-2026-07-28T042104-0700.json`。

## 同口径比较

| 维度 | 机会到交易 | 投研纪律 |
|---|---|---|
| 当前最大未知 | 公开 workaround 表达存在，但本 batch 已确认 `NO_REPEATED_PATTERN_IN_BATCH`；prevalence、persistence today、buyer identity、willingness to pay、feasibility 与 transaction path 全部未知 | 新一轮 direct/genesis findings 能否在 workflow-local immutable successor 中关闭；portfolio 尚无新的绑定快照 |
| 当前最小动作 | interpretation correction checkpoint 已关闭；等待 workflow owner 预声明下一项 read-only source-lane/query checkpoint 及 claim ceiling，不由 portfolio 自动启动 competitor mapping 或选产品 | 由 workflow owner 完成当前原子单元并发布新 immutable snapshot；portfolio 只读绑定后再比较 |
| 可逆性 | 高：只改本地 fixture/verifier，不接触真人或市场 | 高：当前只读审设计；但后续实现触及同一 runtime 安全面，审查成本更高 |
| 距现实反馈 | 相对更近，但仍只到 separately reviewed public-input fixture；尚未到真人需求、付款或交付 | 更远；当前首先解决 runtime 恢复安全，而且 paper-only 边界不允许用收益检验 |
| 当前资产学习 | 训练 evidence ceiling、provenance、需求假设分层与现实输入接口 | 训练高保证恢复、权限根、CAS、不可变候选与独立验收 |
| 现金流证据 | 无 | 无；也无稳定收益或已证 edge |

## 当前组合判断

从“以最低不可逆成本减少最大决策未知”看，机会线的下一步更小、离现实输入更近；投研线的下一步适合作为并行只读架构审查。这个排序是当前证据下的可逆推断，不是永久资源配置，也不是“机会线已经有需求”。

因此当前组合建议是：

1. 机会线作为近期现实校准主线，第一条真正只读公开记录已获 exact record-level PASS；discovery batch predecessor 的 repeated-signal 合并失败，successor 已把污染与 overmerge 修正为 `NO_REPEATED_PATTERN_IN_BATCH`。下一步等待 owner 预声明新的 read-only checkpoint，不得从无重复 pattern 跳到 competitor mapping、联系真人、报价、产品开发或需求结论。
2. 投研线作为后台高保证系统训练线，继续由 workflow owner 在 paper-only、human-final 边界内关闭 direct/genesis findings；portfolio 不签发实现或恢复权限，只等待新的 immutable snapshot。
3. 两线均不得把内部 PASS、代码量、测试数量或文档完整度记为现金流、资产或真实商业进展。

## 仍会真正改变排序的 owner 输入

当前已知暑期时间充足，因此“每周可投入时间”暂不构成阻塞。仍缺三个会改变组合顺序的输入：

- 现金目标：是否存在必须在某个日期前达到的最低月度净现金流；
- 损失边界：可承受的现金损失、法律/隐私/声誉责任上限；
- 投研用途：它首先是可靠性训练场，还是在保持 paper-only 的同时为未来个人投资决策做准备。

这些输入只影响优先级，不改变当前所有高影响权限仍为 false。

## 下一检查点

- 机会线：synthetic baseline、corrected public-input Gate、第一条 exact 真实只读公开 record，以及 discovery batch interpretation-only correction 均已获对应范围的 fresh PASS；predecessor FAIL 历史保留，当前 batch 结论是 `NO_REPEATED_PATTERN_IN_BATCH`。等待 owner 发布下一项预声明的 read-only checkpoint；最高仍为 `OBSERVATION_ONLY / OPPORTUNITY_HYPOTHESIS_UNVALIDATED`，没有 competitor mapping、产品或外部权限。
- 投研线：最后一次 portfolio-bound 快照仍是 `design_freeze / blocked`；等待 owner 当前 direct/genesis 原子单元收敛并发布新的 immutable snapshot，不从 mutable task commentary 推断 PASS。
- portfolio：只在上述任一 workflow 发布新的 immutable observation 时更新比较；不监控 mutable live root，不重建 R4 控制面。
