**这是什么**：商业探针 envgrant 的待批发布文案——探针页已上线，这里是准备发到各社区的最终帖子。
**需要你做什么**：看完本卡后在 [[【商业探针｜发布审批】回一个词-envgrant]] 的 decision: 行回一个词（发 / edit: 怎么改 / 跳过）；手动渠道发完在 [[【商业探针｜手发回执】发完填日期-envgrant]] 填 posted_date。
**不办会怎样**：不批就不发布，deploy+窗口天数到期自动记 not-run（不算负例）。办结后机器自动清进 done/（原件在 innovation/commercial/runs/ 留档）。

# 商业探针行动卡（审批请求）— envgrant（2026-07-15）

**探针页已上线（验收通过：GET 200 / POST 303→Stripe）**：https://probe-envgrant.vercel.app
**Offer**：envgrant — $19.00。GRANT/REVOKE for your .env: split one canonical dotenv into per-role files (human / contractor / CI / AI agent) with a machine-checked guarantee that no secret ever leaks into a lower role — no encryption, no vault, no cloud.

## 待批文案（批准后逐字发出，≤1 卡待批）

### 帖子 1 → **Ask HN (Hacker News)**（手动：credentials 未配或该渠道无写 API——批准后此文案原样手贴）

**标题**：Ask HN: Would a plaintext GRANT/REVOKE split for .env (per-role files, leak-checked) be worth $19 to you?

```
I've seen a few threads here lately about agents touching production secrets, and people asking for guardrails once an AI coding agent or contractor gets repo access. My read: most teams still keep secrets in one .env, and everyone with repo access sees all of it.

I'm working on envgrant: a small CLI that splits one canonical .env into per-role files (human, contractor, CI, AI agent) with two checks — no secret-classified key can appear in a lower-role file, and merging all role files back together reproduces the original .env byte-for-byte. No encryption, no vault, no daemon — just deterministic plaintext splitting plus an invariant a script can verify.

I put together a presale/validation page for it: https://probe-envgrant.vercel.app. To be upfront, there's no working product yet — Stripe is in test mode, no charge today — I'm using pre-orders as a signal for whether to build this at all. Curious whether this solves a real problem for you, or whether Infisical/SOPS already cover it well enough that this isn't needed.
```

### 帖子 2 → **r/selfhosted**（手动：credentials 未配或该渠道无写 API——批准后此文案原样手贴）

**标题**：Presale validation: envgrant — zero-cloud, zero-encryption per-role .env splitting (human/CI/agent), feedback wanted

```
Saw the recurring theme here that anything shipping secrets to a vendor's cloud is dead on arrival. I'm building envgrant along those lines: a local CLI, no daemon, no cloud, no encryption. It takes one canonical .env and splits it into per-role files (human, contractor, CI, AI agent), with a check that no secret-classified key can ever appear in a lower-role file, and that merging the role files back together reproduces the original .env byte-for-byte.

It's not SOPS or git-crypt (no encryption) and it's not Infisical (no server to run) — closer to a plaintext linter for who gets to see which keys. If Infisical or dotenvx already covers this for you, I'd genuinely like to hear that.

This is a presale validation page, not a finished product: https://probe-envgrant.vercel.app. Stripe is in test mode, you won't be charged — pre-orders are just how I'm deciding whether to build it. Repo and spec will go public either way.
```

## 怎么批

打开同目录《【商业探针｜发布审批】回一个词-envgrant.md》，在 decision: 后回一个词：
**发**（照发）/ **edit: 怎么改**（改完重出卡再等你的发）/ **跳过**（本周不发）。

## 什么算信号（预注册，从真实发布时刻起 14 天）

- 主指标：≥ 15 次"点进 checkout"（每周一自动报告；监控流量已剔除）
- 自动渠道回执机器自记；手动渠道发完填 `【商业探针｜手发回执】发完填日期-envgrant.md` 的 posted_date
- 不批也行——deploy+14 天自动记 not-run（不算负例）
