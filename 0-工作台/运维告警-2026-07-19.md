> 归宿：按各条建议动作修复；修好不用动这张卡——巡检确认恢复后自动清进 done/（原件在 ops/alerts/ 留档）

**这是什么**：ops 环每日巡检发现线上产品故障——本环只观察不修复。
**需要你做什么**：按下面每节的「建议动作」修复；修好后这张卡不用动，巡检确认恢复会自动清卡。
**不办会怎样**：产品持续坏着，本告警每 3 天重发一次。

# 运维告警 2026-07-19

> 生成于 2026-07-19T10:15:26-07:00；OPS 环每日巡检（ops/check.py），只观察不修复。

## 诊断

### learn-digest-nightly

learning-digest 这个夜间学习摘要任务现在是挂的状态——它本该周期性跑完后留下一个心跳标记，但这个标记压根不存在。对 owner 来说，意味着这个 job 很可能从来没成功跑过，或者已经被卸载/没在调度里，最近的学习摘要基本可以认为都没产出。最可能的原因有两类：一是任务根本没在调度系统里注册（被卸载或 plist 掉了），二是跑起来了但一启动就崩——最常见的是运行环境里找不到 `claude` 命令这类 PATH 没配好的问题，导致进程还没干活就退出、自然也就写不出心跳。

## learn-digest-nightly（learning-digest）— down

- **证据（程序原文）**: `心跳文件 /Users/javencao/ai-architect/learning/digest/state/heartbeat 不存在——该进程可能从未成功跑过或已被卸载`
- **建议动作**: tail ~/ai-architect/learning/digest/logs/cycle-*.log 最新一份看 digest exit=N / ai_itself exit=N 与 traceback；FileNotFoundError: 'claude' = PATH 问题(run_digest_cycle.sh 的 export PATH 行/learn plist EnvironmentVariables 是否还在)；launchctl list | grep ai-architect.learn 确认 job 在；手动复现：bash ~/ai-architect/learning/run_digest_cycle.sh

## 诚实边界

- 证据是巡检时点快照，现在可能已恢复；恢复会记入下次作战板周报行，不另发文件。
- OPS 环 v1 无修复权限；上面的建议动作由 owner 执行。
- 若同一故障持续存在，每 3 天重发一次本告警。
