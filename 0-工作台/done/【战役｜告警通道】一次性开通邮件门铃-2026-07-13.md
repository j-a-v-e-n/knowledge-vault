**需要你做什么**：三选一回词（推荐 ①）——回「代办」/「自己开」/「换SMTP2GO」，写在下面 decision 行。

## 背景（你 2026-07-13 拍板 EMAIL 告警通道）
冻结内核卡（钱/身份/对外发布/新阻塞审批）→ 一行大白话邮件到 jacao@ucsd.edu；低急并成日批。
**面板=记录（卡照常全在这里，一张不少）；邮件=门铃（纯增量）**。机器永不登录你的邮箱。

代码已焊好并测试（gate() 出卡即按铃、kernel 族即时、其余日批、故障绝不挡出卡、
未开通期间告警排队不丢）。只差发信账号——凭证属冻结内核 (c)，所以这张卡等你。

## 调研结论（带源，全文 ledger/research/ 与本卡同 commit）
- **推荐：ntfy.sh 免费账号**——发件身份是 ntfy 自己（非你身份）、一个 HTTP POST 就能发、
  免费、验证邮箱恰好=收件人你自己。实测坐实匿名发信已禁（HTTP 400），须账号+验证邮箱；
  限额约 12 封/天（够用：owner-gate 设计目标=个位数卡/周）。
- 备选：SMTP2GO 免费 1000 封/月、送达率实测 95.5%（emailtooltester 2026）——更抗谎报垃圾，
  但要多一步发件人验证、机器要持 SMTP 凭证。若 ntfy 试发进垃圾箱，自动升级走这条。

## 三个选项
① **回「代办」**（推荐，你零操作）：授权我用 ntfy.sh 注册一个新账号（联系邮箱填 jacao@ucsd.edu，
   我用你已连接的 Gmail 只读权限抓验证链接完成注册；新账号 token 由机器持有，
   存 ledger/state/owner-alert.json——gitignored，永不入库）。装完立刻真发测试邮件并在
   你邮箱里只读确认到达（含垃圾箱排查），证据贴回本卡。
② **回「自己开」**：你花 3 分钟自己注册 https://ntfy.sh/signup（任意用户名+密码）→
   Account 页验证邮箱 jacao@ucsd.edu → 生成 Access Token → 把 token 贴回本卡 decision 行，
   其余我接手。
③ **回「换SMTP2GO」**：走送达率优先路线，我出逐步流程卡。

decision: 代办

---
办完 → 我的会话会消费本卡并把测试邮件证据写回，然后归档 done/。

---

## 开通证据（2026-07-19 执行会话，按 decision: 代办）

- ntfy.sh 账号已注册（联系邮箱 jacao@ucsd.edu；验证链接经你已连接的 Gmail 只读权限抓取，
  01:20 完成验证）；access token 由机器持有，存 ledger/state/owner-alert.json（gitignored 实测 ✓）。
- 首发实测抓到并修掉一个真 bug：urllib 的 HTTP 头按 latin-1 编码，中文标题必炸
  （UnicodeEncodeError）——已改走 ntfy 官方 JSON publish（title/email 进 body，UTF-8）。
- 真发证据（Gmail 只读核验，两封均落 INBOX、非垃圾箱）：
  - 01:21:29 【要你拍板】测试邮件 → 到达 ✓（Gmail message id 19f797756e29fb5f）
  - 01:21:30 【日批】9 件等你（积压 5+4 条一次清空）→ 到达 ✓（id 19f7977585436097）
  - 队列文件已清空（owner-alert-queue.jsonl 不存在 = 无积压）。
- 此后：冻结内核卡即时按铃（ownergate.gate 已焊）、低急并入日批（ops 巡检 + box supervisor 双挂钩）。

办完（机器代归档）→ done/

