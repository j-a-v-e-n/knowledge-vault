---
date: 2026-06-09
type: email-triage
threads_scanned: 0
threads_surfaced: 0
---

# Email Triage · 2026-06-09

> Gmail MCP 不可用：工具 `mcp__claude_ai_Gmail__search_threads` 等未在 deferred tool registry 中注册，ToolSearch 无法加载其 schema。邮件扫描跳过，无法生成本期报告内容。

**附加错误**：`/MyBrain/career/applications.md` 读取失败，系统报 `EDEADLK: Resource deadlock avoided`，推测为 Google Drive 本地同步锁冲突，已投递公司清单无法加载。

---

## 建议排查步骤

1. 确认 Gmail MCP server 已在 Claude Code 的 MCP 配置中启用（`~/.claude/settings.json` 或项目级 `.claude/settings.json` 的 `mcpServers` 字段）。
2. 若 MCP server 已配置但未出现在 deferred tools 里，尝试重启 Claude Code 后再次运行此 skill。
3. `applications.md` 的 EDEADLK 错误：等待 Google Drive 客户端完成同步后重试，或在 Finder 中手动打开确认文件可访问。

---

*生成于 2026-06-09 by Claude (skill: email-triage)*
