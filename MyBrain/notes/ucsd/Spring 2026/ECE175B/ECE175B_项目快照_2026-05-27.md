---
title: "ECE175B 项目完整快照 — 课程进度 + HW1 + HW2 全部内容"
type: snapshot
tags: [ECE175B, 项目快照, UCSD, Spring2026]
created: 2026-05-27
purpose: "可直接喂给新 Claude 实例的完整 context bundle"
---

# ECE175B 完整项目快照（截至 2026-05-27）

> 这是一份自包含的 context 文档：把它整篇贴进任何一个新的 Claude / GPT 对话，对方就能掌握 Javen 在 ECE175B 这门课的全部进度、已交作业内容、剩余任务、以及关键数字。
>
> 所有数字均**逐字摘自源文件**（report.tex / training script / lecture notes / grace-days tracker），未经任何转写。

---

## 0. 一页摘要（Executive Summary）

| 项目 | 状态 |
|------|------|
| **课程** | ECE175B: Deep Generative Models, Spring 2026, UCSD, Prof. Pengtao Xie |
| **学生** | Javen Cao, ECE undergrad (jacao@ucsd.edu) |
| **评分构成** | HW × 3（各 10%）+ Project（70%：Proposal 10% + Midterm 20% + Final 40%） |
| **Lecture 已上** | L1a / L1b / L2 / L3 / L4（5 讲，截至 2026-04-14 笔记日期） |
| **HW1（UniStory 设计提案）** | ✅ 已交（2026-04-20），2 页 NeurIPS PDF |
| **HW2（Conv β-VAE on Chest X-ray）** | ✅ 已交（2026-05-13，用了 1 grace day），含训练代码 + 结果 + 报告 |
| **HW3** | ❓ 暂无材料，未发布或尚未下载 |
| **Course Project Proposal** | `raw/ucsd/Spring 2026/ECE175B/proposal.pdf` 存在（2026-04-17），具体内容未在 vault 索引（详情见 §6） |
| **Midterm / Final Project** | 尚未启动 |
| **Grace days 余额** | **4 / 5 days remaining**（HW2 用了 1 天） |

### HW2 关键数字（来自 `HW2/report.tex` 第 50–135 行）

| 指标 | 值 |
|---|---|
| Inception Score (IS) | **1.65**（↑ 越高越好） |
| Fréchet Inception Distance (FID) | **209.89**（↓ 越低越好） |
| 模型参数量 | **2.96 M** |
| 训练图片数 | **5,856** |
| Wall time（NVIDIA T4） | **18.0 min** |
| Epochs | 30（前 10 epoch β-annealing） |
| Latent dim | 128 |
| Batch size | 128 |
| Image size | 64×64 grayscale |
| Optimizer | AdamW, lr=2e-3, wd=1e-5 |

---

## 1. 课程信息

来自 `notes/ucsd/Spring 2026/ECE175B/ECE175B_Lecture1a_课程导论与DGM概述.md` 和 `wiki/机器学习/ECE175B_概览.md`。

| 项目 | 内容 |
|------|------|
| 课程名 | ECE175B: Deep Generative Models |
| 教授 | Pengtao Xie (`p1xie@ucsd.edu`) |
| TA | Ruiyi Zhang (`ruz048@ucsd.edu`) |
| 学期 | Spring 2026 |
| Canvas | https://canvas.ucsd.edu/courses/74401 |
| 评分 | HW × 3（各 10%）+ Project 70%（Proposal 10% + Midterm 20% + Final 40%） |
| 迟交政策 | 0–24h = 75%，24–48h = 50%，>48h = 0；共 5 grace days |
| 前置知识 | Probability distributions, Linear algebra, Optimization, Machine learning, Kernels |
| 主教材参考 | Koller & Friedman, *Probabilistic Graphical Models* |

---

## 2. 课程知识地图

```
概率论 + 图论
    ↓
概率图模型 (PGM)
    ├── Bayesian Network (有向图, 因果)
    └── Markov Random Fields (无向图, 相关)
    ↓  + 深度学习
深度生成模型 (DGM)
    ├── VAE (变分自编码器) ← ELBO + 变分推断
    ├── GAN (生成对抗网络) ← 对抗训练
    ├── Diffusion Models (扩散模型) ← 待讲
    └── Autoregressive Models (自回归) ← LLM 就是这个
```

**DGM 的四大任务：**
1. 设计模型（Representation）
2. 参数学习（Learning）→ MLE
3. 数据生成（Sampling）
4. 数据理解（Inference）→ 通常 NP-hard，需变分

---

## 3. Lecture 笔记摘要

源文件均在 `notes/ucsd/Spring 2026/ECE175B/`。每讲对应的 lecture PDF 在 `raw/ucsd/Spring 2026/ECE175B/`。

### Lecture 1a — 课程导论与 DGM 概述（lecture-1a.pdf, 40 slides）

**核心点：**
- **判别 vs 生成**：判别模型建 P(Y|X)，只能分类；生成模型建 P(X) 或 P(X,Y)，能分类+生成。
- **DGM 三大核心问题**：Representation / Learning / Inference。
- **PGM**：用图编码条件独立关系，把指数级参数降到多项式级。
- **Bayesian Network 分解公式**：
  $$p(x_1, \ldots, x_K) = \prod_{k=1}^{K} p(x_k \mid \text{pa}(x_k))$$

### Lecture 1b — 贝叶斯网络（lecture-1b.pdf, 8 slides）

**核心点：**
- 七节点 DAG 分解实例：把 2⁷=128 个参数压缩到一系列条件分布之积。
- **I-map 性质**：图的独立性是真实分布独立性的子集。
- **LLM 是链式 BN**：
  $$P(W_{1:N}) = p(W_1) \cdot \prod_{k=2}^{N} P(W_k \mid W_{1:k-1})$$
  每个 Wₖ 是 Categorical（词表大小 V ≈ 10K–100K），Transformer 参数化条件分布。

### Lecture 2 — VAE 模型设计（lecture-2.pdf, 9 slides）

**核心点：**
- 图结构：Z → X（隐变量→观测）。
- 先验：$p(Z) = \mathcal{N}(0, I)$（无信息先验）。
- Decoder：$p(X|Z) = \mathcal{N}(X | f(Z), \text{diag}(g(Z)))$，f / g 是神经网络。
- VAE 三大任务：训练（学 f, g）、生成（从 Z 采样→X）、推断（P(Z|X)）。
- 推断的核心困难：$p(X) = \int p(X,Z) dZ$ 是 NP-hard。

### Lecture 3 — 变分推断与 ELBO（lecture-3.pdf, 7 slides）

**核心点：**
- **变分推断**：用 encoder 网络 $q_\theta(Z|X) = \mathcal{N}(Z | \tilde{a}(X), \tilde{b}(X))$ 近似真实后验。
- **KL 散度** ≥ 0，最小化 $\text{KL}(q \| p)$ 等价于最大化 ELBO。
- **ELBO 推导**：用 Jensen 不等式（log 是凹函数）：
  $$\log p_\phi(X) \geq \mathbb{E}_{q}\left[\log \frac{p_\phi(X,Z)}{q_\theta(Z|X)}\right] = \text{ELBO}$$
- $\log p(X) = \text{ELBO} + \text{KL}(q \| p)$，所以 ELBO 是下界。
- **EM 算法**：交替优化 encoder（E-step）和 decoder（M-step），坐标上升。

### Lecture 4 — GAN（lecture-4.pdf, 7 slides）

**核心点：**
- Generator G: Z→X；Discriminator D: X→[0,1]。
- 不算似然函数，对抗 minimax 博弈。
- **VAE vs GAN**：

| 维度 | VAE | GAN |
|---|---|---|
| 训练信号 | ELBO | 对抗博弈 |
| 需要 p(X)? | 需要（通过 ELBO 近似） | 不需要 |
| 推断能力 | 有（encoder） | 无 |
| 生成质量 | 倾向模糊 | 倾向锐利但 mode collapse |
| 稳定性 | 较稳 | 不稳 |

- GAN 已知问题：mode collapse、训练不稳定、无法评估似然。

### 笔记完整路径（如果对方有 vault 访问，可直接读全文）

- `notes/ucsd/Spring 2026/ECE175B/ECE175B_Lecture1a_课程导论与DGM概述.md` (116 lines)
- `notes/ucsd/Spring 2026/ECE175B/ECE175B_Lecture1b_贝叶斯网络.md` (89 lines)
- `notes/ucsd/Spring 2026/ECE175B/ECE175B_Lecture2_变分自编码器设计.md` (110 lines)
- `notes/ucsd/Spring 2026/ECE175B/ECE175B_Lecture3_变分推断与ELBO.md` (151 lines)
- `notes/ucsd/Spring 2026/ECE175B/ECE175B_Lecture4_生成对抗网络.md` (90 lines)
- `wiki/机器学习/ECE175B_概览.md`（课程总览页，96 lines）

---

## 4. HW1: UniStory — 统一自回归-扩散 Transformer

### 4.1 状态

- ✅ **已交**（截止 2026-05-05，实际 2026-04-20 完成）
- 格式：NeurIPS 2 页 PDF，249 KB
- 文件：`notes/ucsd/Spring 2026/ECE175B/HW1/unistory.pdf`

### 4.2 内容概述

设计题目：从短 prompt $c$（如 "We build a Disney park on the moon"）生成一个长篇故事 $s$ + 一张配图 $I$，模型 $p_\theta(s, I | c)$。

**核心设计：**
- 一个 Transformer，同时做文本 AR + 图像 diffusion（Transfusion 范式）。
- 文本侧：sketch-then-expand 两段式解码（先生成 ≤50 token 的 sketch，再生成 ~500 token 的 full story）。
- 图像侧：在 VAE latent 空间做扩散，VAE encoder/decoder 都冻结，只训 Transformer + adapters + heads。
- Attention mask：文本因果 + 图像块内部双向 + 图像看所有前置文本。
- 三个 loss：AR 文本 loss + DDPM 扩散 loss + CLIP 对齐 loss（$\lambda_{\text{diff}}=1.0$, $\lambda_{\text{align}}=0.1$）。

### 4.3 架构图（已嵌入提交的 PDF）

源图：`HW1/unistory_architecture.png`

文字描述：
```
            混合输入序列：
            [c][⟨SK⟩][s̃][⟨FS⟩][s]  |  image patches z_t
                       │
              Unified Transformer (L layers)
              shared self-attention over text+image
              causal on text · bidirectional within image
                       │
              ┌────────┴────────┐
        Text Head           Diffusion Head
        (AR softmax)        (ε-prediction)
              │                      │
        emit s̃ after ⟨SK⟩       Image Latent z_0
        emit s after ⟨FS⟩            │
                              VAE Decoder D (frozen)
                                     │
                                  Image I

   关键 attention：image block sees preceding text
   关键 conditioning：s conditions diffusion head
```

### 4.4 完整 LaTeX 源（`HW1/unistory.tex`）

```latex
\documentclass{article}
\usepackage[final]{neurips_2024}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{natbib}
\hypersetup{colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue}

\makeatletter
\renewcommand{\@noticestring}{ECE175B Homework 1 \textendash{} Spring 2026, UC San Diego}
\makeatother

\title{UniStory: A Unified Autoregressive--Diffusion Transformer for Visual-Textual Story Generation}

\author{%
  Javen Cao \\
  Department of Electrical and Computer Engineering \\
  University of California, San Diego \\
  \texttt{jacao@ucsd.edu}
}

\begin{document}
\maketitle
\vspace{-1.0em}

\section{Problem and Design Overview}
\label{sec:intro}

Given a short prompt $c$ (e.g., \emph{``We build a Disney park on the moon''}), we model the joint distribution $p_{\theta}(s, I \mid c)$, where $s = (s_1, \dots, s_N)$ is a long narrative and $I \in \mathbb{R}^{H \times W \times 3}$ is a representative image. Text is discrete and sequential; images are continuous and spatial---a single model must accommodate both. Moreover, $c$ typically underspecifies visual content, so the image should reflect the \emph{generated story}, not only the prompt. We propose \textbf{UniStory}, a single Transformer that performs next-token prediction on text and denoising diffusion on continuous image latents within a shared sequence, following the Transfusion paradigm~\citep{zhou2024transfusion}. A two-stage \emph{sketch-then-expand} decoder improves long-story coherence, and a CLIP-based alignment loss~\citep{radford2021clip} ties the image to the story.

\section{Model Architecture}
\label{sec:arch}

Figure~\ref{fig:arch} illustrates the pipeline. A BPE tokenizer embeds the prompt and the story, while a pretrained and \emph{frozen} VAE encoder $E$ maps an image to latent patches $z_0 = E(I) \in \mathbb{R}^{h \times w \times d}$. Lightweight modality adapters project both into a shared $d$-dimensional space; the same Transformer then consumes a \emph{mixed sequence} of text tokens and image patches. The attention mask is causal on text but bidirectional within the image block, preserving text$\rightarrow$image causal order globally~\citep{zhou2024transfusion}. Two output heads branch from the Transformer: a softmax \emph{text head} over the vocabulary (AR), and a \emph{diffusion head} that predicts $\epsilon$. A frozen VAE decoder $D$ maps the denoised latent to pixels; only the Transformer, adapters, and heads are trainable. For long-story coherence, text decoding proceeds as \emph{two sequential AR passes sharing the same weights and a single forward graph}: using learned separator tokens $\langle\text{SK}\rangle,\langle\text{FS}\rangle$, the model consumes $[c; \langle\text{SK}\rangle; \tilde{s}; \langle\text{FS}\rangle; s]$ and emits a sketch $\tilde{s}$ ($\leq\!50$ tokens) after $\langle\text{SK}\rangle$ and the full story $s$ ($\sim\!500$ tokens) after $\langle\text{FS}\rangle$.

\begin{figure}[h]
\vspace{-0.7em}
\centering
\includegraphics[width=0.48\linewidth]{unistory_architecture.png}
\vspace{-0.8em}
\caption{UniStory interleaves text tokens and image patches in one Transformer. Learned separators $\langle\text{SK}\rangle,\langle\text{FS}\rangle$ split the text side into sketch-then-expand passes; the image block attends to all preceding text, so the diffusion head is conditioned on $c$ and the generated story $s$.}
\label{fig:arch}
\vspace{-1.1em}
\end{figure}

\section{Training Objectives}
\label{sec:loss}

Let $x = (c, s, z_0)$ be a training triple. The \textbf{autoregressive text loss} (chain-rule factorization of the joint probability) is
\begin{equation}
    \mathcal{L}_{\text{LM}}(\theta) \;=\; -\mathbb{E}_{(c,s)}\!\left[\,\sum_{t=1}^{N} \log p_{\theta}\bigl(s_t \mid s_{<t},\, c\bigr)\,\right].
\end{equation}

The \textbf{diffusion loss} on image latents follows DDPM~\citep{ho2020ddpm}, an ELBO on the log-likelihood. With forward process $q(z_t \mid z_0)=\mathcal{N}(z_t; \sqrt{\bar\alpha_t}\, z_0, (1-\bar\alpha_t)\mathbf{I})$, the network $\epsilon_\theta$ predicts the injected noise, conditioned on both the prompt $c$ and the generated story $s$:
\begin{equation}
    \mathcal{L}_{\text{diff}}(\theta) \;=\; \mathbb{E}_{z_0,\,\epsilon,\,t}\!\left[\,\bigl\|\,\epsilon - \epsilon_\theta(z_t, t, c, s)\,\bigr\|_2^2\,\right],\quad \epsilon\sim\mathcal{N}(0,\mathbf{I}),\; t\sim\mathcal{U}\{1,\ldots,T\}.
\end{equation}

A \textbf{cross-modal alignment loss} pulls the generated image toward the story in CLIP space with frozen encoders $\phi_{\text{txt}}, \phi_{\text{img}}$. To keep it differentiable and cheap, we avoid running the full reverse chain and instead use the \emph{$x_0$-prediction shortcut}: at the same sampled $t$ as in $\mathcal{L}_{\text{diff}}$, we reconstruct the clean latent in one step from $\epsilon_\theta$:
\begin{equation}
    \hat{z}_0 \;=\; \frac{z_t - \sqrt{1-\bar\alpha_t}\;\epsilon_\theta(z_t, t, c, s)}{\sqrt{\bar\alpha_t}},\qquad
    \mathcal{L}_{\text{align}}(\theta) \;=\; 1 \;-\; \cos\!\bigl(\phi_{\text{txt}}(s),\; \phi_{\text{img}}(D(\hat{z}_0))\bigr).
\end{equation}

\noindent\textbf{Total objective:}
\begin{equation}
\boxed{\;\mathcal{L}_{\text{UniStory}}(\theta) \;=\; \mathcal{L}_{\text{LM}} \;+\; \lambda_{\text{diff}}\,\mathcal{L}_{\text{diff}} \;+\; \lambda_{\text{align}}\,\mathcal{L}_{\text{align}}\;}
\quad (\lambda_{\text{diff}}{=}1.0,\; \lambda_{\text{align}}{=}0.1).
\end{equation}

\section{Training Strategy and Inference}
\label{sec:train}

\textbf{Training} proceeds in two stages. \emph{Stage 1 (pretraining)}: large-scale image--caption pairs (e.g., LAION-5B) and text-only corpora are used jointly with $\mathcal{L}_{\text{LM}}$ and $\mathcal{L}_{\text{diff}}$; single-modality batches activate only the corresponding term. \emph{Stage 2 (fine-tuning)}: prompt--story pairs $(c, s)$ are harvested from the Reddit WritingPrompts corpus (the top-level comment acts as $s$, the title as $c$); for each pair we synthesize an illustration $I$ by running a frozen pretrained text-to-image teacher (e.g., SDXL) on $s$, and retain triples with $\text{CLIPScore}(s, I)\!\geq\!0.3$ (approximately the top 60\%); $\mathcal{L}_{\text{align}}$ is activated only in this stage. We use AdamW with cosine schedule, classifier-free guidance (CFG) dropout of $c$ and $s$ at 10\% each, and QK-Norm with an auxiliary $z$-loss to stabilize mixed-modal training~\citep{team2024chameleon}.

\textbf{Inference}: (i) sample sketch $\tilde{s}\!\sim\! p_\theta(\cdot\mid c)$; (ii) sample full story $s\!\sim\!p_\theta(\cdot\mid c,\tilde{s})$ with nucleus sampling ($p{=}0.9$); (iii) from $z_T\!\sim\!\mathcal{N}(0,\mathbf{I})$, run $T$ reverse diffusion steps with CFG, $\hat{\epsilon} = (1{+}w)\,\epsilon_\theta(z_t,t,c,s) - w\,\epsilon_\theta(z_t,t,\varnothing)$ where $\varnothing$ denotes dropping both $c$ and $s$ and $w{=}3.0$; decode $I=D(z_0)$.

\textbf{Design justification.} Pure AR image models (e.g., Chameleon) rely on VQ tokenization that caps fidelity; pure diffusion cannot natively generate long coherent text. The hybrid design keeps each paradigm's strength~\citep{zhang2025unified}; conditioning the image on $s$ rather than $c$ captures concrete visual elements (e.g., \emph{``Space Princess Castle''}) that only emerge during story generation.

\setlength{\bibsep}{2pt plus 0.3ex}
{\scriptsize
\bibliographystyle{plainnat}
\bibliography{unistory}
}

\end{document}
```

### 4.5 参考文献（`HW1/unistory.bib`）

1. **Zhou et al. 2024** — *Transfusion: Predict the Next Token and Diffuse Images with One Multi-Modal Model.* arXiv:2408.11039
2. **Chameleon Team (FAIR) 2024** — *Chameleon: Mixed-Modal Early-Fusion Foundation Models.* arXiv:2405.09818
3. **Ho et al. 2020** — *Denoising Diffusion Probabilistic Models.* NeurIPS
4. **Radford et al. 2021** — *Learning Transferable Visual Models from Natural Language Supervision (CLIP).* ICML
5. **Zhang et al. 2025** — *Unified Multimodal Understanding and Generation Models.* arXiv:2505.02567
6. **Ho & Salimans 2022** — *Classifier-Free Diffusion Guidance.* arXiv:2207.12598
7. **Schuhmann et al. 2022** — *LAION-5B.* NeurIPS

### 4.6 课程知识点对应

| 课程内容 | 在 UniStory 中的角色 |
|---|---|
| L1b: LLM 是链式 BN | 文本侧 AR loss 的理论基础 |
| L2: VAE | 图像用 VAE encoder 压到 latent space（降低扩散计算量） |
| L3: ELBO / KL 散度 | DDPM loss 是 ELBO 的简化形式 |
| L4: GAN | Discussion 中作为 baseline（GAN 不稳定 → 选 diffusion） |
| 待讲: Diffusion | 图像侧的核心建模方法 |

### 4.7 HW1 文件清单

```
HW1/
├── unistory.pdf                    ← 已提交文件（2 页, 249 KB）
├── unistory.tex                    ← LaTeX 源
├── unistory.bib                    ← BibTeX
├── unistory_architecture.png       ← 架构图（matplotlib 生成）
├── neurips_2024.sty                ← NeurIPS 样式
├── make_figure.py                  ← 架构图生成脚本（matplotlib）
├── 报告草稿.md                      ← 中英对照设计草稿
└── README_提交指南.md               ← 提交流程笔记
```

---

## 5. HW2: Convolutional β-VAE on Chest X-ray

### 5.1 状态

- ✅ **已交**（用了 1 grace day，截止从 2026-05-12 推到 2026-05-13）
- 提交 platform：Gradescope
- 文件：`notes/ucsd/Spring 2026/ECE175B/HW2/report.pdf`（由 report.tex 编译）
- Kaggle notebook：`HW2/VAE_ChestXray_Kaggle.ipynb`

### 5.2 任务

在 Kaggle 上的 **Chest X-ray Pneumonia 数据集**（paultimothymooney/chest-xray-pneumonia）从零训练一个 conv VAE，报告 IS 和 FID 指标，输出 NeurIPS 2 页 report。

### 5.3 实现要点

**模型：Conv β-VAE**

- Encoder：4 层 4×4 strided conv（1→32→64→128→256 channels），GroupNorm + SiLU
- 64×64 grayscale → 256×4×4 feature map → 两个 linear head 输出 $\mu, \log\sigma^2 \in \mathbb{R}^{128}$
- Decoder：镜像，用 transposed conv，输出 per-pixel Bernoulli logits

**Loss：**
$$\mathcal{L}(x) = \text{BCE}(\hat{x}, x) + \beta \cdot D_{\text{KL}}(q_\phi(z|x) \| \mathcal{N}(0, I))$$
其中 $\beta$ 在前 10 epoch 从 0 线性 anneal 到 1（KL warmup，防止 posterior collapse）。

**训练配置：**
- AdamW, lr=2e-3, weight_decay=1e-5
- Batch size = 128, 30 epochs
- Grad clipping at norm 5
- NVIDIA T4 GPU（Kaggle 免费）
- Wall time **18.0 min**

**评估：FID + IS**
- Inception V3 特征（手动重实现，避免 Kaggle 上的 torch-fidelity 依赖问题）
- 1000 generated vs 1000 real samples
- 3-channel replicate + bilinear upsample 到 299×299 + ImageNet normalize

### 5.4 结果

**Table 1（hyperparameters，from `report.tex`）：**

| Setting | Value |
|---|---|
| Image size | 64×64, grayscale |
| Latent dimension | 128 |
| Batch size | 128 |
| Epochs | 30 (KL warmup over first 10) |
| Optimizer | AdamW, lr=2×10⁻³, wd=10⁻⁵ |
| Loss | per-pixel BCE + β·KL, β_max=1 |
| Training images | 5,856 |
| Parameters | 2.96 M |

**Table 2（metrics，from `report.tex`）：**

| Metric | Value |
|---|---|
| **Inception Score (IS)** | **1.65**（higher is better） |
| **Fréchet Inception Distance (FID)** | **209.89**（lower is better） |

**Loss curve（`HW2/loss_curve.png`）：**
- Total loss 从 ~2620 降到 ~2400
- BCE 从 ~2610 降到 ~2380
- KL 在前 10 epoch 随 β annealing 上升后稳定在 ~22（说明 latent 没塌陷）

**Samples（`HW2/samples.png`）：**
- 16 张从 $z \sim \mathcal{N}(0, I)$ 解码的 chest X-ray
- 整体能抓住 ribcage / lung / mediastinum 布局
- 但纹理细节被洗掉——vanilla VAE 在 64×64 的已知通病

### 5.5 Discussion 中的关键归因

引用 `report.tex` 第 144–152 行原文：

> The relatively high FID and modest IS reflect two well-known VAE properties: (i) the factorized per-pixel Bernoulli likelihood encourages mean-pixel predictions, inducing blurry reconstructions, and (ii) a single global $z\in\mathbb{R}^{128}$ struggles to represent the high-frequency texture of X-ray projections. A natural extension is to replace the decoder with a hierarchical or autoregressive variant (e.g., VQ-VAE+PixelCNN, or NVAE) and/or pair the VAE with a diffusion refiner; such hybrids consistently improve FID by an order of magnitude on medical-imaging benchmarks.

### 5.6 完整 LaTeX 源（`HW2/report.tex`）

```latex
\documentclass{article}

\usepackage[final]{neurips_2024}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\hypersetup{colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue}

\makeatletter
\renewcommand{\@noticestring}{ECE175B Homework 2 \textendash{} Spring 2026, UC San Diego}
\makeatother

\title{A Convolutional $\beta$-VAE for Chest X-ray Image Generation}

\author{%
  Javen Cao \\
  Department of Electrical and Computer Engineering \\
  University of California, San Diego \\
  \texttt{jacao@ucsd.edu}
}

\begin{document}
\maketitle
\vspace{-1.0em}

\section{Introduction}
We train a convolutional variational autoencoder (VAE)~\cite{kingma2014} on the Chest X-ray
Pneumonia dataset~\cite{kermany2018} and evaluate generation quality with Inception
Score (IS) and Fr\'echet Inception Distance (FID). The implementation is from
scratch in PyTorch; ELBO uses a $\beta$-annealing schedule to prevent posterior collapse
during the early epochs.

\section{Method}

\paragraph{Model.} The encoder is a stack of four strided $4{\times}4$ convolutions
($1{\to}32{\to}64{\to}128{\to}256$ channels) with GroupNorm and SiLU activations, mapping
each $64{\times}64$ grayscale image to a $256{\times}4{\times}4$ feature map; two
linear heads then produce the latent mean $\mu \in \mathbb{R}^{128}$ and log-variance
$\log\sigma^2$. The decoder mirrors the encoder using transposed convolutions and outputs
per-pixel Bernoulli logits.

\paragraph{Objective.} We optimize the standard ELBO with a $\beta$-weighted KL term:
\begin{equation}
  \mathcal{L}(x) \;=\; \underbrace{\mathrm{BCE}\bigl(\hat{x}, x\bigr)}_{\text{reconstruction}}
  \;+\; \beta \cdot \underbrace{D_{\mathrm{KL}}\!\bigl(q_\phi(z\!\mid\!x)\,\|\,\mathcal{N}(0,I)\bigr)}_{\text{KL regularization}}\,,
\end{equation}
where $\beta$ is linearly annealed from $0$ to $1$ over the first $10$ epochs (KL warmup).
The total network has $2.96$\,M parameters.

\paragraph{Data and training.} We pool all $5{,}856$ chest X-ray images
(train + val + test splits, both classes) and treat them as a single unsupervised set, as
the VAE does not need pneumonia labels. Images are converted to grayscale and resized to
$64{\times}64$. Training uses AdamW ($lr=2{\times}10^{-3}$, $wd=10^{-5}$), batch size 128,
gradient clipping at norm 5, for $30$ epochs on a single NVIDIA T4 GPU
(wall time $18.0$\,min). Code: see \texttt{train\_vae\_chest\_xray.py}.

\paragraph{Evaluation.} Following the PyTorch-Ignite GAN evaluation
recipe~\cite{ignite_gan_eval} for FID~\cite{heusel2017} / IS~\cite{salimans2016}, we
extract features with InceptionV3 (manually reimplemented to avoid the
\texttt{torchmetrics}$\rightarrow$\texttt{torch-fidelity} dependency on Kaggle). $1{,}000$
samples are drawn from $\mathcal{N}(0,I)$, decoded, replicated to $3$ channels, bilinearly
upsampled to $299{\times}299$, and ImageNet-normalized. The reference set is $1{,}000$
random real chest X-rays. FID uses the $2048$-d pool features and
\texttt{scipy.linalg.sqrtm}; IS computes
$\exp\!\left(\mathbb{E}_x \mathrm{KL}\!\left(p(y\!\mid\!x)\,\|\,p(y)\right)\right)$ on logit softmaxes.

\section{Results}

\paragraph{Hyperparameters.} Table~\ref{tab:hparams} summarizes the configuration.

[See Table 1 above for the same content]

\paragraph{Training curves.} Figure~\ref{fig:loss} plots total loss, reconstruction
(BCE), and KL divergence per epoch. The KL term grows monotonically as $\beta$ increases
from $0$ to $1$ during epochs $1$--$10$, then the BCE continues to decrease while the KL
stabilizes, indicating the model uses the latent code without collapsing.

\paragraph{Evaluation metrics.} On 1{,}000 generated vs.\ 1{,}000 real images:
[See Table 2 above — IS=1.65, FID=209.89]

\paragraph{Generated samples.} Figure~\ref{fig:samples} shows 16 chest X-rays decoded
from i.i.d.\ samples $z\sim\mathcal{N}(0,I)$. The model captures the overall
ribcage--lung--mediastinum layout of a chest radiograph, though fine pulmonary detail is
washed out --- a known limitation of vanilla VAEs at $64{\times}64$ resolution.

\section{Discussion}
[See section 5.5 above for full text]

\paragraph{AI tool disclosure.} Per syllabus: I used GitHub Copilot for boilerplate
(\textit{e.g.}, dataloader scaffolding) and Claude (Anthropic) for LaTeX formatting and
debugging suggestions; all model design choices and analyses are my own.

\bibliographystyle{plain}
\begin{thebibliography}{9}
\bibitem{kingma2014} Kingma, D.P., \& Welling, M. (2014). Auto-Encoding Variational Bayes. \textit{ICLR}.
\bibitem{kermany2018} Kermany, D.S. et al. (2018). Identifying Medical Diagnoses and Treatable Diseases by Image-Based Deep Learning. \textit{Cell}, 172(5):1122--1131.
\bibitem{heusel2017} Heusel, M. et al. (2017). GANs Trained by a Two Time-Scale Update Rule Converge to a Local Nash Equilibrium. \textit{NeurIPS}.
\bibitem{salimans2016} Salimans, T. et al. (2016). Improved Techniques for Training GANs. \textit{NeurIPS}.
\bibitem{ignite_gan_eval} PyTorch-Ignite. GAN Evaluation: FID \& IS. \url{https://pytorch-ignite.ai/blog/gan-evaluation-with-fid-and-is/}
\end{thebibliography}

\end{document}
```

### 5.7 训练脚本（`HW2/train_vae_chest_xray.py`，287 lines）

完整脚本太长，这里给关键结构摘录：

```python
"""ECE175B HW2 — VAE on Chest X-ray Pneumonia dataset.
End-to-end script for Kaggle.
Architecture: Conv β-VAE, 64×64 grayscale, latent=128.
Training: 30 epochs, AdamW 2e-3, KL warmup over first 10 epochs.
Evaluation: FID + Inception Score (via pytorch-ignite) on 1000 generated samples.
"""

# Config
SEED = 42
IMG_SIZE = 64
LATENT_DIM = 128
BATCH = 128
EPOCHS = 30
LR = 2e-3
WD = 1e-5
BETA_MAX = 1.0
WARMUP_EPOCHS = 10

class ConvVAE(nn.Module):
    """Symmetric Conv β-VAE, 64×64×1 → latent=128 → 64×64×1."""
    def __init__(self, latent_dim=128):
        super().__init__()
        self.enc = nn.Sequential(
            nn.Conv2d(1, 32, 4, stride=2, padding=1), nn.GroupNorm(8, 32), nn.SiLU(),
            nn.Conv2d(32, 64, 4, stride=2, padding=1), nn.GroupNorm(8, 64), nn.SiLU(),
            nn.Conv2d(64, 128, 4, stride=2, padding=1), nn.GroupNorm(8, 128), nn.SiLU(),
            nn.Conv2d(128, 256, 4, stride=2, padding=1), nn.GroupNorm(8, 256), nn.SiLU(),
        )
        self.fc_mu = nn.Linear(256 * 4 * 4, latent_dim)
        self.fc_lv = nn.Linear(256 * 4 * 4, latent_dim)
        self.fc_dec = nn.Linear(latent_dim, 256 * 4 * 4)
        self.dec = nn.Sequential(
            nn.ConvTranspose2d(256, 128, 4, stride=2, padding=1), nn.GroupNorm(8, 128), nn.SiLU(),
            nn.ConvTranspose2d(128, 64, 4, stride=2, padding=1), nn.GroupNorm(8, 64), nn.SiLU(),
            nn.ConvTranspose2d(64, 32, 4, stride=2, padding=1), nn.GroupNorm(8, 32), nn.SiLU(),
            nn.ConvTranspose2d(32, 1, 4, stride=2, padding=1),
        )

    def reparam(self, mu, lv):
        std = torch.exp(0.5 * lv)
        eps = torch.randn_like(std)
        return mu + eps * std

    def forward(self, x):
        h = self.enc(x).flatten(1)
        mu, lv = self.fc_mu(h), self.fc_lv(h)
        z = self.reparam(mu, lv)
        h2 = self.fc_dec(z).view(-1, 256, 4, 4)
        return self.dec(h2), mu, lv

def vae_loss(logits, x, mu, lv, beta):
    bce = F.binary_cross_entropy_with_logits(logits, x, reduction="sum") / x.size(0)
    kl = -0.5 * torch.sum(1 + lv - mu.pow(2) - lv.exp()) / x.size(0)
    return bce + beta * kl, bce.detach(), kl.detach()

# Training loop
for epoch in range(EPOCHS):
    beta = BETA_MAX * min(1.0, (epoch + 1) / WARMUP_EPOCHS)  # KL warmup
    for x, _ in LOADER:
        logits, mu, lv = model(x)
        loss, bce, kl = vae_loss(logits, x, mu, lv, beta)
        loss.backward()
        torch.nn.utils.clip_grad_norm_(model.parameters(), 5.0)
        opt.step()

# Evaluation: FID + IS via pytorch-ignite on 1000 vs 1000
```

### 5.8 HW2 文件清单

```
HW2/
├── Homework_2.pdf                  ← 作业要求
├── report.tex                      ← LaTeX 源（已填好数字）
├── neurips_2024.sty
├── train_vae_chest_xray.py         ← 训练脚本（287 lines）
├── VAE_ChestXray_Kaggle.ipynb      ← Kaggle 版 notebook
├── VAE_ChestXray_Kaggle.ipynb.bak  ← 备份
├── loss_curve.png                  ← 训练曲线图
├── samples.png                     ← 16 张生成样本
└── README_提交指南.md
```

---

## 6. 待启动 / 模糊状态

### 6.1 HW3
- vault 中无任何 HW3 相关文件
- 可能未发布，或已发布但未下载

### 6.2 Course Final Project
- `raw/ucsd/Spring 2026/ECE175B/proposal.pdf` 存在（2026-04-17）
- vault 中未对 proposal 内容做笔记或拆解
- **状态未知**——可能是 (a) 课程发的 proposal 模板/要求，或 (b) Javen 自己写的 proposal 草稿
- 评分占比：Proposal 10% + Midterm 20% + Final 40% = 共 70%

### 6.3 Midterm / Final
- 未启动

---

## 7. 后勤信息（来自 `grace-days.md`）

### 7.1 Grace days 余额

**4 / 5 days remaining**（HW2 用了 1 天）

使用记录：
| 日期 | 作业 | 用了几天 | TA 通知 | 余额 |
|---|---|---|---|---|
| 2026-05-12 | HW2 | 1 | 已发 `ruz048@ucsd.edu` | 4/5 |

### 7.2 Late policy（来自 lecture-1a syllabus）

- 0–24h late = 75% credit
- 24–48h late = 50% credit
- \>48h late = 0% credit
- Grace days 5 total，优先于扣分使用

### 7.3 联系方式

| 角色 | 名字 | 邮箱 |
|---|---|---|
| Instructor | Pengtao Xie | `p1xie@ucsd.edu` |
| TA | Ruiyi Zhang (PhD, ECE) | `ruz048@ucsd.edu` |
| Canvas | https://canvas.ucsd.edu/courses/74401（5/7+ 受 Canvas 勒索软件事件影响） |

---

## 8. Vault 中所有相关文件路径汇总

### 笔记 / Markdown
```
notes/ucsd/Spring 2026/ECE175B/
├── ECE175B_Lecture1a_课程导论与DGM概述.md
├── ECE175B_Lecture1b_贝叶斯网络.md
├── ECE175B_Lecture2_变分自编码器设计.md
├── ECE175B_Lecture3_变分推断与ELBO.md
├── ECE175B_Lecture4_生成对抗网络.md
├── grace-days.md
├── proposal.pdf
├── Homework_1.pdf
├── HW1/                            （8 个文件，详见 §4.7）
└── HW2/                            （9 个文件，详见 §5.8）

wiki/机器学习/
└── ECE175B_概览.md

wiki/机器学习/统一多模态生成架构.md  （HW1 调研沉淀，与 ECE175B 强相关）

notes/web-research/
└── 2026-04-20_多模态故事生成研究.md  （HW1 调研笔记）
```

### 原始 PDF（slides + 作业）
```
raw/ucsd/Spring 2026/ECE175B/
├── lecture-1a.pdf  (5.7 MB, 40 slides)
├── lecture-1b.pdf  (978 KB, 8 slides)
├── lecture-2.pdf   (1.2 MB, 9 slides)
├── lecture-3.pdf   (915 KB, 7 slides)
├── lecture-4.pdf   (937 KB, 7 slides)
├── proposal.pdf    (117 KB, 2026-04-17)
└── Homework_1.pdf  (2.5 MB, 2026-04-14)
```

### Lecture 配图（18 张 PNG）
```
attachments/ECE175B/
├── ECE175B_L1a_*.png  (6 张：title, DGM overview, disc vs gen, key problems, PGM overview, BN factorization)
├── ECE175B_L1b_*.png  (2 张：DAG example, LLM as BN)
├── ECE175B_L2_*.png   (3 张：VAE graph, decoder, three tasks)
├── ECE175B_L3_*.png   (4 张：inference problem, KL divergence, ELBO, EM algorithm)
└── ECE175B_L4_*.png   (3 张：GAN intro, architecture, training)
```

---

## 9. 给收件 Claude 的使用说明

如果你（接收这份文档的 Claude）需要继续 Javen 的 ECE175B 工作：

- **不要凭印象给数字**——所有用户报告里的数字必须从源文件原样抄录（CLAUDE.md 第 1 条规则）。
- **数据库锁定的数字**：HW2 的 IS=1.65, FID=209.89, params=2.96M, train images=5,856, wall time=18.0 min, epochs=30, KL warmup=10, latent dim=128, batch=128, lr=2e-3, wd=1e-5。
- **未知状态**：HW3、final project proposal 内容、midterm 形式——若涉及，先承认未知并问 Javen。
- **可用的源文件**：上面 §8 列出的 vault 路径都可以用 Read tool 直接读全文（如果你能访问 Javen 的 vault）。
- **架构 / 结果图**：`HW1/unistory_architecture.png`、`HW2/loss_curve.png`、`HW2/samples.png`——能 Read 图片就直接看。

---

**生成日期：** 2026-05-27
**生成方式：** Claudian 读取 vault 中所有 ECE175B 文件后整合
**源文件检查日期：** 截至 2026-05-27 11:38 PT
